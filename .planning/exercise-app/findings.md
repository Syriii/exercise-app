# 项目事实、假设与决策

## 2026-08-26：持续目标完成性审计

- 训练消耗估算的代码落点已经确认：`TrainingRepository` 需要提供带 revision 校验的独立更新方法；内存与 PostgreSQL 实现都必须把旧的 `expenditureAssessment` 随训练场次修订一起保存。新建训练场次默认没有估算，只有训练结束后、用户确认与官方标准中的具体活动模式相符并填写有效时长，系统才计算。
- 现有 `TrainingService` 只依赖训练仓库。为了按训练日期取得当时最近一次有效体重，同时不破坏大量既有单元测试，规划服务依赖将采用可选注入：生产与契约装配必须注入；未注入时估算接口明确返回不可用，而不是猜测体重。
- PostgreSQL 和内存训练映射当前都显式列举训练场次字段，因此新增 JSON 快照不能只改 Drizzle schema：`toSession`、`toSessionRevision`、新建场次、元数据修订和新增的消耗修订方法都要同步，否则类型检查或历史回溯会丢字段。
- `PlanningService` 已经在每日参考计算内部使用 `getLatestMeasurement(userId, localDate)`，但没有对训练模块暴露只读方法；可以增加同样经过日期校验的公开查询，而不让训练模块直接越过规划服务读取仓库。
- 训练 HTTP 响应使用 `additionalProperties: false` 的显式 schema；训练消耗快照必须加入场次和场次修订响应 schema，否则后端即使保存成功，Fastify 序列化也不会把它交给 Web。
- 当前仓库没有单独的测试目录，模块测试与源码同目录；后续验证命令不能引用不存在的 `apps/server/test`。
- 生产 API 已经先创建 `planningService`，因此给 `TrainingService` 注入它不改变装配顺序；契约应用和 E2E 测试目前相反，需要先创建规划服务再创建训练服务并注入，避免测试环境与生产能力不一致。
- 数据库新增字段均为 nullable JSONB，可安全生成非破坏性迁移；空库和既有库仍需后续在真实 PostgreSQL 上分别验证。
- 历史页已经具备训练场次展开、训练项修正、补记和场次修订记录，是训练消耗估算最自然的入口：只对已结束场次显示，和实际训练回顾放在一起，不挤入“今日计划”流程。
- Web 训练 API 目前尚无消耗活动/快照类型；需要同步类型、目录读取和保存方法。历史页加载时可一次读取小型官方活动目录，单个场次默认时长可用开始/结束时间差预填，但界面必须要求用户按“有效训练时长”确认，不能把墙钟时长当成自动事实。
- Drizzle 已生成 `0016_volatile_pretty_boy.sql`，内容只有给当前场次和场次修订表各增加一个 nullable `expenditure_assessment jsonb` 字段，没有删除、重命名或回填操作。
- 训练服务现有测试工厂没有规划依赖；消耗数值测试应单独建立真实的内存规划服务并写入完整档案/历史体重，验证 70kg、60 分钟、4.9 MET 得到总消耗 343 kcal、净消耗 273 kcal，同时验证进行中拒绝、无匹配模式不给数字、旧快照进入修订记录。
- 本轮页面验收时本机 5173 没有现成进程；根脚本的 `dev` 只启动 Web、不能提供 API。可先构建全仓库，再单独启动 `apps/server/dist/testing/e2e-server.js`（内存仓储）供浏览器走真实前后端契约。
- 浏览器端到端人工验收已证明：测试账号可以保存个人档案与 70kg 身体测量；空白训练可以补记 5 组杠铃卧推并结束；历史页展开后只对已结束训练显示“估算训练消耗”。活动下拉完整展示动作、RM、组次、休息和 MET，默认是“不完全相符”，选择 25RM 卧推并确认 30 分钟后正确显示总消耗 171.5 kcal、净消耗 136.5 kcal。
- 结果页同时明确“只用于训练回顾，不会改变今天还能吃多少”，满足训练与饮食独立更新的产品纠正。浏览器通过 role 能定位下拉框，但 `getByLabel` 没有匹配；可访问性快照已有名称，仍应在后续界面整理时考虑给动态表单控件增加显式 `id/for`。
- 在 390×844 移动视口中，训练历史页的 `bodyScrollWidth` 与 `documentScrollWidth` 都是 390，没有横向溢出；估算结果、公式折叠项、修正入口和底部导航均可见。信息密度偏高但未出现遮挡核心操作，适合作为当前移动端基线。
- 图片暂定采用的确认规则已重新核对：成功后若餐食没有当前采用值，候选必须自动形成“暂定”贡献并计入；用户可确认、修改或拒绝。已有手工值或确认值时只能新增候选，任何重新分析都不得覆盖。
- 当前实现只有 `manual | model_adopted` 来源和 `adoptedAt`，没有贡献审核状态。`process()` 先只把分析标成 succeeded；手动 `adopt()` 又先新增贡献、再标记分析，二者不是一个原子操作，进程在中间失败可能造成重复计入。现有测试名称和断言也明确固化了过时行为“候选等待用户采用”，必须随领域规则一起重写。
- 安全实现不能只在 `process()` 后调用现有两个公开服务方法：需要让“分析成功 + 若无采用值则写入唯一暂定贡献”具备幂等边界。PostgreSQL 可在图片分析仓储事务中完成跨表写入；内存仓储应使用同一分析 ID 的唯一性/锁语义模拟。贡献需增加 `reviewStatus: tentative | confirmed`，手工贡献默认 confirmed，暂定确认应生成修订而不重复贡献。
- 数据库已经有 `meal_contributions_source_analysis_uq` 唯一索引，能防止同一分析生成两个贡献；缺的是审核状态与成功事务内的自动写入。PostgreSQL `succeed()` 已经是锁分析与尝试的事务，可在同一事务再锁所属餐食，若不存在任何当前贡献则插入 whole-meal 暂定贡献并递增餐食 revision，从根本上避免“分析成功但暂定值没写入”的崩溃窗口。
- 现有手动 `adopt()` 可以兼容为“确认或修正模型结果”：若已存在相同 `sourceAnalysisId` 的暂定贡献，NutritionService 应更新该贡献为 confirmed 并保留旧 tentative 修订；若只是候选（餐食已有其他值），则按用户是否明确替换创建 confirmed 贡献。这样无需给用户制造第二套确认接口。
- API 进程和 Worker 进程都分别创建 Postgres 图片仓储与 NutritionService；把自动暂定写入放在 `PostgresImageAnalysisRepository.succeed()` 后，不需要依赖 Worker 内另一份服务实例，而且两进程共享同一数据库事务语义。
- 手工贡献的创建/修改输入不应让浏览器提交审核状态：服务端固定把普通手工值写为 confirmed。食物模板不属于当前采用值，构造模板输入时需剔除新增的 reviewStatus 字段。
- Web 当前只显示贡献来源，增加 reviewStatus 后要对暂定值给出显眼但不阻断的“照片估算·待确认”提示；每日汇总仍计入暂定值，符合领域规则。
- NutritionPage 的轮询目前只刷新分析列表，不重新读取餐食与日汇总；即使 Worker 正确写入暂定贡献，页面也要刷新才看得到。轮询检测到任务从 pending/running 进入 succeeded 后，应重新读取当天餐食和汇总，并保留分析表单本地状态。
- 现有页面文案“结果只是候选，不确认就不会计入”和上传成功“回来确认”已经与确认规则冲突。应改为：无当前值时完成后先暂定计入；已有值时只保留候选；用户仍需核对、修正或移除。分析状态也应根据对应贡献的 reviewStatus 显示“暂定计入”或“已确认”，而不是一律“等待确认”。
- 现有图片服务测试正好覆盖迁移路径：成功后应断言一条 tentative 贡献已计入；随后通过既有 adopt 接口修改为 confirmed，贡献 ID 不变、revision 增加、旧 tentative 进入修订，并按选项删除原图。
- 当前图片上传使用普通 `fetch`，只能显示“正在上传”，浏览器端拿不到上传字节进度；为满足真实进度，应给二进制上传单独使用同源 `XMLHttpRequest.upload.onprogress`，仍保留 Cookie、统一解析 `ApiError`，其余 JSON API 继续使用 fetch。
- 客户端压缩采用安全可逆规则：JPEG/PNG/WebP 通过 `createImageBitmap + canvas` 限制最长边 1920px、输出 WebP 0.82；只有生成文件确实更小时才替换上传文件，GIF 保持原样。界面始终显示原始大小与实际上传大小，不能宣称未发生的压缩。
- 历史趋势无需新建第二套统计事实：HistoryPage 已加载 90 天训练与餐食，再并行读取身体测量即可派生按记录日期的趋势行。训练显示实际场次/完成动作，饮食只对已知营养求和，体重只显示当天有测量的值；缺少记录一律显示“—”，不补零、不推断健康变化。
- 当前历史 CSS 已有移动端列表与卡片规则；新增趋势适合用可横向滚动的语义表格和三项摘要，避免引入图表库或把稀疏记录画成连续曲线。
- 饮食计划最小但不降级的领域形态确定为“日期范围 + 自由标题/原则 + 可选条目”：条目可以绑定范围内某一天，也可以对整个范围生效；每条含可选餐次名、计划食物文本和备注。这样同一对象可表达宽泛原则、一天安排、周计划或具体餐次，不强迫所有人使用同一粒度。
- 计划适合作为直接归属 user 的 `diet_plans` 根表，条目先以版本化 JSONB 快照保存；更新递增 revision，归档而非硬删除。它必须加入用户导出、RLS 和账号级级联删除，不能只做浏览器本地状态。
- 现有 RLS 是一次性迁移 0013；新表迁移必须自己追加 `ENABLE ROW LEVEL SECURITY` 和 `exercise_api` 账号策略，Drizzle 自动生成不会替我们加入这部分。
- `0018_wakeful_carmella_unuscione.sql` 的自动部分只创建 `diet_plans`、外键和索引，尚无 RLS；需手工在同一迁移末尾添加账号策略。用户导出器采用根表白名单，已加入 `diet_plans` 后会自动去掉 `user_id` 并导出 JSON 条目。
- 饮食计划服务测试应覆盖：范围重叠查询、全范围原则与指定日期餐次并存、跨账号隔离、修订冲突、条目日期越界和归档后默认不可见；路由测试至少证明正常序列化和第二账号列表为空。

- 规划恢复确认当前 Web 增量 0–6 已标记为本地完成；目标尚未完成的证据门集中在真实 PostgreSQL/Docker/pg-boss/DeepSeek/备份恢复，以及产品定义中的公共食物目录。
- `session-catchup.py` 正常退出且没有报告未同步上下文；当前工作树和规划文件可作为本轮审计起点。
- 本轮不会用已有单元测试替代部署范围验证；先从产品、交付方案和部署验证脚本逐项建立证据矩阵，再判断当前环境还能补哪些自动验收。
- 产品与交付方案复核新增两个必须实证的 Web 要求，不能只沿用“增量 0–6 已完成”结论：实际训练需要运动消耗近似估算并保存输入/方法/范围；动作指导需要视频或同等指导内容及可追溯来源。下一步直接检查代码和界面证据。
- 手工饮食要求“食物搜索、常用项、最近使用项、份量选择”，当前个人搜索只证明后两类；公共目录许可缺口不影响手工闭环，但产品所写“大量食物库”尚不能标成完整交付。
- 自部署完成条件还要求空库迁移、已有数据升级、容器重建持久化、事务/崩溃恢复、备份恢复和真实手机/桌面回归；当前本地 E2E 只覆盖内存服务，部署脚本存在不等于这些运行证据已成立。
- 代码检索确认训练模块没有任何运动消耗字段、MET 计算或界面展示；命中的 `energy_kcal` 全部属于饮食模块。因此交付方案“实际训练运动消耗估算”并未实现，之前的增量 2 完成声明过宽。
- 动作指导已有许可、版本、来源、动作步骤、常见错误、替代动作和安全限制，并明确无许可视频时不伪造；它满足“同等指导内容”的最低结构，但当前仅为 MIT 项目原创且未经专业审阅，不能宣称提供标准姿势视频或专业审阅内容。
- 领域规则与证据登记已经给出运动消耗实现契约：DERV-002 使用 `MET × 当日有效体重 × 时长` 和 `(MET−1) × 当日有效体重 × 时长`，必须保存总/净消耗、活动映射、时长、体重、证据 ID、方法版本和限制；无法可靠映射则明确返回未知。
- E-003 可支持 18–64 岁健康成人和标准覆盖活动，但其 202 项表格禁止未经许可整表复制。实现应只登记产品实际支持的少量训练分类和值及精确出处，不导入完整标准表。
- 当前仓库只登记了 E-003 的公式和“抗阻训练位于表 1 第 3 页”，没有保存可实现的具体抗阻活动 MET 值。为避免凭记忆填数，下一步必须重新读取官方 PDF 的对应页，且只转写实现所需的最小映射与出处。
- 内置网页抓取器本轮直接打开中国体育科学学会公告和两组搜索均未返回可读内容；这不是来源不存在的证据。下一步改用只读网络请求取得公告 HTML，解析其官方附件链接。
- 受控网络请求已从中国体育科学学会公告的附件链接取得正式发布 PDF（9 页，A4，2023-04-14 元数据），保存在临时目录，仅用于核对且不会进入仓库。下一步按 PDF 规范同时做文本定位和对应页视觉复核。
- 系统存在 `pdfinfo` 但没有 `pdftotext`；工作区依赖定位工具本轮未返回可见路径。后续直接探测当前 Python 的 `pypdf`/`pdfplumber`，若可用则按只读方式提取，视觉复核仍通过可用渲染器或 PDF 截图完成。
- 已定位 Codex bundled `pdftotext` 并从官方 PDF 提取到抗阻训练最小映射：例如杠铃卧推 25RM/5×25/组间 1 分钟为 4.9 MET，12RM/3×12/组间 2 分钟为 5.2 MET；哑铃深蹲对应 5.7/6.8 MET；组合手臂/胸/腹对应 5.7/6.6 MET。数值都绑定非常具体的动作、RM、组次和间歇，不能泛化成任意“力量训练”。
- 当前训练记录只保存动作组次/重量，没有整次训练的实际有效时长、RM 分类或组间间歇，无法忠实匹配上述具体项目。安全的首个闭环应允许用户为已结束训练补录“标准映射 + 实际持续时长”，否则明确显示无法可靠估算；不从组数自动猜时长或强度。
- 官方 PDF 第 6 个物理页（标准正文页码 3）已视觉复核，表格列名、活动条件、MET 和强度分类与文本提取一致；页面清晰显示抗阻项目均是具体实验组合，而非通用强度标签。这进一步排除用单个“中等力量训练”常数覆盖任意训练的做法。
- 训练会话已有不可变开始/结束时间、状态、账号、日期和实际动作，但没有消耗估算快照；现有修订表只保存训练日期/时区/备注旧值。新增消耗必须走独立、可修订的会话子资源，不能塞进饮食能量字段或改写每日营养参考。
- 可复用现有乐观修订号：只允许已完成/已放弃会话设置或清除估算，写入时保存上一份估算修订；页面在历史详情展示“未估算 / 无法可靠估算 / 已估算”以及总活动和净活动两个数字。
- `PlanningService.listMeasurements` 已提供账号内测量时间序列，运动消耗可选择训练日期当日或之前的最新有效体重，不需要用户重复输入；未来日期的体重不能用于回算旧训练。
- 训练 session 结束接口已经保存 `endedAt`，可把实际起止差作为默认时长，但用户必须能修正为标准活动所覆盖的有效时长；不能默认整个页面开启时长都属于目标活动。
- 内存和 PostgreSQL 的训练元数据修订都在同一乐观修订边界内先保存旧快照再更新会话；把 `expenditureEstimate` 同时加入 session 与该修订快照，可以用同一模式安全追溯消耗修正，不必新增第 42 张表。
- 训练响应 Schema 是严格白名单，新增估算字段必须同步 session 与 revision 的 OpenAPI schema、序列化、Web 类型和 E2E；否则数据库值会被 Fastify 响应过滤掉。
- 规划仓储已经有 `getLatestMeasurement(userId, localDate)`，只需由 PlanningService 暴露，不必自行过滤全量测量；这能保证旧训练只使用该日期及之前的体重。
- 为不破坏大量既有训练测试，运动消耗可作为 TrainingService 的可选规划依赖：普通训练能力保持独立；只有调用消耗估算时要求装配 PlanningService，生产、contract 和 E2E 入口必须显式传入。
- 运动消耗数据模型首步已加入训练会话与会话修订 JSON 快照类型，并在仓储契约增加乐观更新入口；没有新增账号表，因此现有 41/31 表访问分类口径暂不变化。
- 产品代码检索没有饮食计划或未来餐次/食物安排实体；当前只有系统参考和实际餐食，因此 `product.md` 中“用户可以制定自己的饮食计划”“可选餐次或食物安排”尚未实现。
- 历史页实现了最近 90 天按日期合并、筛选和修正，但没有训练量、体重或营养变化趋势视图；它证明“回顾历史”，尚不足以证明产品文字中的“变化趋势”。
- 图片流程已有拍照/选图、上传中禁用、后台等待、失败重试、候选修正和采用；但没有客户端压缩、压缩前后提示或可量化上传进度。交付方案“图片压缩提示、上传进度”目前只有二态“正在上传”，仍需补强或明确验收口径。
- 图片候选已经保存食物、可见份量、四项营养、置信等级、假设、不确定性、模型和 prompt 版本；这一部分不是缺口。
- 图片分析成功后当前状态是“等待确认”，只有用户点击采用才创建 `model_adopted` 贡献；数据库没有暂定/已确认采用状态。它与已确认规则“餐食没有当前采用值时自动成为暂定采用值并计入”相冲突，是第二个需要修复的核心业务缺口。
- 当前采用流程也把创建营养贡献和标记分析已采用拆成两个独立仓储写入；如果进程在两步之间退出，可能留下已计入但分析仍可再次采用的结果。修复暂定采用时应同时解决幂等/事务边界，不能只在前端自动调用现有接口。

## 2026-08-26：训练建议切片与收口审计

- 训练建议已接入 Web：用户可以按目标、经验、器械和时间生成候选方案，查看适用范围与官方证据，随后采纳为普通训练方案或放弃。
- 训练建议页面已通过桌面端与移动端端到端测试（2/2）；证据说明位于可展开区域，测试已按真实交互打开后校验。
- 后端训练建议的服务、路由、访问策略和 OpenAPI 定向测试已通过（4 个测试文件、6 个测试）。
- 一次宽范围的可移植性与硬编码计数审查输出被截断；下一步改为小范围复核，重点确认账号数据导出包含训练建议，以及 41 张表 / 31 张 RLS 表的口径一致。
- 当前仍需处理训练建议采纳的并发/故障幂等性：界面禁用重复点击只能覆盖前端，服务端仍需保证同一建议不会生成多个训练方案。
- 可移植性导出器当前通过显式账号表清单导出数据，清单中尚未看到 `training_suggestions`，需要补入并增加回归测试。
- 第二次宽范围计数搜索仍因历史规划材料和 Drizzle 快照命中过多而截断；后续只检查运行代码、迁移和当前文档中的精确词组，不再扫描整个历史目录。
- 用户数据导出器原本漏掉新建的 `training_suggestions` 根表；现已加入显式导出清单。训练建议本身没有独立子表，因此无需增加子查询。
- 可移植性模块当前只有服务层的固定导出器测试，没有 PostgreSQL 导出器查询清单测试；应补一个轻量测试，使未来新增账号表却忘记导出时能被自动发现。
- 训练建议采纳当前顺序是“先创建训练方案，再把建议标为已采用”。乐观修订能阻止第二次状态写入，但不能阻止并发请求各自先创建方案，也不能处理创建方案后进程崩溃；需要把建议来源变成训练方案的唯一幂等键，或在同一数据库事务内完成两步。
- 访问分类已把 `training_suggestions` 列为直接账号表，并纳入 31 张 RLS 表；数据导出清单补齐后，账号删除仍由用户外键级联覆盖。
- 训练方案仓储目前只接受普通创建输入，没有建议来源字段。收口方案应在训练方案表增加不对用户展示的 `source_suggestion_id` 唯一来源，并提供“按建议创建或返回已有方案”的专用仓储能力；这样并发重试不会复制方案，仍保留建议与方案两个领域对象。
- PostgreSQL 的训练方案创建已经在事务内同时写入方案与动作，因此可以复用这条边界：专用“从建议创建”写入唯一来源键，冲突时读取已存在方案。进程若在建议状态更新前退出，重试会复用原方案并完成状态更新，不会留下重复方案。
- 幂等创建已同时加入内存仓储和 PostgreSQL 仓储，普通用户创建方案的接口保持不变；建议采纳改走专用入口。还需生成增量迁移并用并发回归测试证明只生成一个方案。
- Drizzle 已基于当前 41 张表生成 `0015_sloppy_ghost_rider.sql`，只增加 `training_templates.source_suggestion_id` 和唯一索引，没有夹带其他 schema 变化。
- 当前运行文档中的数据库口径一致：41 张 schema 表、31 张启用 RLS 的账号表；未发现仍使用旧 40/30 口径的当前文档或运行代码。
- 并发测试第一次暴露了内存仓储检查后写入的竞态；增加按账号与建议键共享的进行中 Promise 后，重复采纳只产生一份训练方案。全仓类型检查及训练建议、导出清单、RLS 定向测试现已通过（4 文件、7 测试）。
- 训练建议路由当前响应 Schema 使用 `additionalProperties: true`，只能证明路径存在，不能让 OpenAPI 消费者知道候选、输入快照和状态字段；收口时应改为显式响应契约。
- 部署烟雾脚本已使用 31 张 RLS 表的新口径；真实 PostgreSQL 集成测试已存在，但还需确认它覆盖新训练建议表的双账号隔离。
- 现有 PostgreSQL RLS 集成测试只实际验证 `meals` 与其子表，不能证明新 `training_suggestions` 策略在真实数据库执行；应在同一双账号事务中插入两份建议并断言 API 角色只看到当前账号。
- 训练路由已有严格的训练方案输入/响应 Schema，可导出并复用于建议接口，避免两套动作字段契约漂移。
- 训练建议 OpenAPI 响应已改为显式契约，并复用训练方案输入/响应 Schema；定向 OpenAPI 测试随服务、路由、导出与访问策略共 5 个文件、8 项测试通过。
- PostgreSQL 集成用例已加入两账号训练建议数据，并在 `exercise_api` 事务上下文中断言只可见当前账号的建议。由于本机仍无 PostgreSQL/Docker，这一新增断言只能在真实部署环境执行，不能宣称本地已通过。
- 当前 `task_plan.md`、`AGENTS.md`、README 和交付方案仍把朋友与开源增量写成“进行中”，但代码层的 RLS、限流/配额、密码重置、隐私/安全/贡献材料现已存在；本地全量验收通过后应统一改成“本地完成，真实服务器待验收”。
- 当前工作区包含从早期地基到训练、规划、饮食、图片、长期运行和安全收口的大批未提交改动。这些不是临时垃圾文件，而是尚未提交的完整实现；本轮只做一致性收口，不删除或覆盖用户已有内容。
- Android 仍受已确认门槛约束：必须先由产品所有者实际使用 Web 并认可体验，再进行 Capacitor 真机验证。它不能在当前无 Android 环境时被冒充为“剩余 Web 开发”。
- 当前全量单元/服务测试通过：23 个测试文件、88 项测试；服务端与 Vue 生产构建通过，部署 Shell 脚本语法和 `git diff --check` 也通过。Compose YAML 的首个校验命令因本机旧 Ruby 参数不兼容，需要单独重跑。
- 使用本机 Ruby 2.6 兼容方式后，两份 Compose YAML 均可解析；完整 OpenAPI 文档也成功生成，训练建议和个人食物搜索路径已进入契约。命令输出约 14 万字符而被界面截断，但进程以 0 退出，路径级完整性另有 OpenAPI 自动测试覆盖。
- 全量 Playwright 回归通过：桌面 Chromium 与移动 Chromium 共 25 项，覆盖注册登录、预置管理员改密、管理员运行摘要与临时密码、训练方案/周期/日程/实际记录、系统训练建议、官方营养规划、手工饮食与个人食物搜索、图片候选修正采用、提醒、导出和账号删除。
- 生产依赖 `npm audit --omit=dev` 返回 0 个已知漏洞。
- 仓库敏感模式扫描未发现私钥、长格式 API key 或 DeepSeek 实际密钥；未跟踪文件检查只因文件名规则命中受版本控制的备份脚本源码，没有发现 `.env`、数据库、日志、照片、私钥或真实备份产物。
- 当前文档存在阶段状态滞后：`docs/product.md` 仍写 Phase 4 草案形成中，交付方案仍写增量 6 实施中，AGENTS/README/task plan 也未反映本轮训练建议、个人食物搜索和安全收口。应统一更新，但保留真实 Docker/PostgreSQL/DeepSeek/备份恢复为服务器验收门。
- 产品能力仍有两个不能在当前环境伪造完成的后续项：合法可再分发的大型公共食物目录尚无确定数据源；Android 必须等 Web 实际使用认可和真机环境。当前可完成的是完整 Web 本地实现及服务器部署材料，不应把这两个外部门槛写成代码失败。
- `task_plan.md` 的“继续后移”段仍列出已经完成的训练日期、营养、图片任务与候选流程，应替换为真实的服务器验收、公共食物数据源和 Android 门槛，避免恢复工作时重复开发。
- 阶段文档已统一到“Web 增量 0–6 本地完成、真实服务器待验收”；旧的 Phase 4 草案、增量 6 进行中和重复开发下一步已清理。`git diff --check` 继续通过。
- `source_suggestion_id` 只作为 PostgreSQL 内部幂等字段；训练方案映射器显式挑选公开字段，不会把该来源键泄露到普通训练方案 API。
- 最终当前文档与测试检索确认：隐私说明覆盖导出/删除与管理员不可读取健康数据，安全说明覆盖训练建议 RLS；OpenAPI 测试包含训练建议和个人食物搜索路径。工作区共有 115 条修改/新增状态，反映整个 Phase 5 尚未提交的实现，不是 115 个失败项。
- 最终轻量复核再次通过全仓严格类型检查和 `git diff --check`；当前分支为 `main`，远端仍正确关联 `https://github.com/Syriii/exercise-app.git`。本轮未提交、未推送，符合仓库规则。

> **状态：规划重置前材料。** 本文件保留历史发现与风险，不再驱动讨论或充当当前产品事实源。

> 本文件只保存规划所依据的信息。事实、假设与决定必须分开，避免把设想误当成需求。

## 2026-08-30：第二轮界面与文案审计

- 产品所有者在真实使用中指出出生日期原生日期框只有局部易于触发，希望整块字段都可以点击。
- “能量公式分类”把实现细节暴露成可随意修改的设置；产品语义应是基础资料中的性别，方程选择由系统在内部完成。
- 当前全站已经有 Sport / Workbench 的 OKLCH、4pt 间距和字体 token，但控件只提供 `0.25rem`、`0.75rem` 和 pill 三档半径，面板、输入、按钮、状态条之间混用后出现圆角与平角过渡不协调。
- 当前中文 display 栈以 `Arial Narrow` 开头，但该字体通常不含中文字形，中文标题实际回退到与正文相近的系统字体；同时标题普遍使用 800，正文/帮助/状态文字在多个字号和颜色之间切换，容易显得忽大忽小。
- 页面文案反复解释“不会改变记录”“不会算成 0”“不是必填”“仍可继续使用”等边界。单条都正确，但在同一视图重复出现会稀释主要操作；应把长期规则留在一处，把当前状态和下一步放在组件附近。
- 本轮不改变训练、饮食、历史和安全领域规则，不删除路由或业务组件；只修正资料字段语义、交互命中区、视觉 token/样式和面向用户的文字。
- 线上设置页桌面目视确认：页面标题约 56px、面板标题约 31px、正文 16px、说明 15px，标题又统一使用 800 字重，导致面板内容比表单本身更抢眼；本轮应降低应用级标题上限和面板标题等级，并把中文标题改用稳定的 CJK UI 字体栈。
- 个人档案首屏同时出现页面说明、面板说明、两个字段帮助和“修订 0”，信息密度超过完成基础资料所需；“修订”属于并发/历史实现信息，不应作为普通用户首要状态。
- 出生日期的原生输入外观完整，但浏览器默认只保证输入与日历图标响应；需要让带标签、输入和帮助文字的字段区域主动调用原生日历选择器，同时保留原生键盘与无障碍行为。
- 性别选项当前暴露为“男性方程 / 女性方程”，并配“不是身份称谓”的防御性解释，既像实现设置也增加 AI 式说明。应改为“性别：男 / 女”，简短说明系统会据此选用适用公式。
- “今天”空状态同一屏三次解释缺少数据不会被猜测或算成 0，并直接展示内部方法版本 `daily-reference-2026-08-26.1`；普通视图应只保留“按已记录内容计算”和补资料动作，把计算版本放到可展开依据或诊断信息。
- “今天”标题下“安排会放在这里”和训练空状态“不会把方案库算成未完成”表达的是同一规则；保留离操作最近的一处即可。空状态应直接说“今天还没有安排训练”，下一步按钮已经足够。
- 训练页首屏连续出现“选一个方案或直接开始”“方案只是备选”“不选具体方案也可以”“边练边补动作”四次自主性说明。规则本身正确，但反复辩解显得像 AI；标题和空状态各保留一句，导航说明进一步缩短。
- 左侧导航每项同时显示单字、标题和完整描述，在 18rem 侧栏内被省略号截断；说明文字既不可完整阅读又制造字号层级噪声。桌面导航可只保留标题和更短的辅助词，或隐藏被截断的说明。
- 饮食页把官方限制、方法版本和缺资料消息全部直接铺在“系统参考”卡片内，空档案时形成三段解释和一条内部版本号。首层只需指出“补充出生日期、性别和身高后生成参考”并提供入口；依据、适用范围和方法版本收进可展开详情。
- 饮食空状态同时出现“全天覆盖未知”“四项已记录未知 / 本项有未知值”和“还没有餐食记录”，在没有任何餐食时重复表达未知。无记录时每项只显示“尚未记录”，覆盖确认放到有餐食之后更合适。
- “我的饮食安排”同时说明“不覆盖系统参考、不等于实际已吃、不是必填任务”，语义正确但一次出现三个否定句。改成正向的一句“提前写下准备怎么吃；实际摄入仍以餐食记录为准”即可。
- 历史页当前空状态已经收敛：标题、说明和“回到今天”动作各一项，没有重复趋势卡。本轮只需要微调页面标题和说明语气，不应重新扩张结构。
- 管理页面向管理员，运行状态中的限制说明和结构化状态有必要保留；可压缩的是“真实探测结果”等防御性表述，不应删掉模型未配置、备份/恢复尚无记录等运维事实。
- 当前浏览器已有登录会话，访问 `/login` 会回到“今天”，因此登录/注册页不在生产会话中强行退出后检查；后续使用本地 E2E 的独立账号环境复核，避免改动真实会话。

## A. 已确认事实

### 项目意图

- 产品最初服务于项目所有者本人。
- 验证后希望开放给朋友使用，长期希望开源给更多人。
- 长期关注两个问题域：
  - 如何训练，以及实际练了多少；
  - 吃了什么，以及营养结构如何。
- 希望先做网页端，达到满意效果后再转为 Android APK。
- 视觉偏好是运动、简洁、实用。
- 用户明确希望先做规划和讨论，而不是直接产出完整第一版。

### Phase 1 访谈证据

- 项目的直接触发点是看到一款健身 App，并认为其中有值得借鉴的设计。
- 用户明确提出的产品能力包括：
  - 自己制定运动计划和饮食计划；
  - 判断当天是否去健身房，并安排练习动作；
  - 查看动作标准姿势的视频；
  - 根据个人体重估算运动消耗；
  - 给出每日蛋白质、脂肪及另一项尚待确认的营养摄入目标；
  - 拍摄一顿饭，由 AI 估算各项营养摄入。
- 这些能力属于已确认的产品目标；它们的交付顺序尚未确定。
- 参考 App 的名称、实际使用深度、不能直接满足需求的原因尚未说明。
- 用户没有实际使用参考 App，只看过截图；因此目前不存在与该产品的真实使用对比。
- 最能引起用户兴趣的单项能力是“拍照后由 AI 估算一餐的营养”。
- 这提示“降低饮食记录和营养估算成本”可能是首要问题，但目前仍是假设，需要通过现实场景验证。
- 用户目前主要吃食堂餐；菜品缺少明确配方、份量和营养标签，因此很难手动记录。
- 用户目前没有形成可行的记录方式，但明确希望进行饮食记录。
- 当前最有证据支持的问题候选不是单纯记录食堂餐，而是建立“每日营养目标与实际摄入之间的低成本反馈闭环”。
- 照片难以直接判断隐藏用油、调味料和精确配方，因此“快速、可修正、有误差提示的估算”比“精确计算”更符合实际。
- 用户期望的完整流程是：
  1. 根据个人信息与目标计算一天需要的蛋白质、脂肪等营养目标；
  2. 拍摄每顿饭，估算该餐热量和营养成分；
  3. 将每餐估算累计到当天，并与每日目标对照。
- 食堂是拍照估算价值较高的现实环境，但不是产品目标本身。
- 用户明确认为训练与饮食在健身目标上不可分割，但两件事本身应当独立。
- 用户期望的联动原则是：当天训练较多时相应提高饮食摄入目标，训练较少时相应降低。
- 因此当前产品模型是“两个独立模块 + 一层每日目标联动”，而不是把所有能力做成一个不可拆分流程。
- 用户认为饮食是健身过程中难度最大的部分，尤其是在依赖食堂、无法得知配方和份量的情况下。
- 这一表述使饮食目标与拍照估算成为目前证据最强的首要价值；训练仍是独立模块和营养调整的必要输入。
- 训练模块的目标能力已经明确：自定义训练计划、今日训练安排、动作标准视频、实际训练量记录、基于个人信息和训练情况的消耗估算、训练提醒。
- 用户工作日可能通常在晚上训练，但周末、假期或其他安排下也会在白天训练；营养参考需要在训练完成前先依据计划给出暂定值，不能把晚上设为默认时段。
- 如果计划训练没有完成：
  - 当天还会继续进食时，应根据实际活动调整剩余摄入参考；
  - 用户已经结束当天进食时，不能改写已吃内容，应提示当天摄入与最新参考的偏差；
  - 状态未知时只显示参考变化。
- 这证明联动需要同时考虑训练计划、实际完成状态和后续进食状态，而不只是一个静态公式或时钟判断。
- 用户明确要求训练提醒与饮食提醒分开：
  - 训练提醒只处理训练计划、开始和完成状态；
  - 饮食提醒只处理摄入目标、缺口和超标状态；
  - 两个模块共享联动后的数据，但不混淆提醒语义。
- 用户确认个人使用阶段需要同时满足三类成功表现：
  - 可以持续记录饮食；
  - 可以知道当天还应该摄入多少；
  - 可以更稳定地完成训练计划。
- 三类表现分别对应持续使用、饮食决策支持和训练执行，具体验证周期与最低达标线尚未确定。
- 用户确认第一轮个人验证周期为连续 14 天。
- 用户明确反对把使用频率变成产品对用户的强制要求。
- 用户认为使用多少应由用户决定；按照设计，记录越多效果越好，但少用仍应可以正常使用。
- 因此“14 天”是验证观察窗口，不是连续打卡要求；使用频率是观察信号，不是产品内的达标配额。

### 仓库与工程

- GitHub 仓库为 `https://github.com/Syriii/exercise-app`，可见性为 Public。
- 本地仓库默认分支为 `main`，远程名称为 `origin`。
- 当前尚无首次提交；Git 提交身份尚未配置。
- 当前工程采用 React、TypeScript、Vite 和 npm。
- 现有页面是可运行的项目骨架，不包含训练或饮食业务能力。
- 当前没有路由、状态管理、业务数据库、账号、PWA 或 Android 工程。
- 依赖审计、类型检查、生产构建和既有页面响应式检查均已通过。

## B. 尚待验证的产品假设

- 首个 MVP 需要同时包含训练与饮食的最小能力，才能验证跨模块联动。
- 计划训练量还是实际完成训练量应作为营养目标的主要调整依据。
- “指导 + 记录 + 回顾”是最合适的首要闭环。
- 项目所有者的需求能够直接代表朋友或开源用户的需求。
- 每日聚合首页能够显著提高使用价值。
- 本地优先、无账号能够满足个人阶段。
- Web 流程稳定后可以低成本封装为理想的 Android 体验。
- AI 生成、动作识别或食物识别能够解决核心问题。

这些内容不得在获得证据前写成确定需求。

## C. 已做决定

| 决定 | 适用范围 | 依据 | 可否重审 |
|---|---|---|---|
| 使用公开 GitHub 仓库 | 项目管理 | 用户明确要求未来开源 | 可以，但目前无理由 |
| 使用 MIT License | 当前代码许可 | 与公开复用目标一致 | 发布前可重审 |
| 保留现有 React + TypeScript + Vite 骨架 | 探索阶段工程 | 已可运行且验证成本低 | 产品需求不适配时可重审 |
| 暂停业务功能开发 | Phase 1 | 用户要求先规划和讨论 | 产品关卡通过后结束 |
| Web 在 Android 之前验证 | 交付顺序 | 用户明确要求 | 新证据出现时可重审 |

## D. 当前未知，按讨论顺序排列

### Phase 1：产品定义

1. 每日营养目标主要服务于什么身体或训练结果？
2. 训练提醒与饮食提醒分别在什么时间和条件下触发？
3. 用户最关心哪些目标：总热量、蛋白质、碳水、脂肪或其他指标？
4. 每日目标由什么个人信息和活动信息计算，是否允许用户自行修改？
5. 用户能接受为了提高拍照估算准确度补充多少信息或手动修正？
6. 首个 MVP 需要两个模块各自做到什么最小程度？
7. 哪些可观察证据可以证明它比当前方法更有用？

### Phase 2：领域需求

- 选定场景所需的个人背景、限制和上下文。
- 指导内容的来源、责任边界和更新方式。
- 训练或饮食数据的最小记录单位、字段与单位。
- 修改、撤销、中断和恢复规则。
- 健康风险、免责声明和需要建议就医的边界。
- 内容与数据来源的版权、许可、地区覆盖和误差。

### 后续阶段

- 信息架构、页面数量和导航方式。
- 存储、离线、备份、账号及云同步需求。
- 产品名称、语言和国际化策略。
- Web Beta、朋友测试和开源发布方式。
- Android 封装方案与分发渠道。

## Phase 1 当前产出

- Product Brief v0.1：`docs/archive/product-brief-v0.1.md`
- 状态：讨论稿
- 下一项产品定义任务：建立个人验证的成功标准。

## E. 风险

- 健身与营养涉及健康风险，产品不能把一般性建议包装成医疗判断。
- 如果同时实现两个完整领域，首版范围可能失控。
- 如果先选技术或页面再理解场景，数据模型和交互会反复返工。
- 个人数据长期积累后，导出、迁移和隐私问题会变得关键。
- 营养数据存在品牌、地区、份量和许可差异。
- 朋友的目标和习惯可能与项目所有者明显不同，个人验证不能代替朋友测试。
- “持续使用”和“身体结果改善”不是同一件事，成功指标必须避免错误归因。

## F. 现有资产及其地位

| 资产 | 当前作用 | 是否构成产品决定 |
|---|---|---|
| `apps/web/src/app/App.tsx` | 展示项目骨架 | 否 |
| `apps/web/src/app/modules.ts` | 标记两个长期问题域 | 否 |
| `apps/web/src/styles/tokens.css` | 初始视觉令牌 | 否，可在原型阶段调整 |
| `docs/product-decisions.md` | 保存早期项目意图 | 需要以后按新格式复核 |
| `.planning/exercise-app/task_plan.md` | 当前唯一有效路线图 | 是，版本为 v3 |
| `.planning/exercise-app/progress.md` | 保存工作与验证历史 | 否 |

## G. 已有验证记录

- 页面在 320、375、414、768 和 1280 像素宽度下无横向溢出。
- 触控目标和首屏布局通过已有检查。
- 浏览器控制台无错误或警告。
- Hallmark 58 项检查全部通过。

这些验证只证明工程骨架可运行，不证明产品价值或业务流程有效。

## H. 2026-08-20 产品文档重构审计发现

> 本节记录多智能体审阅发现，不直接构成产品事实；正式事实以重写后的 `docs/product.md` 为准。

- 当前产品稿的主要问题是信息架构，而不是能力数量：产品事实、运行约束、技术候选、代表场景和后续阶段任务混在同一层级，并存在多处重复。
- 重写需要统一术语并把内容归入产品定位、用户与演进、产品原则、能力地图、代表场景、当前运行约束和后续产出。
- 必须修正三项高优先级内容：
  - Android APK 是 Web 满意后的明确后续交付目标，不能写成仅“再考虑”。
  - 晚间训练与晚餐先后顺序是一个代表场景，但不能扩写为所有训练都发生在晚上；一般规则必须覆盖任意训练时段和当天是否还会继续进食。
  - 用户早期明确提出可以自己制定饮食计划；重写需保留该高层意图，但其具体含义留到领域规则阶段。
- “允许 IP + 端口访问”不能扩写成“不需要安全连接”；是否使用 HTTPS、VPN、公网或局域网属于技术设计。
- 产品层保留长期服务器、数据不进入公共仓库、部署后数据不丢失、Codex 受控异步处理和未来分析执行者可替换等约束；具体目录、卷、CLI、MCP 和数据库后移。
- 模型原始估算与最终采用值都保留，但 Phase 1 不规定是否必须逐餐点击确认或默认采用哪一个结果。
- README 对 `findings.md` 和 `product-decisions.md` 的角色说明与 `AGENTS.md` 不一致，需要在正式文档重写后同步修正。

## I. 2026-08-20 GitHub 仓库结构调研

> 本节记录外部项目的目录组织方式与可借鉴结论，不构成本项目的产品事实或技术选型。

- OpenAI Codex 仓库把主要代码工作区（如 `codex-rs/`、`codex-cli/`、`sdk/`）、文档 `docs/`、工具 `tools/` 和脚本 `scripts/` 分开；根目录保留入口文件、治理文件和构建配置。
- OpenAI Agents Python 仓库采用 `src/`、`tests/`、`examples/`、`docs/` 的常见分层，根目录的 `AGENTS.md` 主要描述代码结构、工程约定和验证命令，不承载产品设计正文。
- Immich 作为同时包含 Web、服务端、移动端和部署内容的开源应用，把 `web/`、`server/`、`mobile/`、`deployment/`、`design/` 和 `docs/` 分开。它适合说明项目扩大后的边界，但不适合原样复制到当前小型项目。
- OpenAI 的 Codex 文档说明 `AGENTS.md` 会按仓库根目录到当前工作目录逐层发现，越接近工作目录的指令优先，因此根级 `AGENTS.md` 应只保存全仓库长期有效的协作规则；未来代码子目录确有不同规则时再增加嵌套文件。
- 可迁移到当前项目的共同原则：运行代码进入 `src/`；长期有效的产品与设计资料进入 `docs/`；历史稿进入 `docs/archive/`；Codex 工作状态进入 `.planning/exercise-app/`；生成物和运行数据不进入源码与公开文档目录；根目录只保留项目入口、Agent 规则和工程配置。
- 当前项目不需要预先建立 `web/`、`server/`、`mobile/` 等空目录。等技术设计确认真实组件边界后再拆分，可以避免目录结构提前暗示尚未作出的架构决定。

上述“保持根 `src/`”的结论只解决了代码与文档分离，没有解决多应用边界，已被下一节在产品所有者纠正后形成的多应用仓库结论替代。

## J. 2026-08-20 多应用仓库架构复核

> 本节包含外部官方资料的研究摘要。资料只作为架构评估输入，不覆盖产品所有者的明确决定。

- 产品所有者指出上一轮只把代码和文档分开，仓库根目录仍由 Vite、TypeScript、前端入口和前端依赖配置占据，无法清楚容纳未来后端与 Android。这一纠正成立：当前结构仍是单前端应用结构，而不是产品级多应用仓库。
- npm 官方将 workspaces 定义为由顶层根包管理多个本地嵌套包的机制；根 `package.json` 声明工作区，根 `package-lock.json` 统一记录依赖关系，并可从根目录针对指定 workspace 执行脚本。来源：https://docs.npmjs.com/cli/v11/using-npm/workspaces/
- Android 官方结构把 Gradle 项目级配置、应用模块、源码资源与构建输出分开；本机 `local.properties` 不应进入版本控制，APK 位于模块的 `build/outputs/apk/`，属于构建产物而不是源码。来源：https://developer.android.com/build/android-build-structure 与 https://developer.android.com/build/build-for-release
- Capacitor 官方说明它可以加入既有现代 JavaScript 项目，使 Web 优先的产品使用原生容器并访问 Android 原生能力。这说明当前仓库应保留“复用 Web 的 Android 容器”和“未来独立原生实现”两种路线，而不在 Phase 1 提前锁定其中一种。来源：https://capacitorjs.com/docs
- 架构结论：采用单仓库、多应用边界。当前前端迁入 `apps/web/`；未来后端放入 `apps/server/`；未来 Android 工程放入 `apps/android/`。共享包和部署目录只在实际出现内容时创建，不创建空目录。
- 根目录的 `package.json` 改为 npm workspace 编排器，前端依赖与 Vite/TypeScript 配置归属 `apps/web/`；根目录只保留全仓库规则、入口、许可、锁文件和跨应用编排配置。

## K. 2026-08-21 Phase 2 领域规则依据

> 本节记录外部资料和由其推导出的设计约束。外部资料用于校准规则，不直接覆盖 `docs/product.md` 中已经确认的产品事实；具体算法、参数和地区数据源仍需在后续阶段验证与版本化。

### 营养目标与营养数据

- USDA DRI Calculator 使用年龄、身高、体重、活动水平及与公式相关的生理信息估算每日热量、宏量和微量营养素参考值，并明确说明个人实际需要可能高于或低于计算值。来源：https://www.nal.usda.gov/human-nutrition-and-food-safety/dri-calculator
- USDA FoodData Central 同时提供 Foundation、SR Legacy、FNDDS、Branded 等不同类型的数据集及字段说明；同一种食物的营养结果会受到数据类型、份量基准、地区、品牌和版本影响。来源：https://fdc.nal.usda.gov/download-datasets/
- NIDDK Body Weight Planner 根据当前身体信息、活动水平、目标体重和时间估算饮食与活动变化，说明训练活动变化可以影响能量需求，但结果仍是模型估计而不是个体测量。来源：https://www.niddk.nih.gov/bwp
- 领域规则需要保留营养数据的来源、类型、版本和份量基准；缺失营养项必须表示为“未知”，不能自动当作零参与完整性判断。

### 运动消耗估算

- 2024 Compendium of Physical Activities 使用 MET 提供标准化活动能量成本；其修正 MET 说明同时强调，标准 MET 会因年龄、体重、身高、生理特征、瘦体重和静息代谢不同而偏离个体实际值，Compendium 不是为精确测量个人运动消耗而设计。来源：https://pacompendium.com/ 、https://pacompendium.com/corrected-mets/ 及同行评审论文 https://pmc.ncbi.nlm.nih.gov/articles/PMC10818145/
- 因此运动消耗必须标为估算，记录方法、活动映射、输入、来源版本和假设；计划训练只能提供当天的临时参考，存在实际训练记录后应以实际时长和训练量更新估算。
- 日常活动水平和单次训练可能发生重复计算。领域模型必须明确某套计算策略是否已经包含计划或实际训练，禁止在不知情时把同一活动重复加入当天能量目标。

### 餐食图片估算

- 单张二维照片很难准确恢复距离、角度、深度、遮挡、隐藏油脂、烹饪方式和真实食用比例；额外文字或人工修正能够帮助处理这些信息，但不应成为上传和分析的强制条件。来源：https://pmc.ncbi.nlm.nih.gov/articles/PMC11607557/ 、https://pmc.ncbi.nlm.nih.gov/articles/PMC7686022/
- 现有图片饮食评估研究的任务、数据集和误差表达差异很大，不能把某个总体准确率直接套用到本产品；模型结果应保留候选食物、份量假设、营养来源、不确定性及原始结果。来源：https://pmc.ncbi.nlm.nih.gov/articles/PMC9194158/
- 由此推导：图片分析任务、模型原始估算、当前采用值和用户修订必须是不同概念；重试不能覆盖旧尝试，用户修正应重新计算对应餐食和当天汇总。

### 健康与安全边界

- 美国《Physical Activity Guidelines for Americans》把活动建议置于一般健康指导范围，并指出存在慢性疾病或特殊健康状况的人可能需要医疗专业人员协助选择适合的活动。来源：https://health.gov/paguidelines/second-edition/pdf/Physical_Activity_Guidelines_2nd_edition.pdf
- 产品应提供一般健身和营养参考，不进行诊断、治疗或个体医疗承诺；动作指导和估算结果不能表述为绝对标准、绝对安全或精确测量。

### 对领域文档的共同约束

- 所有计算结果都应能够说明“使用了什么输入、哪套方法、哪个版本和哪些假设”。
- 原始事实、系统推导、用户当前采用值和历史修订需要分开保存，不能只保留最后一个数字。
- 未记录、未知、待分析和确认为零是不同状态；数据不完整不阻断产品使用，也不能生成虚假的确定性结论。
- 数值点估计应在可行时同时提供合理范围、置信等级或限制说明；具体呈现方式属于 Phase 3，计算与状态含义属于 Phase 2。

## L. 2026-08-23 多模态 API 能力变化

> 本节记录产品所有者提供的新运行条件及官方资料核对结果。产品事实以更新后的 `docs/product.md` 为准，供应商与接口选择仍属于 Phase 4。

- 产品所有者确认现在已经具备可由应用调用的多模态模型 API 能力，因此“只有 Codex 能分析图片”的旧运行条件不再成立。
- 产品所有者随后提供 DeepSeek 最新官方中文定价页；该页面已经列出实验性视觉模型 `deepseek-v4-flash-vision-exp`，模型版本为 DeepSeek-V4-Flash-Vision-Exp，支持 JSON Output、Tool Calls、Responses API 和 Anthropic API，并明确图片按尺寸换算为 token 计费。来源：https://api-docs.deepseek.com/zh-cn/quick_start/pricing/
- 上一轮依据普通 V4 和旧兼容接口页面作出的“DeepSeek 当前没有官方图片输入模型”判断已经过时，不能继续作为设计依据。普通 `deepseek-v4-flash` / `deepseek-v4-pro` 与实验性视觉模型需要区分，不能因普通 V4 文本限制否定视觉模型。
- 当前产品可以确认存在可调用的 DeepSeek 多模态 API 候选。由于模型名带 `vision-exp`，具体图片输入格式、实验性稳定性、结构化输出表现、价格、速率限制和数据政策仍在 Phase 4 通过真实接口验证，不在 Phase 2 锁死供应商实现。
- 设计影响：多模态 API 成为图片分析的主要执行方式；上传照片后可以自动或手动创建异步分析任务，结构化结果回填原餐食。Codex 保留为服务不可用、结果复核或特殊餐食处理时的人工补偿方式。
- 不变部分：照片先持久保存；分析失败不丢记录；模型原始估算与当前采用值分离；用户可以修正；任务、照片和结果必须同账号；分析执行者保持可替换。
- 外部图片 API 会把授权照片发送给第三方服务。是否自动分析、发送哪些照片和说明、数据保留边界需要可说明并由用户控制；API 密钥属于私密运行配置，不能进入公开仓库。
- 本轮打开两个旧 API 参考页时发生抓取超时，且官方搜索索引首先返回了普通 V4 的文本能力；产品所有者给出的最新中文定价页成功打开并提供了视觉实验模型的直接证据，因此以后核验此能力应同时检查最新模型表与图像理解指南，不能只查看普通 V4 接口。

## M. 2026-08-23 营养参考与训练联动计算研究

> 本节是 Phase 2 下一主题的研究输入，不直接构成已确认领域规则。具体方案将在完成来源核对后由 Agent 整体提交产品所有者纠正。

### 每日能量参考

- 美国国家科学院 2023 年《Dietary Reference Intakes for Energy》给出了 19 岁以上成人按年龄、性别、身高、体重和四级 PAL 分类计算的 EER 方程；EER 面向体重稳定成人，代表维持能量平衡的平均估计摄入。来源：https://www.nationalacademies.org/read/26818/chapter/7
- 成人方程的输入单位是年龄（年）、身高（厘米）、体重（千克），男性与女性、Inactive / Low active / Active / Very active 分别使用不同方程。报告给出的个体预测标准误可达到约 96–342 kcal/日，说明结果必须呈现为估算而非精确额度。
- 官方应用章节明确建议根据一段时间内的体重变化逐步校准摄入；同样身体参数的人，真实需要也可能明显偏离方程结果。如果用户在稳定活动下体重保持稳定，历史实际摄入可以帮助校准，而不是永远只依赖公式。来源：https://www.nationalacademies.org/read/26818/page/154
- 2023 EER 的 PAL 分类已经把身体活动计入总能量消耗，因此若再把单次训练的总消耗直接相加会存在重复计算风险。产品必须选择“习惯活动已含训练”或“非训练基线 + 显式训练调整”的一致策略。

### 宏量营养参考

- 国家科学院当前成人 AMDR 参考范围为碳水化合物占能量 45%–65%、脂肪 20%–35%、蛋白质 10%–35%；AMDR 是降低慢性病风险并满足必需营养素的总体范围，不是针对力量训练目标的唯一分配方式。来源：https://www.nationalacademies.org/read/27957/chapter/5 与 https://www.nationalacademies.org/read/10872/chapter/7
- 成人普通蛋白质 RDA 的通用参考为 0.8 g/kg/日；运动人群需要结合训练目标使用运动营养证据，而不是把 AMDR 百分比直接换成健身蛋白质目标。来源：https://www.nationalacademies.org/index.php/cdn/materials/9fb9fae6-337c-4b7c-9821-2c81d1f65ad0
- Academy of Nutrition and Dietetics、Dietitians of Canada 与 ACSM 的联合立场指出运动表现和恢复需要匹配训练情景的能量与宏量营养策略，并建议个性化运动营养计划由合格营养专业人员评估。来源：https://pubmed.ncbi.nlm.nih.gov/26920240/

### 初步设计含义

- 系统应先产生“维持能量参考”，再单独记录目标策略调整；减脂或增重不能偷偷混进 EER 本身。
- 蛋白质适合按体重与训练/目标规则计算，脂肪适合保留能量占比边界，碳水化合物适合承担满足总能量后的剩余分配；三者不能随单次训练消耗机械同比缩放。
- 训练联动需要明确定义基线 PAL 是否含训练。一个可解释候选方案是以不包含计划训练的日常活动作为基线，再用单次训练的净活动消耗更新当天能量；是否采用及如何估算净消耗仍需继续核对。

### 运动人群蛋白质

- ISSN 2017 蛋白质立场文件建议多数健康运动人群每天摄入约 1.4–2.0 g/kg 蛋白质，以支持训练适应；能量限制期间、以保留瘦体重为目的的阻力训练人群可能需要更高，但该高范围不应自动套用于所有用户。来源：https://pmc.ncbi.nlm.nih.gov/articles/PMC5477153/
- 2018 年包含 49 项研究、1863 人的系统综述与元回归发现，在阻力训练条件下，总蛋白质摄入超过约 1.62 g/kg/日后，对瘦体重增长没有观察到进一步总体收益。来源：https://pubmed.ncbi.nlm.nih.gov/28698222/
- 因此面向普通健身的初始系统建议可以把 1.6 g/kg/日作为中性默认点，把 1.4–2.0 g/kg/日作为可解释范围；特殊减脂、极低体脂、肾脏疾病或其他临床情况不能只靠应用自动提高目标。
- 每日蛋白质目标应主要由体重、训练类型和目标决定，不随当天估算消耗同比波动；当天训练联动更适合调整能量和碳水，而不是把蛋白质当作运动热量的剩余项。

### 运动消耗与重复计算

- 2024 Adult Compendium 以 1 MET ≈ 1 kcal/kg/小时作为标准化能量成本，活动总消耗可近似为 `MET × 体重 kg × 时长小时`。来源：https://pmc.ncbi.nlm.nih.gov/articles/PMC10818145/ 与 https://pmc.ncbi.nlm.nih.gov/articles/PMC11336313/
- 如果基础能量参考已经覆盖同一时段的静息消耗，把活动总消耗全额相加会重复计算至少 1 MET；单次活动的净增量候选公式为 `(MET − 1) × 体重 kg × 时长小时`。这仍是群体估算，不是个人测量。
- 2024 成人 Compendium 主要面向 19–59 岁；60 岁以上有独立 Older Adult Compendium，标准静息代谢换算不同。系统必须按年龄选择适用来源或停止输出个性化消耗，而不是跨年龄直接套用。

### 体重目标调整

- CDC 当前建议减重采用渐进、稳定的节奏，约每周 1–2 磅；药物、疾病、压力、激素等因素都可能影响体重管理。来源：https://www.cdc.gov/healthy-weight-growth/losing-weight/index.html
- ISSN 体成分立场认为减脂依赖持续能量缺口，起始体脂更低时应采用更慢速度以更好保留瘦体重；阻力训练备赛文献常见每周体重下降 0.5%–1.0%，但这不是所有普通用户的通用处方。来源：https://pmc.ncbi.nlm.nih.gov/articles/PMC5470183/
- NIH Body Weight Planner 使用当前体重、性别、年龄、身高、PAL、目标体重、目标时间与活动变化计算达到和维持目标所需能量，并明确活动变化会影响摄入需要。它比固定使用“每 7700 kcal 对应 1 kg”更适合作为减重动态模型的验证参考。来源：https://www.niddk.nih.gov/bwp
- 增肌所需最优能量盈余缺乏经过验证的统一值；2019 综述明确指出常见盈余估计尚未在阻力训练人群中验证。来源：https://pubmed.ncbi.nlm.nih.gov/31482093/
- 自然健美休赛期综述给出约 10%–20% 能量盈余和每周体重增加 0.25%–0.5% 的实践建议，但适用对象偏向健美训练者，不能直接成为所有用户默认值。来源：https://pubmed.ncbi.nlm.nih.gov/31247944/
- 产品应把减脂/增重速度和能量调整标为可校准策略：先给保守建议，结合连续数周体重趋势逐步修正；不能声称公式能准确预测个体体重变化。

## N. 2026-08-23 引用资料与产品推导置信度审计

> 本节评估 M 节及多模态能力讨论中的证据质量。分数是便于产品决策的审阅标尺，不是统计学概率。必须分别判断“来源本身是否可靠”和“该来源是否足以支持本产品的具体规则”。

### 评估标尺

- 90–100：政府或国家级共识报告直接支持，适用范围清楚；仍不表示个体数值精确。
- 75–89：高质量系统综述、同行评审标准或多专业组织立场，结论整体可用但存在人群或方法限制。
- 60–74：专业立场、叙述性综述或从较强证据作出的合理产品推导，需要保守表达和校准。
- 40–59：特定人群实践建议或未经直接验证的组合算法，只能作为可调整初始策略。
- 低于 40 或“未建立”：没有与本产品任务直接对应的验证，不能宣称准确。

### 逐类评估

| 资料或规则 | 来源可信度 | 支撑本产品具体结论的置信度 | 主要限制 |
|---|---:|---:|---|
| 中国营养学会《中国居民膳食营养素参考摄入量（2023版）》 | 95/100 | 85–90/100 | 最适合中国一般居民的营养基线，但参考摄入量仍不是个人处方 |
| 国家卫健委成人肥胖相关指南 | 95/100 | 80–90/100（仅适用于其定义的人群） | 面向超重/肥胖与体重管理，不能直接套给所有普通健身用户 |
| National Academies 2023 成人 EER | 95/100 | 65–75/100（个人每日数值） | 美国/加拿大人群方程；PAL 分类和个体预测误差会显著影响结果 |
| NIH/NIDDK Body Weight Planner 与其动态模型 | 90–95/100 | 70–80/100 | 有同行评审模型和官方实现，但仅限成人，排除孕期/哺乳期，仍不是医疗建议或个体测量 |
| CDC 每周约 1–2 磅渐进减重建议 | 90/100 | 60–70/100（作为应用默认速度） | 公共健康建议，不是按体重比例或个体状态生成的精确处方 |
| Morton 等阻力训练蛋白质系统综述与元回归 | 85–90/100 | 70–80/100（把 1.6 g/kg 作为普通健身参考点） | 1.62 g/kg 断点的 95% 区间约 1.03–2.20，分段回归本身未达传统统计显著；不能当成精确上限 |
| ISSN 1.4–2.0 g/kg 运动蛋白质立场 | 75–80/100 | 70–80/100（健康运动成人范围） | 属专业学会立场而非政府 DRI；多位作者披露补剂行业利益关系；不适用于肾病等临床情况 |
| National Academies AMDR | 95/100 | 85–90/100（一般成人宏量范围） | 是总体健康范围，不给出力量训练者唯一最优比例；选择脂肪 25% 是产品默认而非资料结论 |
| 2024 Adult Compendium MET | 90/100 | 55–70/100（个人某次训练热量） | 主要提供标准化活动成本，19–59 岁适用；标准 MET、活动匹配、体重和时长误差都会影响个体估算，力量训练尤其粗糙 |
| `(MET - 1) × 体重 × 时长` 净活动候选 | 75–85/100（概念） | 60–70/100 | 扣除静息部分的逻辑成立，但把个人静息消耗固定为 1 MET 仍是近似 |
| 增肌每周增重 0.25%–0.5% | 60–70/100 | 45–60/100（普通用户默认） | 主要来自自然健美叙述性综述和有限试验，适用人群窄，最优盈余尚无统一验证 |
| 训练日相对周均训练量重分配能量 | 无直接权威来源给出同一算法 | 55–65/100 | 数学上可避免把 PAL 与整次训练重复相加，但这是产品策略，需要模拟、真实使用和体重趋势校准 |
| 训练增加的能量主要分给碳水、蛋白质和脂肪不同比例波动 | 相关运动营养原则中等支持 | 60–70/100 | 方向合理，但当前引用不足以支持唯一分配公式，具体比例需继续核对或保留为用户可调策略 |
| DeepSeek 官方文档所示视觉 API 存在、接口与价格 | 99/100 | 99/100（仅针对能力存在） | 官方文档可直接证明 API 能力，但模型为实验版本，价格和能力会变化 |
| DeepSeek 对食堂餐照片的营养估算准确度 | 未建立 | 未建立 | 官方 API 文档没有提供与本项目餐食、份量和营养误差直接对应的独立基准；必须用自建样本和人工复核验证 |

### 审计结论与修正方向

- 当前引用集合的来源质量整体可评为高，约 85/100；但上一轮整套计算方案包含多项跨资料推导，作为产品算法的整体置信度只宜评为约 65–75/100。根据产品所有者随后确认的“规划准确性优先”要求，这个整体水平不足以成为正式规划算法；低置信产品策略必须与高置信核心计算拆开，继续验证或提供显式可调选项。
- 高置信部分可作为规划核心候选，仍需保留适用范围、来源版本、公式测试和个体校准；中低置信部分不能借用权威来源的名义包装成确定规则。
- 面向当前主要用户，应把中国营养学会 2023 DRIs 和国家卫健委现行指南作为一般营养与中国成人体重管理的第一层依据；National Academies、NIDDK 和国际运动营养证据用于公式交叉验证与训练场景补充。
- 不把 1.62 g/kg 描述为蛋白质收益的确定阈值；更合适的表达是普通健康阻力训练者的参考中心约 1.6 g/kg，常见证据范围约 1.4–2.0 g/kg，并根据目标、饮食可行性和健康状态调整。
- 不把每周增重 0.25%–0.5%、脂肪固定 25%、训练增量主要分配给碳水或周均训练重分配写成外部共识；这些都应明确标为待验证的产品策略。
- 图片多模态模型的“可调用”与“能可靠估算营养”必须分开。后者在完成食堂餐样本基准测试前没有可接受的准确度结论，只能输出暂定采用值并允许用户修正。

### 本轮新增或复核来源

- 中国营养学会 DRIs（2023版）：https://www.cnsoc.org/drpostand/
- 国家卫健委《成人肥胖食养指南（2024年版）》编制说明：https://www.nhc.gov.cn/sps/c100088/202312/3fbdf286857a4235be5749ca7a7b2ac9/files/1732845040999_78396.pdf
- 国家卫健委《体重管理指导原则（2024年版）》：https://www.nhc.gov.cn/wjw/ylyjs/202412/b3d40e0141834897808ce6c9dce76a60/files/1736390749000_59785.pdf
- National Academies 2023 Energy DRI：https://www.nationalacademies.org/publications/26818
- NIDDK Body Weight Planner 研究与适用边界：https://www.niddk.nih.gov/research-funding/at-niddk/labs-branches/laboratory-biological-modeling/integrative-physiology-section/research/body-weight-planner
- Morton 等蛋白质元分析：https://pubmed.ncbi.nlm.nih.gov/28698222/
- ISSN 蛋白质立场与利益冲突披露：https://pmc.ncbi.nlm.nih.gov/articles/PMC5477153/
- 2024 Adult Compendium：https://pmc.ncbi.nlm.nih.gov/articles/PMC10818145/
- 自然健美休赛期叙述性综述：https://pmc.ncbi.nlm.nih.gov/articles/PMC6680710/
- DeepSeek 官方模型与价格：https://api-docs.deepseek.com/zh-cn/quick_start/pricing/

## O. 2026-08-23 规划准确性与识别准确性分层

- 产品所有者纠正了置信度审计后的产品含义：多模态能力与营养识别准确度确实独立，但识别不确定性不影响系统设定每日目标和阈值。
- 系统提供的每日能量与营养目标属于规划基准，必须优先保证依据选择、适用人群、公式、输入、单位、边界和训练联动计算正确；这一优先级高于餐食识别准确性和当前实现便利。
- 照片分析是对“实际吃了多少”的估算。它的不确定性只传播到已摄入、剩余量和超出量的判断，不传播回每日目标本身，也不能成为降低规划算法标准的理由。
- “规划准确”不等于承诺系统能直接测量个人代谢或提供医疗处方。正确含义是：在明确适用范围内采用当前可靠证据，忠实实现公式，避免重复计算，并通过测试样例和版本记录保证结果可复核；个体差异继续通过合理适用范围和后续校准处理。
- 因此 N 节对多模态识别准确度“未建立”的结论只约束餐食估算验证，不构成推迟或降低每日营养目标计算规则的理由。

## P. 2026-08-23 高准确性优先的营养规划方案 v2

> 本节是依据产品所有者纠正后形成的完整 Phase 2 候选方案，等待整体纠正。它只定义计算责任和规则，不锁定代码库、SDK 或页面实现。

### P.1 三层结果严格分开

1. **规划基准：** 系统根据个人档案、目标和训练安排计算每日能量与营养目标，不读取餐食照片结果来决定“应该吃多少”。
2. **实际摄入：** 手工记录或图片模型估算“已经吃了多少”；模型不确定性保留在摄入结果中。
3. **比较结果：** `剩余或偏差 = 规划目标 − 当前采用的实际摄入`。摄入不完整或含暂定值时，只降低比较结果的完整性和置信程度，不修改规划目标。

### P.2 规划证据顺序

- 一般中国成人的能量与宏量营养基线优先使用中国营养学会 2023 DRIs；国家卫健委现行体重管理与成人肥胖指南用于其明确覆盖的人群和目标。
- 中国健康成年人运动消耗优先使用 T/CSSS 002—2023；活动不在该标准内时才使用年龄适用的 2024 Compendium，并明确标记为国际补充来源。
- National Academies 2023 EER、NIDDK 动态体重模型和国际运动营养系统综述用于补足中国公开资料缺少的个体化公式或训练情景，并作为交叉核验，不与中国参考简单取平均。
- 叙述性综述、特定健美人群建议和模型供应商文档不得成为每日规划核心数值的唯一依据。
- 同一指标只能有一个当前主计算方法；备用方法只用于资格不匹配、来源不可用或对照检查，不能静默切换。

### P.3 能量规划分为周预算和日分配

系统内部保留以下可解释组成：

`周能量预算 = 7 × 维持能量参考 + 周目标策略调整`

`某日规划目标 = 周能量预算 ÷ 7 + 当日训练净消耗参考 − 该周日均训练净消耗参考`

- **维持能量参考：** 首选适用于当前用户的中国 DRIs 方法；精确转写官方输入、单位、年龄、性别、生理状态和 PAL 定义。若缺少必要信息，不使用虚构默认值生成个性化结果。
- **周目标策略调整：** 维持为零；减脂或增重单独计算并保留来源、目标速度与方法版本，不能藏进维持能量。
- **日分配：** 把周预算按训练安排在一周内重新分布。训练日高、休息日低，但按原计划完成时七天总和仍等于周预算；这是一项透明的分配策略，不宣称人体必须每天完全补回运动热量。
- **无稳定周训练参考：** 系统仍可给出稳定的每日规划目标，但不伪造精细的训练日/休息日差异；用户形成计划或典型周后再启用日分配。
- **计划变化：** 训练前使用计划净消耗；训练中使用“已完成实际 + 尚未完成计划”；完成、放弃或取消后使用最终实际。相同训练只计一次。

采用“相对周均训练量”的原因是：维持能量方法中的活动水平通常已经反映习惯活动，直接再加整次训练容易重复计算；只分配某日相对周均值的差额，可以在不改写周总目标的情况下满足训练多则当天多吃、训练少则当天少吃的产品要求。

### P.4 运动消耗只服务活动差额

- 对 18–64 岁健康中国成人，活动与 MET 映射优先采用 T/CSSS 002—2023；其他年龄或未覆盖活动使用明确适龄来源，否则不输出伪精确数字。
- 同时保存总活动消耗和净活动消耗；用于日目标调整的是扣除同一时段静息消耗后的净活动部分，避免重复加入静息能量。
- 每次估算保留活动代码、强度、实际或计划时长、采用体重、来源版本和适用范围。
- 力量训练、间歇训练和无法可靠计时的活动允许给中心值与范围；范围影响当日目标的活动调整部分，不影响维持能量和宏量营养基础规则。

### P.5 目标策略

#### 维持体重

- 周目标策略调整为零，是最高可信和最容易校准的路径。
- 持续体重趋势明显偏离维持目标时，只提出校准建议；未经用户确认不静默改写当前算法或历史目标。

#### 减脂

- 对国家卫健委指南明确覆盖的超重或肥胖成人，优先按目标时间使用动态体重模型求解能量缺口，并用中国现行指南的速度、缺口和最低安全边界作资格检查。
- 当前中国指导原则对多数超重或轻度肥胖成人给出约每周 0.5–1 kg、每日减少 500–1000 kcal 或约 30% 总能量的范围。系统默认从范围的保守端开始，而不是自动选择最大缺口。
- 不符合该指南人群范围、存在相关疾病、孕期/哺乳期或结果触发安全边界时，不把肥胖管理参数直接套用为普通健身计划；保留手工目标和一般参考，并提示需要专业评估。
- 不使用固定“7700 kcal 等于 1 kg”直接推算长期计划；使用能够反映体重变化后能量需求变化的动态方法。

#### 增重或增肌

- 当前没有与维持或减脂同等级的权威公式能够保证能量盈余转化为肌肉，因此系统必须把“总体重增加计划”和“肌肉增加目标”分开表达。
- 使用动态体重模型或保守小幅盈余生成初始体重增加路径，再结合抗阻训练记录、数周体重和腰围趋势校准；不能承诺增加的体重都是肌肉。
- 每周增重 0.25%–0.5% 和 5%–20% 盈余只能作为较低等级的运动人群候选范围，不直接写成所有用户的高置信默认值。正式默认值需要继续核对或由产品所有者接受为保守产品策略。

### P.6 宏量营养分配

- **蛋白质先确定：** 无规律训练时采用中国 DRIs 对应年龄与性别参考；健康、规律运动成人使用运动营养证据，普通阻力训练以约 1.6 g/kg/日作为参考中心、1.4–2.0 g/kg/日作为可解释范围。1.62 g/kg 不是确定阈值。
- **蛋白质计算体重必须可解释：** 正常适用范围内可使用当前体重；肥胖、明显低体重或可靠瘦体重数据存在时，不能无条件用实际体重相乘。应采用有直接依据的调整体重、目标体重或瘦体重方法；方法未确认前不输出伪精确高蛋白目标。
- **脂肪保留范围与中心：** 一般中国成人优先遵守中国 DRIs 的脂肪供能范围；系统可以用范围中点形成单一规划值，但必须标明中点是分配规则而非唯一生理最优点。
- **碳水承担剩余：** 在蛋白质和脂肪约束满足后，用剩余能量计算碳水。训练日能量变化优先反映到碳水，是为了保持蛋白质目标和脂肪基础稳定；这是明确的分配策略，不冒充唯一医学结论。
- **约束冲突不得静默修正：** 若总能量太低，导致蛋白质目标、脂肪范围和碳水范围无法同时满足，系统不挤压某一项后继续显示“达标计划”，而是标记目标组合不成立并回到能量或目标速度校正。
- 克数与能量换算采用明确、版本化的换算常数；宏量营养换算后的能量总和必须在舍入容差内等于当日能量目标。

### P.7 规划算法的验收门槛

一套方法只有同时满足以下条件，才能从候选升级为正式规划算法：

1. 有能够直接支持该公式或范围的原始官方资料、共识报告或高质量系统综述。
2. 明确适用年龄、性别、生理状态、健康状态、目标和所需输入；缺失时不偷偷填默认值。
3. 公式、单位和舍入通过官方示例或独立计算样例复核。
4. 同一训练、静息能量和 PAL 不重复计算。
5. 七日目标在没有计划偏差时严格回到周预算；计划与实际变化只影响应变化的日期和组成。
6. 蛋白质、脂肪和碳水换算后与能量目标一致；约束冲突能被检测。
7. 每次结果保存输入快照、来源、方法版本、生成时间和组成，能够重现。
8. 以边界样例验证正常体重、超重、肥胖、低体重、缺失输入、无训练计划、训练取消、少练、多练及跨日训练。

### P.8 当前仍不能伪装成已解决的事项

- 中国 DRIs 2023 个体能量方法的正式转写与示例校验，需要在实现前取得并逐项核对完整官方表格或正文；当前不能凭二手摘要手抄公式。
- 肥胖或明显低体重时的蛋白质“计算体重”选择需要继续锁定适合本产品人群的直接依据。
- 增肌初始盈余没有与维持、减脂同等级的强证据，必须采用保守、可校准且明确降级的策略。
- 训练日额外能量全部还是部分当天分配，属于产品分配政策；使用相对周均值能够保证周预算守恒，但最终系数仍需产品所有者整体确认。

### P.9 新增依据

- 中国体育科学学会 T/CSSS 002—2023 公告：https://csss.kejie.org.cn/a1497.html
- 国家标准馆适用范围与起草机构：https://ndls.cnis.ac.cn/standard/detail/5981fd4ea2f7a00d6730162510357973
- 国家卫健委《体重管理指导原则（2024年版）》：https://www.nhc.gov.cn/wjw/ylyjs/202412/b3d40e0141834897808ce6c9dce76a60/files/1736390749000_59785.pdf
- 中国大陆成人 RMR 方程验证研究：https://pubmed.ncbi.nlm.nih.gov/31374849/
- NIDDK Body Weight Planner 研究：https://www.niddk.nih.gov/research-funding/at-niddk/labs-branches/laboratory-biological-modeling/integrative-physiology-section/research/body-weight-planner

## Q. 2026-08-23 规划方案确认与官方依据门槛

- 产品所有者整体认可 P 节的高准确性营养规划方案 v2，并追加硬性要求：规划中的所有相关信息都必须有官方文档背书。
- 该要求不表示产品不能形成推导，而是要求每项产品推导公开列出全部官方输入依据、计算过程和不变量，不得把产品推导表述为外部机构直接结论。
- 只有博客、搜索摘要、百科、论坛、模型回答、二手转述或无出处行业惯例时，相关数值不能进入自动规划核心。
- 已创建 `docs/domain/calculation-evidence.md`，按证据 ID 管理发布机构、文档版本、精确位置、支持内容、适用范围和验证状态；只有“可实现”状态能支撑生产算法。
- 当前已可直接定位的高等级内容包括国家卫健委 2024 体重管理指导原则中的中国成人 BMI、适用减重目标、能量缺口与宏量范围，以及 ISSN 正式蛋白质立场中的健康运动成人范围。
- 中国 DRIs 2023 个体能量正文、T/CSSS 002—2023 正式活动表，以及部分国际补充来源仍需取得原文或登记精确位置；在完成前不得凭二手公式实现。

## R. 2026-08-23 首轮官方原文核验

- 已从中国营养学会官网取得并逐项查看 2023 版表 9–14，覆盖 18–29、30–49、50–64 岁男性与女性。公开表直接提供分 PAL 的能量参考、蛋白质 EAR/RNI、碳水与脂肪 AMDR 等，可支撑中国成人分组校验与宏量范围；公开表没有提供本轮可核对的个人身高、体重能量方程。
- 已从中国体育科学学会发布公告取得 T/CSSS 002—2023 正式 PDF。标准第 1 页限定 18–64 岁健康成年人；4.3.1–4.3.3 给出强度分级和 `1 MET = 1 kcal/(kg·h)`；表 1 第 2–6 页列出 202 项活动，抗阻训练位于表 1 第 3 页。
- T/CSSS 原文包含禁止未经许可复制、再版或在网络发布标准及章节的版权说明。因此临时核验文件不进入仓库版本记录，正式项目只保留官方链接、精确位置、必要的计算关系和活动编码映射设计。
- National Academies 2023 正式报告 Summary 表 S-1/S-3 和第 5 章表 5-16 提供 19 岁及以上男女四种 PAL 分类的完整 TEE/EER 方程，并报告男性 MAPE 9.4%、女性 8.7%；第 7 章明确要求先计算 EER，再监测体重趋势并按需调整。
- E-004 的公式针对美国和加拿大参照人群。它可作为中国公开资料缺少个人公式时的正式补足来源，但不能被改称中国人群专用公式；产品必须同时显示来源、适用差异，并用中国 DRIs 分组表作交叉检查。
- 本轮把 E-001、E-003、E-004 升级为“可实现”，只表示原文内容可忠实转写和测试。产品算法仍需解决 PAL 选择、体重趋势校准、特殊体重蛋白质计算和增重策略，不能因来源升级而假定整个算法已经完成。

## S. 2026-08-23 动态体重模型、运动营养与许可核验

- NIDDK 官方研究页直接关联 Hall 等 2011 年 Lancet 研究和 8 页模型附录。附录第 2–5 页给出体液、脂肪与瘦体重分配、总能量消耗、食物热效应、适应性产热和活动参数等公式 1–18，第 3 页说明模型曾用未参与开发的人体喂养研究验证。
- NIDDK 明确限制 Body Weight Planner 用于 18 岁以上成人，不用于未成年人、孕期或哺乳期，也不提供医疗建议。动态模型可替代固定能量密度的长期线性推算，但体重增加结果不能解释为肌肉增加。
- Dietitians of Canada 官网提供 Academy of Nutrition and Dietetics、Dietitians of Canada、ACSM 三方 2016 年联合立场全文；ACSM 当前 Position Stands 页面仍将其列为正式立场。第 6 页要求营养支持随训练周期和单次训练负荷变化并个体化；第 15–17 页给出碳水与蛋白质范围；第 36–37 页汇总蛋白质 1.2–2.0 g/kg/日、脂肪 20%–35% 能量及按负荷周期化的碳水范围。
- E-005 与 E-006 已升级为“可实现”，表示公式、范围、位置和适用边界已能进入算法评审；是否采用完整动态模型以及采用哪些运动情景范围，仍由后续产品算法和技术版本决策决定。
- 科学证据与内容许可必须分开。T/CSSS 限制复制和使用标准章节，三方运动营养 PDF 也保留版权；公开 MIT 仓库不能直接打包这些 PDF、图片或整张数据表。项目只保存引用、必要计算关系和自行编写的实现；完整活动数据映射需取得授权或选择许可兼容的正式来源。

## T. 2026-08-23 PAL 分类官方边界

- National Academies 2023 第 7 章第 148–149 页明确指出，当前没有有效、可靠的简便工具能够准确把个人分入 PAL 档位；步数和问卷与双标水测得 PAL 的个人相关性通常较弱，是否达到每周健康运动建议也不能直接用于分类。
- 表 7-1 仍提供可用于候选分类的成人活动模式：PAL 约 1.4、1.6、1.75、2.05，并给出职业活动较少人群的日常活动和额外活动示例；报告同时提醒职业活动必须纳入整体判断。
- 因此系统不能把“每周练几次”“今天练了多久”或一个步数阈值直接转换成 PAL。初始方案改为收集惯常职业活动、日常移动和规律训练，对照表 7-1 提出可修改候选；相邻档位都合理时展示两个方程形成的范围。
- 官方报告要求 EER 规划采用两步过程：先选择方程计算，再持续监测体重并按需调整。它没有为本产品直接给出统一观察天数、波动阈值或自动调整步长，这些参数在找到直接依据或完成产品确认前不能伪装成官方结论。

## U. 2026-08-23 PAL、特殊体重、增重与食物数据收敛

- National Academies 2023 第 7 章只要求随时间监测体重并逐步调整摄入，没有规定一个适用于所有成人的观察天数、波动容差或调整步长。因此当前产品不实现隐藏的自动校准阈值，只展示趋势并由用户明确接受目标变化。
- 同一报告第 157–158 页说明 EER 本身是维持方程，可以用预测区间寻找可能导致增减重的起点，之后仍要按实际速度调整；该示例不等于增肌公式，也不适合作为全体用户的固定盈余。
- NIDDK Body Weight Planner 官方说明明确支持成人按目标体重和时间制定个性化热量与活动计划。当前增重路径因此改为“目标体重 + 目标期限 + 完整动态模型”，不再等待一个不存在的通用增肌盈余常数。
- 三方运动营养立场给出健康运动成人蛋白质范围，但明确提示体重谱两端需要细化，未提供普通健身应用可直接采用的统一替代计算体重公式。当前安全规则是在低体重、肥胖、显著特殊体成分或特殊健康状态时停止自动 `g/kg 实际体重`计算，保留中国 DRIs 固定参考和专业目标录入。
- 国家卫生健康委《居民体重管理核心知识（2024 年版）释义》要求体重过轻者先排除疾病原因，再根据健康、饮食和运动情况逐步提高能量至推荐水平或稍高；它不支持自行设定固定增重常数。
- 中国疾控中心营养与健康所、中国营养学会共同提供的食物营养成分查询平台收录约 1300 种常见食物和 31 项营养成分，适合作为中国常见食物的优先核对来源；平台同时明确数据版权和解释权归营养与健康所，不能据此把整库纳入 MIT 仓库。
- 国家卫生健康委 GB 28050—2025 官方问答明确了预包装食品营养标签的字段、单位、基准、能量换算与修约规则。具体品牌食品优先使用用户当前包装标签，食堂混合菜仍以可修正候选和份量假设表达。

## V. 2026-08-23 训练建议证据边界

- 全领域核对发现：系统训练建议同样会包含频率、负荷、组数和周训练量，不能只为营养目标建立证据门槛。`calculation-evidence.md` 因此扩展为训练与营养共同的规划证据登记表。
- 国家卫生健康委《中国公民健康素养——基本知识与技能（2024 年版）》第 32 条及官方释义给出中国健康成人总体活动基线：每周 150–300 分钟中等强度或 75–150 分钟高强度有氧运动、每周 2–3 次抗阻训练，并强调负荷和组次按个人素质与技能确定、循序渐进。
- ACSM 于 2026 年发布更新后的健康成人抗阻训练 Position Stand。官方说明称其综合 137 项系统综述、超过 30000 名参与者；当前立场强调覆盖主要肌群、每周至少 2 天、计划按个人目标和可持续性调整。
- ACSM 官方信息图直接给出目标特异参考：力量约 80% 1RM、每动作 2–3 组；肌肥大约每肌群每周 10 组；功率 30%–70% 1RM、向心快速。上述数值仅限健康成人建议，系统没有可靠 1RM 时不换算具体重量，也不要求用户先做最大重量测试。
- ACSM 同时指出，对普通健康成人，练到力竭、指定器械类型和复杂周期化没有稳定额外收益，因此不作为系统默认要求。65 岁以上、疾病、伤病、孕期或哺乳期、竞技专项等继续保留手工或专业方案路径。

## W. 2026-08-23 Vue 前端基线与工具链兼容性

- 产品所有者明确选择 Vue 作为 Web 前端框架。Vue 官方将 `create-vue` 作为推荐脚手架，生成基于 Vite、支持 Vue 单文件组件的项目，并提供 TypeScript 选项：<https://vuejs.org/guide/quick-start.html>。
- Vue 官方 TypeScript 指南说明 Vue 自身使用 TypeScript 编写，官方包自带类型声明；Vite 负责转译时，命令行类型检查使用 `vue-tsc`：<https://vuejs.org/guide/typescript/overview>。
- Vite 官方同时提供 `vue-ts` 模板与官方 `@vitejs/plugin-vue`，因此现有 Vite workspace 可以原地迁移，无需重新建立仓库：<https://vite.dev/guide/>。
- Capacitor 官方说明可以加入任何现有现代 JavaScript 项目，并明确列出 Vue，因此 Vue 不阻断以后复用 Web 构建结果生成 Android 应用的候选路线：<https://capacitorjs.com/docs>。
- 本地迁移验证发现 TypeScript 7.0.2 改变了包导出，当前 `vue-tsc` 3.3.11 仍需要 `typescript/lib/tsc`，无法直接工作。`vue-tsc` 官方说明当前要求 TypeScript 5.0 以上，但 TypeScript 7 支持仍处于工具链演进中：<https://github.com/vuejs/language-tools/blob/master/packages/tsc/README.md>。
- npm 为 Vue 的可选 TypeScript peer 在根目录提升了 7.0.2，单独在 `apps/web` 安装 5.9.3 或只写根 `overrides` 都未改变 `vue-tsc` 的解析结果。最终由根 workspace 统一声明并覆盖 TypeScript 5.9.3，依赖树、类型检查和生产构建通过。

## X. 2026-08-24 Phase 3 首轮体验原型

- 一级信息架构采用“今天、训练、饮食、历史、设置”五个入口：手机使用底部导航，桌面使用侧栏，但保持同一任务结构。
- “今天”承担判断和快捷入口，不要求用户先补齐所有资料；训练与饮食仍可独立开始，当日联动只解释当天参考或偏差的变化。
- 首轮曾把系统训练建议、用户计划和实际记录作为三个并列页面层级；2026-08-24 经产品所有者纠正后，建议只在建立计划时出现，训练主页只呈现用户计划及与计划项关联的实际完成和额外动作。
- 首轮曾展示独立的用户营养目标和当日有效目标；2026-08-24 经产品所有者纠正后，系统每日营养参考成为唯一数值比较基准，用户改为选择有官方依据的策略与范围内偏好。
- 图片分析以餐食、照片、任务、尝试和结果可分离为前提；原型覆盖等待、暂定、失败、不完整和修正状态，但没有实现上传、模型调用或保存。
- 所有营养数值暂用“—”，避免在体验阶段制造无个人资料、无正式算法支撑的示例值。
- 浏览器实测覆盖 320、375、414、768 和 1280 CSS 像素宽度：无横向溢出，移动端底部导航和桌面侧栏按预期切换，可见按钮不低于 44 CSS 像素。

## Y. 2026-08-24 训练时段与进食联动纠正

- 产品所有者指出训练不一定发生在晚上：工作日可能常在晚上，但周末、假期或其他安排下也可能在早上或白天训练。
- “常见在晚上”只能作为个人习惯或计划输入，不能成为系统固定前提。计划训练时间属于意图，实际开始和结束时间属于事实。
- 联动判断改为“今天是否还会继续进食”：还会吃时显示剩余摄入参考，已经结束当天进食时显示已有摄入偏差，状态未知时只显示目标变化。
- 时钟时间、训练时间和“早饭、午饭、晚饭”等餐次名称都不能单独判断用户是否还会进食；用户明确选择优先，餐食记录和饮食计划只用于辅助提示。

## Z. 2026-08-24 饮食手动记录参考模式

> 本节只记录外部产品公开资料带来的体验参考，不把第三方产品的规模、界面或数据直接当成本项目能力。

- 薄荷健康在开发者发布的 Apple App Store 产品说明中公开描述了“海量食物数据库”“热量营养素一查便知”和三餐热量随手记录；当前版本记录还公开提到拍照估算食物热量、饮食记录建议，以及自定义食物搜索。
- 这足以支持借鉴一条任务模式：饮食记录同时提供“拍照估算”和“搜索食物后选择份量”两个入口；不能仅凭这些公开文字推断其具体页面布局、排序算法、数据库许可或准确性，也不复制其界面。
- 本项目的食物库仍必须遵守 `docs/domain/calculation-evidence.md` 和 `docs/domain/domain-rules.md` 中的数据来源、许可、份量、版本与未知值规则。薄荷健康宣称的数据库条目数量不构成本项目的数据来源或实现承诺。
- 公开资料来源：[Apple App Store 上由 BOOHEE TECHNOLOGY 发布的薄荷健康产品页](https://apps.apple.com/cn/app/id457856023)，访问日期 2026-08-24。

## AA. 2026-08-24 Phase 3 基线确认与 Phase 4 启动

- 产品所有者确认当前可点击体验版本整体不错；当前“今天、训练、饮食、历史、设置”信息架构、训练计划与实际组合、系统营养建议与双入口记餐、日期主索引历史，以及“还能吃多少 / 还需练什么”的今日表达可作为 Phase 3 基线。
- 这项确认结束体验结构的开放审阅，但不代表高保真视觉、真实业务能力或技术实现已经完成。
- Phase 4 需要基于已确认产品与领域规则一次性提出完整技术架构和分阶段交付方案，至少覆盖 Web、服务端、数据库、照片与备份、多模态 API、长期部署、账号隔离、Android 路线、测试与开源边界。
- Phase 4 的技术候选必须保持当前单仓库多应用边界；不得因实现方便削弱模型原始估算与采用值分离、历史修订、异步任务、数据隔离或官方证据规则。
- `findings.md` 前部保留的 React、14 天验证和后续进食状态属于历史材料，不能带入当前架构；当前前端基线是 Vue，验证不得成为使用配额，当日页面不再维护后续进食状态。
- DeepSeek 视觉模型当前只能作为可替换的分析提供方候选；正式架构需要把图片持久化、分析任务、分析尝试、结构化结果与当前采用值分开，并把供应商密钥留在服务端私密配置中。
- 个人阶段已经要求长期服务器与持久数据，因此 Phase 4 不能采用纯前端、本机浏览器存储作为主架构；即使首位用户只有一人，数据模型和服务边界也必须能平滑增加账号隔离。
- 旧的“服务器队列 + Codex 人工发起”只能保留为分析服务故障或复核补偿路径；当前主路径应由服务端异步调用经配置的多模态 API。
- `docs/product.md` 的 8.2 与 8.3 仍以“后续需要定义”描述已经完成的 Phase 2/3，Phase 4 同步审计时应改为完成状态，避免产品事实源与当前规划冲突。
- 已确认体验要求客户端在弱网或异步分析期间可继续操作，但当前没有确认离线写入或多端实时同步；技术方案应先提供可靠的在线 Web 流程，不提前引入离线优先复杂度。
- 领域规则包含大量版本、修订、归属、唯一当前值和跨账号不变量，适合使用具备事务、外键和约束能力的关系型数据库；仅用文档文件或无约束 KV 存储会把一致性责任推给应用代码。
- 照片必须独立持久化且不能进入数据库大对象或代码发布目录；业务数据库保存对象键、校验信息、归属和状态，实际媒体进入可备份的对象存储接口。
- 图片任务需要持久任务表、尝试表、租约或幂等执行键与迟到结果校验；单纯在 Web 请求中同步等待模型会破坏已确认的离开页面、失败重试和删除失效语义。
- 规划算法需要成为服务端内可独立单元测试的纯计算模块，输入、证据 ID、方法版本和输出快照都必须显式；不能把公式散落在控制器、数据库触发器或前端组件中。
- E-003 活动表、E-011 食物数据库和动作视频都存在许可边界，技术方案必须把“开源代码”“可公开的小型自编映射”“运行时外部内容”和“用户私有内容”分层，不能用种子数据文件绕过授权问题。
- 当前 `AGENTS.md` 仍声明 Phase 3，创建 Phase 4 架构文档后必须同步阶段约束、有效文档索引和根命令边界。
- Node.js 官方发布页显示 24 系列在当前日期处于 LTS，26 系列仍为 Current；服务端生产基线应固定到 Node.js 24 LTS，而不是追随最新 Current：<https://nodejs.org/en/about/previous-releases>。
- Fastify 5 官方文档提供 TypeScript 支持、JSON Schema 请求校验与响应序列化，并内置结构化日志和敏感字段遮盖能力；这些特性适合把 API 合约、安全输入和日志边界放在一个模块化服务端中：<https://fastify.dev/docs/latest/Reference/TypeScript/>、<https://fastify.dev/docs/v5.8.x/Reference/Validation-and-Serialization/>、<https://fastify.dev/docs/latest/Reference/Logging/>。
- Drizzle 官方文档明确支持数据库事务以及索引、唯一约束、检查约束和外键；它可作为 TypeScript 数据访问与迁移工具，但领域不变量仍需显式落为 PostgreSQL 约束与事务，而不是只依赖 ORM 类型：<https://orm.drizzle.team/docs/transactions>、<https://orm.drizzle.team/docs/indexes-constraints>。
- pg-boss 官方仓库将任务持久化在 PostgreSQL，支持重试、退避、定时任务、死信与事务内入队，并列出 Drizzle 适配；个人阶段可据此避免额外维护 Redis。它仍是独立开源依赖，实施时必须固定版本、运行真实 PostgreSQL 集成测试并保留以后替换队列适配器的边界：<https://github.com/timgit/pg-boss>。
- PostgreSQL 官方行级安全策略可按当前用户限制每一行的可见与可写范围；当前架构应从第一天在所有私有业务表保存 `user_id`，朋友开放前再把数据库 RLS 作为应用层授权之外的纵深防护：<https://www.postgresql.org/docs/17/ddl-rowsecurity.html>。
- 产品所有者补充的 DeepSeek 官方“图像理解”指南直接确认 `deepseek-v4-flash-vision-exp` 支持图片输入，并给出 OpenAI 兼容 Chat Completions、Responses API 与 Anthropic API 的调用格式：<https://api-docs.deepseek.com/zh-cn/guides/vision>。
- 官方指南列出三种图片交付方式：Base64 data URL、可公开访问的外部 URL、通过 Files API 上传后使用 `file_id`。当前产品的照片是私有数据，首选应为服务端读取后以内联数据或受控 Files API 发送，不应为了调用模型创建长期公开照片 URL。
- 官方限制包括 JPEG/PNG/GIF/WebP、48 MiB 请求体、Base64/URL 单图 32 MiB、Files API 单图 64 MiB、图片仅可用于视觉模型等；模型会按尺寸缩放并将图片折算为 token。产品上传限制应取自身更保守的值，并记录模型、输入方式和用量。
- Copilot 集成页所说的“DeepSeek V4 为纯文本模型”描述的是普通 `deepseek-v4-flash` / `deepseek-v4-pro` 及插件代理流程，不能用于否定独立的 `deepseek-v4-flash-vision-exp`。上一轮 Phase 4 研究把普通 V4 的请求模式错误推广到视觉模型，现已由官方视觉指南纠正。
- 对“DeepSeek 视觉 API 能直接接收并理解图片”可给高置信判断；对“能准确估算食堂餐份量和营养”仍没有官方任务级证据，必须继续保留候选、范围、用户修正和本项目样本验证。
- PostgreSQL 官方说明 `pg_dump` 可在数据库仍被使用时生成内部一致的快照，并可通过 `pg_restore` 恢复；个人部署的备份基线可用自定义格式数据库转储，但照片文件仍需同一备份批次单独快照并记录清单与截止时间：<https://www.postgresql.org/docs/17/backup-dump.html>。
- Docker Compose 官方 secrets 机制按服务授权并把密码、证书或 API key 以文件注入容器，避免把敏感信息写进 Dockerfile、源码或普通环境变量；部署定义应使用 secrets 或等价的主机私密文件，不在仓库保存真实值：<https://docs.docker.com/compose/how-tos/use-secrets/>。
- Capacitor 官方确认可以加入现有 Vue Web 工程并生成 Android 项目，同时提供 Camera 和 Local Notifications 等原生插件；它适合作为 Web 满意后的首选 Android 复用路线，但仍应在正式迁移前验证照片选择、后台上传、登录会话和通知在目标 Android 版本上的行为：<https://capacitorjs.com/>。
- Vue Router 是 Vue 官方客户端路由，Pinia 是 Vue 生态当前默认状态管理方案；实现阶段使用二者承接五个一级入口、跨页身份状态和少量客户端任务状态，不把服务端业务事实长期复制进 Pinia：<https://router.vuejs.org/>、<https://pinia.vuejs.org/introduction.html>。
- OWASP 会话管理指南建议浏览器会话使用 `Secure`、`HttpOnly`、`SameSite` Cookie，并明确不应把 session、JWT 或 refresh token 存入 `localStorage`；Web 端因此采用服务端不透明会话与同源 Cookie，公开网络必须使用 HTTPS：<https://cheatsheetseries.owasp.org/cheatsheets/Session_Management_Cheat_Sheet.html>。
- OWASP 密码存储指南当前首选 Argon2id，并给出最低成本参数；账号密码只保存 Argon2id 派生值和独立盐，具体成本在目标服务器基准测试后不低于官方最低值：<https://cheatsheetseries.owasp.org/cheatsheets/Password_Storage_Cheat_Sheet.html>。
- Drizzle Kit 官方 `generate` 流程生成可审阅并提交的 SQL migration 与 schema snapshot，随后由 `migrate` 应用；正式环境只允许经评审的 migration 前滚，不用 `push` 直接同步生产数据库：<https://orm.drizzle.team/docs/drizzle-kit-generate>。
- Docker 官方把 volume 定义为独立于容器生命周期的持久数据存储，并认为它适合备份、迁移和长期数据；PostgreSQL 数据与用户照片必须进入命名 volume 或显式主机持久目录，不能留在容器可写层：<https://docs.docker.com/engine/storage/volumes/>。
- PostgreSQL 18 是当前正式版本，`numeric`/`decimal` 是可选精度的精确数值类型；保存克数、能量和计算快照时使用有明确单位与精度的 exact numeric，避免用二进制浮点作为权威值：<https://www.postgresql.org/docs/>、<https://www.postgresql.org/docs/18/datatype.html>。
- OWASP 文件上传指南要求允许列表、签名与类型核验、随机服务端文件名、权限与大小限制，并建议把文件放在 Web 根目录之外；餐食照片必须经认证接口读取，不能成为静态公开 URL：<https://cheatsheetseries.owasp.org/cheatsheets/File_Upload_Cheat_Sheet.html>。
- Fastify 官方维护的 `@fastify/swagger` 可从路由 Schema 生成 OpenAPI，`@fastify/static` 可提供 Web 构建产物；静态插件近期存在已公开的路径规范化授权绕过公告，因此实现时必须固定已修复版本、只提供公开 Web 构建目录，并绝不借静态插件保护用户照片：<https://github.com/fastify/fastify-swagger>、<https://github.com/fastify/fastify-static>、<https://github.com/fastify/fastify-static/security/advisories/GHSA-8pvw-jcv7-9cmj>。
- Vitest 官方说明其复用 Vite 配置并原生支持 TypeScript/Vue，Playwright 官方提供 Chromium、WebKit、Firefox 的端到端测试与移动端模拟；二者适合当前 Vue 单元/组件和跨浏览器流程验证，数据库与任务仍需另外运行真实 PostgreSQL 集成测试：<https://vitest.dev/guide/features>、<https://playwright.dev/docs/intro>。
- Phase 4 草案的最终一致性检查确认：所有既定产品与领域能力都有对应技术责任和交付增量；草案在当前文档中始终标为“等待产品所有者整体确认”，没有提前写入产品决策日志，也没有创建业务实现目录。
- DeepSeek Vision 纠正已同步到当前所有技术文档：DeepSeek 视觉模型成为个人版首个提供方实现，Base64 是私有单张餐食照片的首选交付方式，供应商适配器、异步任务、原始结果/采用值分离和 Codex 补偿边界保持不变。

## AB. 2026-08-24 Redis 与异步任务架构复核

- 产品所有者提出可以考虑 Redis，因此 Phase 4 不再把“个人阶段不增加 Redis”视为默认结论，改为重新比较 PostgreSQL 内队列和 Redis 队列。
- Redis 官方提供 RDB、AOF、两者结合或无持久化四类策略；AOF 默认每秒同步时仍可能丢失约一秒写入，RDB 快照可能丢失最近数分钟。因此即使采用 Redis，也不能把唯一业务记录或无法重建的分析任务只保存在 Redis：<https://redis.io/docs/latest/operate/oss_and_stack/management/persistence/>。
- BullMQ 官方确认它是建立在 Redis 上的 Node.js 队列，支持延迟任务、周期调度、失败重试、并发控制和进程崩溃恢复；官方同时明确最坏情况下是至少一次投递，所以 Worker 仍必须幂等，不能依赖“恰好一次”：<https://docs.bullmq.io/>。
- BullMQ 的去重 ID 能减少同一未完成任务的重复入队，但手工删除作业会取消去重状态；它只是一道运行保护，不能替代 PostgreSQL 中的唯一约束、任务状态和采用值保护：<https://docs.bullmq.io/guide/jobs/deduplication>。
- BullMQ Job Scheduler 会在上一个作业开始处理后再生成下一项；队列繁忙或 Worker 不足时，实际触发频率可能低于配置频率。因此提醒意图和到期时间应保存在 PostgreSQL，由调度任务扫描到期记录，不能只把用户提醒定义保存在 Redis：<https://docs.bullmq.io/guide/job-schedulers/>。
- Redis 官方安全文档警告不得把 Redis 端口暴露到不可信网络；生产部署应只允许内部网络访问，使用 ACL 最小权限和认证，Redis 不对公网开放：<https://redis.io/docs/latest/operate/oss_and_stack/management/security/>、<https://redis.io/docs/latest/operate/oss_and_stack/management/security/acl/>。
- 当前更稳妥的候选不是让 Redis 取代 PostgreSQL，而是采用“PostgreSQL 业务真相 + 事务 outbox + Redis/BullMQ 执行队列”：业务事务同时写任务和 outbox，分发器把仅含任务 ID 的消息送入 BullMQ，Worker 每次回到 PostgreSQL校验归属、状态和幂等条件；Redis 清空后可从未完成任务和 outbox 重建队列。
- BullMQ 的生产指南要求 Redis 启用持久化，并把 `maxmemory-policy` 设为 `noeviction`，否则任意逐出队列键会破坏队列行为；生产者应在 Redis 断开时快速失败，Worker 则持续重连：<https://docs.bullmq.io/guide/going-to-production>。
- BullMQ 提供全局队列限流，也能在供应商返回 `429` 时手动把任务重新放回等待状态；这比在各 Worker 内各自计数更适合约束视觉 API 的总调用速率，但具体限额仍以供应商实时配置为准：<https://docs.bullmq.io/guide/rate-limiting>。
- PostgreSQL 18 官方说明 `FOR UPDATE ... SKIP LOCKED` 适合多个消费者避免队列表争用；outbox 分发器可据此并发领取待投递记录，但必须使用确定排序、租约/重试和投递幂等，不能把 `SKIP LOCKED` 当成完整队列实现：<https://www.postgresql.org/docs/18/sql-select.html>。
- Redis 8 及后续版本提供 RSALv2、SSPLv1、AGPLv3 三种许可选择，其中只有 AGPLv3 是 OSI 批准的开源许可；BullMQ 本身为 MIT。当前项目不修改或嵌入 Redis 源码，但开源发布前仍需在第三方许可清单中说明独立 Redis 服务与选用许可，避免把 Redis 服务器误写成项目 MIT 代码的一部分：<https://redis.io/legal/licenses/>、<https://docs.bullmq.io/>。
- 综合个人部署与未来朋友使用，Redis 的价值主要不是当前吞吐量，而是把视觉 API 的并发、重试、退避、延迟和限流从业务数据库轮询中分离；代价是一项额外有状态服务和 PostgreSQL→Redis 的一致性交接。只有采用可重建队列与 outbox，收益才大于这项复杂度。

## AC. 2026-08-24 pg-boss 专项可靠性复核与确认

- 独立复核比较了 pg-boss、Graphile Worker 与 Redis/BullMQ 路线。针对“不超过十人、单机 Docker、Node.js + PostgreSQL”的首个长期部署版，建议采用 pg-boss，推荐置信度为 82%。
- pg-boss 开源版提供 heartbeat 与 expiration，能够按配置重新调度因 `SIGKILL`、OOM 或网络分区中断的任务；Graphile Worker 开源版遇到非优雅崩溃时，运行中任务默认至少锁定四小时，更快恢复依赖 Pro 或自行实现可靠解锁。来源：<https://github.com/timgit/pg-boss>、<https://worker.graphile.org/docs/error-handling>、<https://worker.graphile.org/docs/pro/recovery>。
- pg-boss 与 Drizzle 事务适配允许业务数据和任务在同一 PostgreSQL 事务中提交；正式实现仍要用本项目组合验证 commit 后可见、rollback 后不可见。来源：<https://github.com/timgit/pg-boss/blob/master/docs/api/jobs.md>。
- 无论依赖如何宣传领取语义，跨 PostgreSQL 与视觉供应商的完整处理流程都必须按至少一次执行设计。`analysis_task`、`analysis_attempt` 和采用值修订是权威状态，队列只负责执行，所有写回必须幂等。
- pg-boss 的维护集中且近期修复较频繁，因此不追随当天 latest；实现时固定经验证的精确 patch，独立执行队列 migration，并覆盖事务回滚、重复投递、Worker 强杀、数据库断线及模型响应后崩溃等故障测试。
- 产品所有者已经确认采用 pg-boss，当前不引入 Redis。只有本地故障测试不通过、规模或隔离需求显著改变、或维护状态恶化时才重新评估队列。

## AD. 2026-08-25 Phase 4 收口审查

- 当前技术架构已经覆盖 Web、服务端、PostgreSQL、Drizzle、pg-boss、临时媒体、DeepSeek 视觉适配器、账号会话、管理员、安全访问、Docker Compose、备份恢复、开源边界和 Capacitor Android 路线；已确认产品能力都有技术责任和交付增量承接。
- 已确认的后续业务修正也已进入架构：餐食原图确认后删除、预置 `admin`、单次方案与周期计划双层训练模型、身体测量时间序列、计算输入快照和每周可关闭的体重提醒。
- `technical-architecture.md` 的 `reminders` 模块仍只写训练与饮食提醒，遗漏已经确认的身体测量提醒，需要在收口稿中补齐。
- `technical-architecture.md` 第 15 节把真实 DeepSeek API 契约测试写在整个 Phase 5 的启动门中，与 `delivery-plan.md` 的增量 4 前置门冲突。账号、数据库、训练和手工饮食实现不依赖视觉 key；契约测试应只阻塞增量 4，不阻塞 Phase 5 增量 0。
- 服务器 CPU、内存、操作系统和网络属于增量 0 部署参数，可以在实现开始时采集，不改变架构，也不需要作为产品所有者再次设计产品的阻塞问题。
- 食物数据库许可、目标设备 Capacitor 行为和 pg-boss 故障表现仍是各自增量的风险门；它们已经有安全降级路径，不阻塞 Phase 4 架构整体确认。
- 完成上述两处一致性修正后，Phase 4 不再存在需要提前决定的新技术组件；下一步应向产品所有者一次性汇总整体架构、交付顺序、已知风险和 Phase 5 启动边界。

## AE. 2026-08-25 Phase 5 增量 0 本地环境

- 当前本机运行 Node.js 24.16.0 与 npm 11.13.0，符合已确认的 Node.js 24 LTS 服务端基线。
- 当前 shell 找不到 `docker` 和 `docker compose`。仓库仍可创建 Compose、Dockerfile、持久卷和 secret 模板，但不能把本机容器启动、真实 PostgreSQL migration、备份恢复或 pg-boss 故障恢复标记为通过。
- 增量 0 应把不依赖容器的 TypeScript、单元测试、配置校验和服务端模块边界先落地；数据库集成测试使用显式环境门，缺少真实 PostgreSQL 时跳过并在交付状态中保留未验收，而不是改用与生产语义不同的 SQLite 冒充。
- 本机也没有 `psql`，因此当前没有可用的真实 PostgreSQL 客户端或服务可用于集成验证。
- 2026-08-25 从 npm registry 核对到的当前版本包括 Fastify 5.12.1、Drizzle ORM 0.45.2、Drizzle Kit 0.31.10、pg-boss 12.28.0、argon2 0.45.1、`@fastify/cookie` 11.1.2、`@fastify/static` 10.1.3、`@fastify/swagger` 9.8.1、Vitest 4.1.11 和 tsx 4.23.12。
- pg-boss 12.28.0 是专项复核时刚发布的版本，按 D-027 的精确版本策略不直接追当天 latest；初始工程应固定到已经检查过实现资料的 12.27.0，并由真实 PostgreSQL 契约测试决定是否升级。
- 服务端依赖首次安装成功，共新增 161 个包；npm 同时报告 4 个中等风险漏洞，需要读取审计链后处理，不能直接使用破坏性 `--force` 自动升级。
- 安装警告中的 `@esbuild-kit/esm-loader` 与 `@esbuild-kit/core-utils` 已废弃，需要通过 `npm ls` 确认其上游；当前不能仅凭警告假设它们来自直接依赖。
- `npm audit` 已定位全部 4 个中等风险项：Drizzle Kit 0.31.10 仍通过 `@esbuild-kit/esm-loader` → `@esbuild-kit/core-utils` 带入 esbuild 0.18.20，命中 GHSA-67mh-4wv8-2f99；风险内容是旧 esbuild 开发服务器可能被任意网站读取响应。
- Drizzle Kit 自身还直接依赖安全范围的 esbuild `^0.25.4` 与 tsx `^4.21.0`，生产服务端不会打包 drizzle-kit。npm 给出的自动修复是倒退到 drizzle-kit 0.18.1，不可采用。
- 当前处理候选是在根 overrides 中仅把 `@esbuild-kit/core-utils` 的 esbuild 提升到首个修复范围以上，再用 `drizzle-kit generate`、类型检查与 `npm audit` 验证；如果加载器不兼容，则移除该覆盖并把 Drizzle Kit 隔离为不对网络暴露的本地工具，同时保留已知风险说明。
- 首次使用 `"@esbuild-kit/core-utils": { "esbuild": "0.24.3" }` 的根 override 后，npm 仍保留嵌套 esbuild 0.18.20，说明该写法没有作用于当前依赖路径；继续用父依赖和精确版本限定的嵌套 override 做一次不同形式的验证。
- 带 `@esbuild-kit/esm-loader@2.6.5` 和 `@esbuild-kit/core-utils@3.3.2` 的第二种嵌套 override 同样未生效。继续强制全局 esbuild 版本会跨越多个依赖的 semver 范围，风险高于当前只存在于开发 migration 工具的漏洞，因此已移除无效覆盖。
- 当前控制方式是：Drizzle Kit 只保留为 devDependency，不进入生产镜像运行依赖，不启动或暴露其开发服务器；根命令新增 `npm run audit:prod` 单独验证生产依赖。上游修复后再通过正常精确版本升级消除完整审计告警。
- 首个 Drizzle migration 已成功生成 8 张表：账号、密码摘要、会话、单例应用设置、审计事件、自有后台任务、任务尝试和临时媒体。SQL 中包含账号外键、唯一当前键、任务去重、尝试序号、媒体大小与设置单例约束，未出现密码明文列或照片二进制列。
- 首轮配置与健康检查测试 5 项通过；严格 TypeScript 继续发现并修正错误处理器的 `unknown` 收窄，未知服务端异常只返回统一公开错误，不泄漏连接串或内部消息。
- pg-boss 12.27.0 的实际类型入口位于 `dist/index.d.ts`。公开 API 支持构造连接、`start`/`stop`、`createQueue`、`send`、`work`、`schedule`、schema 版本与漂移检查，并公开 `fromDrizzle(tx, sql)` 支持事务内入队。
- QueueOptions 直接提供 `expireInSeconds`、`heartbeatSeconds`、重试策略和保留策略；具体值仍须等真实模型任务耗时验证，地基只建立适配接口和队列初始化，不制造未经测试的图片任务时限。
- pg-boss 12.27.0 package 声明 Node `>=22.12.0`，当前 Node 24.16.0 满足；其正式运行依赖 `pg ^8.22.0`，与项目固定的 pg 8.23.0 兼容。
- pg-boss 构造参数支持独立 `schema`、`application_name`、`migrate` 与 `createSchema`。因此增量 0 将拆成显式 `queue:migrate` 命令和运行时 `migrate:false/createSchema:false`，避免 API 或 Worker 在普通启动时悄悄修改队列 schema。
- pg-boss 默认 heartbeat 关闭、任务过期 15 分钟、重试 2 次；这些默认值不能直接冒充视觉任务的已验证参数。地基只为专用 smoke queue 设置短时故障测试参数，真实图片队列在增量 4 按实际模型耗时另行确定。
- `work` 默认按批次把 `Job[]` 交给处理器；当前适配器会把领域处理器限制为只接收任务 ID，并逐项返回，避免队列载荷承载密码、照片或完整业务事实。
## 2026-08-25：Increment 0 前端与忽略规则复核

- 当前 `apps/web` 已是 Vue 3 + TypeScript + Vite，但页面切换仍由 `App.vue` 内部的局部状态完成，尚未形成稳定 URL、Vue Router 路由边界、Pinia 会话状态或统一 API 客户端。
- 当前 Vite 配置尚未代理 `/api`，本地前后端联调需要补充开发代理。
- 当前 `.gitignore` 已忽略通用 `.env` 与构建产物，但还需明确忽略运行时临时目录、覆盖率输出和部署用真实 secret；示例配置仍应允许提交。
- 2026-08-25 通过 npm registry 核对的当前版本为 `vue-router@5.2.0`、`pinia@4.0.3`；Increment 0 将锁定明确版本，避免部署时被浮动版本改变。
- 现有 Web 原型只有 `today/training/nutrition/history/settings` 的内存页面状态，所有入口都可在不改动页面主体的前提下映射到稳定路由；页面仍显示“Phase 3 / 体验稿”，进入工程骨架后应改为不暴露内部阶段术语的产品文案。
- 账号模块已能创建带 `passwordChangeRequired` 的预置管理员，但缺少修改密码的仓储操作与 API；若不补齐，管理员首次登录无法消除强制改密状态，因此属于 Increment 0 的功能缺口。
- 改密流程应验证当前密码、仅保存新的 Argon2id 哈希、清除 `passwordChangeRequired`、撤销该账号既有会话并签发新会话；这样默认管理员完成首次改密后不会继续使用部署时注入的初始凭证，其他已泄露会话也同时失效。
- 服务端目前只为 `/` 返回 Web 的 `index.html`；接入 History 路由后，直接打开 `/training` 等地址会在生产环境得到 404。静态服务需要增加“非 `/api` 的 GET/HEAD 路径回退到 `index.html`”，同时不能吞掉未知 API 的 404。
- 当前 `db:setup` 只执行 Drizzle 迁移和管理员初始化；部署编排还应在同一显式 setup 步骤执行 pg-boss schema 迁移，避免 worker 在运行时自行改库。
- Vue 工程已能在保留既有训练/饮食原型主体的同时拆出稳定路由：公开登录/注册、强制改密、管理员页与五个主功能 URL；主功能路由统一通过 Pinia 恢复 HttpOnly Cookie 会话，避免把令牌放入浏览器持久存储。
- 新增表单样式时发现现有设计令牌使用 `--color-error` 而不是 `--color-danger`；应沿用已存在令牌，避免浏览器把无定义变量导致的声明整条丢弃。
- Compose 若分别保存 `POSTGRES_PASSWORD` 与完整 `DATABASE_URL`，会制造两份必须人工保持一致的数据库密码。服务端配置应支持由 host/port/database/user 与同一个 `DATABASE_PASSWORD_FILE` 安全拼出连接串，同时继续保留本地开发使用 `DATABASE_URL(_FILE)` 的方式。
- 容器运行镜像不能依赖 `tsx` 或 Drizzle Kit 等开发依赖；setup、API 和 worker 都应运行已编译的 `dist` 入口，Drizzle SQL migration 文件作为只读运行资产进入镜像。
- 2026-08-25 官方来源复核：PostgreSQL 当前受支持主版本为 18，官方镜像存在 `postgres:18.6-bookworm`；PostgreSQL 官方建议同一主版本使用最新 minor。Node 官方镜像存在 `node:24.19.0-bookworm-slim`。部署骨架据此锁定明确的 PostgreSQL 18.6 与 Node 24.19.0 镜像标签，不使用 `latest`。
- 初始 migration 的密码列名称已经明确为 `password_hash`，但只有应用服务保证写入 Argon2id。为落实“数据库中也不允许明文密码”，数据库层应再增加摘要前缀 CHECK 约束，防止未来其他写入路径误把明文塞入该列。
- 本地浏览器验收通过：正常登录会按 redirect 返回、强制改密页可达、`/today` 与 `/training` 可直接导航和刷新、管理员页只显示注册开关与账号状态；390×844 视口下 `scrollWidth` 与 viewport 同为 390，无横向溢出，浏览器控制台无 warning/error。
- 浏览器安全策略不允许代理用户完成最终改密提交，因此页面级验收停在表单可达；改密提交、旧会话撤销与新 Cookie 已由服务端单元/路由测试覆盖。
- Increment 0 一致性复核发现少量文档仍使用“未来 server/deployment”或 Compose `app` 旧称；已统一为当前 `apps/server`、当前 `deployment` 与实际 `api` 服务名，并把 task plan 下一步改为真实 Docker/PostgreSQL 验收，不再重复已经完成的骨架工作。

## 2026-08-25：本地与服务器验收边界

- 当前本地继续开发只需要仓库已在使用的 Node.js 24 与 npm；不需要另外安装 PostgreSQL、pg-boss、Redis 或 Android 工具链。
- 增量 0 尚未完成的项目都依赖真实 Docker/PostgreSQL 环境，因此优先在长期运行服务器验收比继续扩充本机环境更贴近最终部署条件；服务器宿主机只需 Docker Engine、Docker Compose 插件和 Git。
- 产品所有者要求保存当前成果到 Git；本轮只创建本地提交，不把“提交”自动扩大为推送或服务器部署。
- Git 预检确认当前是 `main` 上尚无提交的新仓库，远端为 `https://github.com/Syriii/exercise-app.git`；未配置 Git 作者身份，真实 secret、`.env` 与 `.runtime` 均已被忽略。
- 当前仓库已单独配置公开作者身份 `Syriii <Syriii@users.noreply.github.com>`，未修改全局 Git 配置；常见私钥、云密钥和 API key 模式扫描没有命中。
- 首次提交暂存审查覆盖 78 个文件；内容为规划/产品/架构文档、Vue Web、Fastify 服务端、Drizzle migration、pg-boss 适配和部署示例，没有 `node_modules`、`dist`、真实 secret、数据库、照片或备份。`git diff --cached --check` 通过。
## 2026-08-25：Increment 0 本地收口范围

- 当前本地骨架已经覆盖身份、管理员基础能力、PostgreSQL/Drizzle、pg-boss 与 Docker 部署定义，但仍缺少三类可在无服务器连接条件下完成的基础设施收口：临时媒体存储抽象、管理员运行状态展示、可重复执行的数据库/API/浏览器测试入口。
- 临时媒体存储只负责服务器私有目录中的不透明对象键、原子写入、大小限制、读取、存在性检查和删除；不提供公开 URL，也不在本阶段增加拍照上传业务接口。图片在后续分析确认后删除的产品规则保持不变。
- 管理员运行状态必须来自真实信号：API 自身、数据库连通性与 worker 心跳分别展示，不能把“进程已配置”伪装成“进程健康”。worker 心跳适合存入 PostgreSQL，从而不引入 Redis。
- 数据库集成测试必须只允许连接显式测试数据库，并在名称不满足安全约束时拒绝运行；API 契约测试由 Fastify 的 OpenAPI 输出验证；浏览器自动化使用独立测试入口，不依赖生产账号或生产数据库。
- 当前机器没有 Docker、Compose 或本地 PostgreSQL，因此本轮可以完成代码、静态检查和不依赖数据库的测试；容器启动、真实迁移、持久卷与备份恢复仍须在具备 Docker 的目标环境验证。
- 代码核对结果：`temporary_media` 已有元数据表，但没有文件系统实现；管理员页仅有注册开关与账号状态；API 已有公开的 live/ready 探针，但没有可鉴权的分项运行状态；worker 启动后也没有可供 API 判断存活性的心跳。
- 本轮采用 PostgreSQL `runtime_heartbeats` 单例记录 worker 最近心跳。API 的管理员运行状态接口分别执行数据库探测并读取 worker 心跳，返回 `healthy`、`stale`、`unavailable` 或 `unknown`，前端按真实状态展示，不把未知状态显示为正常。
- 运行心跳属于部署基础设施，不承载业务任务状态；默认 15 秒写入一次，超过 45 秒视为过期，两个值允许用环境变量调整。
- 浏览器自动化依赖将固定为 npm registry 当前返回的 `@playwright/test` 1.62.1；只安装测试包，不在本轮自动下载浏览器二进制。测试入口使用内存身份仓库和构建后的 Web 资源，不接触生产账号、生产密码或生产数据库。
- PostgreSQL 集成测试入口将以 `TEST_DATABASE_URL` 为唯一连接来源，并强制数据库名以 `_test` 结尾；普通 `npm test` 排除集成测试，避免开发者误触真实数据库。专用集成命令缺少安全测试 URL 时应明确失败，而不是显示“跳过但通过”。
- OpenAPI 契约由与运行路由相同的 `buildApp` 生成，并用测试确认关键身份、管理与健康路径存在；契约生成只使用内存仓库和固定测试配置，不需要数据库与秘密文件。
- 当前环境已有 Playwright 可用的 Chromium 浏览器，因此无需额外下载浏览器二进制。首轮端到端执行确认测试服务、注册和改密页面均可到达；失败来自测试断言过宽和改密后的等待缺失，不是应用接口报错。
- 最终 Playwright 使用项目独立账号夹具消除了并发用户名冲突与共享管理员改密问题：桌面 Chromium 和 Pixel 7 视口均完成注册与管理员运行状态检查，桌面额外覆盖预置管理员首次改密，共 5 项全部通过、无跳过。
- 自托管文档当前已经正确说明四容器、named volume、IP + 端口、HTTPS 边界、secret、备份恢复与未验收项；需要补充 worker 心跳环境变量和管理员分项状态的解释，但不能把本地内存 E2E 等同于真实 Compose 验收。

## 2026-08-25：Increment 0 服务器验收自动化

- 产品所有者允许继续开发，但当前阶段规则仍要求先完成增量 0，不能直接跳到训练业务。接下来的可逆本地工作是把目标服务器上的验收步骤做成仓库内脚本，而不是安装本地 PostgreSQL 或伪造容器结果。
- 验收入口需要分成“非破坏性烟雾检查”和“会创建隔离验证数据的完整验收”。前者检查 Compose 状态、live/ready、worker 心跳和容器安全边界，并创建后立即删除临时媒体写入探针；后者使用名称明确的临时测试数据库验证 migration、pg-boss、备份恢复与数据卷重建。
- 任何恢复演练都不得覆盖默认 `exercise` 数据库；脚本只允许操作带固定校验后缀的临时数据库，并在删除前再次核对名称。备份产物必须写入仓库外或被忽略的目录。
- 容器持久化测试不能执行 `docker compose down --volumes`；应写入专用探针数据，重建 `postgres` 容器后读取同一 named volume，再清理该探针。这样验证的是容器重建而非 volume 删除。
- 脚本必须在缺少 Docker、Compose 配置、secret 或目标服务异常时快速失败，并打印正在验证的步骤；当前机器只做 shell 静态检查，真实结果仍由目标服务器产生。
- 当前运行镜像在 build 阶段后执行 `npm prune --omit=dev`，因此不包含 Vitest，不能直接用 `api` 容器执行 PostgreSQL 集成测试。Dockerfile 应把“完成构建且保留开发依赖的 verification target”和“裁剪后的 production dependencies”拆开，Compose 仅在显式 verification profile 中构建测试 target，正式 API/worker 镜像保持不含测试工具。
- 集成测试目前只接受完整 `TEST_DATABASE_URL`。为了避免把含特殊字符的数据库密码拼入 Compose 环境变量或命令行，将增加分量式测试配置：host、port、name、user 与 password file，内部统一 URL 编码后再执行既有 `_test` 后缀防护。
- 服务器验收应保留三个独立入口：数据库/migration 集成测试、容器重建后的 named-volume 持久性测试、`pg_dump`→隔离临时库的恢复测试。一个汇总脚本负责排序，且只有显式设置破坏性验收开关时才会重建 PostgreSQL 容器。
- 首版脚本静态复核发现不能依赖宿主 shell 已导出 `.env` 中的 `APP_PORT`；烟雾检查已改为读取 `docker compose port api 3000` 的实际映射，仍允许通过 `APP_BASE_URL` 覆盖反向代理或远端入口。
- 验收脚本已采用固定隔离命名：业务集成库必须为当前库名加 `_test`，持久化探针以 `exercise_volume_check_` 开头，恢复库以 `exercise_restore_check_` 开头。持久化脚本只 `--force-recreate postgres`，从不调用 `down --volumes`；恢复备份只写入 `mktemp` 目录并在退出时清理。
- 现有自托管手册的手工备份示例写入 `deployment/backups/`，此前忽略规则未覆盖该目录；已补充 Git 忽略，防止私有数据库 dump 被误提交。
- pg-boss 12.27.0 源码核对确认：`work` 会根据任务的 `heartbeatSeconds` 自动以默认一半间隔调用 `touch`；supervisor 默认每 60 秒扫描失联心跳。故障测试可以把 supervisor 间隔仅在测试实例中缩短到 1 秒，启动一个真实子进程领取任务后以 `SIGKILL` 终止，再由第二个 worker 等待 heartbeat 超时后的重试并完成任务；生产默认值不随测试改变。

## 2026-08-25：增量 1 开始方式纠正

- 产品所有者明确指出尚有大量功能未开发，可以继续推进。由此撤销“必须等增量 0 服务器验收完成才写任何训练业务”的本地串行限制。
- 新的并行边界是：增量 0 继续保持“本地实现完成、真实服务器待验收”，不得伪报完成；同时开始不依赖该验收结果的增量 1 本地开发。若服务器验收发现数据库、任务或持久化问题，再回到地基修正。
- 增量 1 仍按已经确认的内部顺序推进：先做单次训练方案与实际训练，再做周期计划。首个切片必须保存用户归属、方案内容与实际记录，不能继续使用静态原型冒充业务实现。

### 首个训练闭环的领域落点

- 单次训练方案是可复用的训练选项，不是历史版本；首个闭环需要支持多个方案及归档，但不引入方案版本历史。
- 开始训练时复制必要的方案快照到实际训练，之后修改原方案不得改变既有训练记录。
- 实际训练由计划动作的独立执行状态、每组实际数据和额外动作共同组成；未填写的组数、次数、重量等仍表示未知，不得自动写成零。
- 训练可以从方案开始，也可以空白开始；首个页面优先把方案选择、动作完成和额外补充串成一个可保存的闭环。
- 训练时间不能假定为晚上。服务端应保存 UTC 时间，并为按日期回看保留训练发生时的用户时区与本地日期。
- 所有训练方案和实际训练都属于私有用户数据，服务端查询与修改必须以当前会话的 `user_id` 为边界。

### 开始实现前的仓库状态

- 当前服务端已按 `modules/identity`、`modules/media`、`modules/operations`、`modules/tasks` 分域，训练能力应新增独立的 `modules/training`，并沿用仓储接口、内存仓储测试和 PostgreSQL 仓储的现有模式。
- Web 端目前只有身份、管理员和统一原型仪表盘；训练闭环应新增真实训练视图与 API 客户端能力，不能继续向静态 `modules.ts` 填充演示数据。
- 工作区含有尚未提交的增量 0 文件和文档更新；新增训练改动必须在其上增量叠加，不回滚或覆盖这些用户已认可的工作。
- 当前 Git 仅有地基提交 `fd5cf95`，因此本轮验证要明确区分“既有未提交地基”与“新增训练切片”，且未经再次要求不提交、不推送。

### 现有接入模式

- `buildApp` 通过显式依赖注入装配身份与运维模块；训练服务也应作为可选依赖注册，使纯健康检查与现有测试不必被迫构造训练依赖。
- 身份路由已经提供同源写请求防护和基于 HttpOnly Cookie 的当前账号解析；训练路由应复用身份服务认证结果，不另建令牌或前端身份状态。
- 数据库定义集中在 `apps/server/src/db/schema/index.ts`，现有表统一使用 UUID、带时区时间戳、外键和必要索引；训练表应遵循同一规范并由 Drizzle 生成迁移。
- `/training` 当前仍由 `Dashboard.vue` 的内存示例状态驱动，勾选与输入都不会持久化。真实页面必须替换该路由，同时保留其他尚未开发页面的原型，避免一次重写整个仪表盘。

### 训练模块实现约束

- `IdentityService.authenticate` 已集中处理缺失、过期和停用账号会话；训练路由只需从同一 Cookie 获取当前账号，并把 `account.id` 传入训练服务，仓储方法仍须同时按资源 ID 与用户 ID 查询，形成纵深隔离。
- PostgreSQL 仓储已使用 Drizzle 事务处理成组写入。创建训练方案及动作、从方案创建训练并复制快照，都应在单个事务中完成，避免只保存根记录或部分动作。
- 服务端的模块依赖由入口显式创建；生产入口应装配 `PostgresTrainingRepository`，测试可装配 `MemoryTrainingRepository`，从而在无 Docker 环境下验证业务规则和跨账号隔离。
- Web 已有统一 `apiRequest` 处理 Cookie 与受控错误，训练 API 可直接复用；不需要引入新的前端状态库抽象，首个页面可用组件内状态完成加载、编辑与保存。

### 测试与界面复用点

- 服务端启用了严格 TypeScript（包括 `exactOptionalPropertyTypes` 与 `noUncheckedIndexedAccess`）；训练输入类型要明确使用 `null` 表示已提交但未知的可选量，避免用缺失属性在各层产生不一致。
- 身份路由测试已证明可以通过注册响应提取 Cookie 后测试私有接口；训练路由测试应建立两个账号并验证第二个账号无法读取或修改第一个账号的方案和训练。
- E2E 服务当前使用内存身份仓储。加入内存训练仓储后可让浏览器测试覆盖完整训练流程，同时不依赖本机 PostgreSQL。
- 现有视觉令牌和通用面板/按钮样式可复用；训练真实页只需增加方案编辑器、训练动作卡片和组记录控件的局部样式，不另起一套视觉系统。

### 首轮训练代码落点

- 新增训练领域类型、受控错误、仓储接口、内存仓储和服务层；方案与实际训练都带修订号，写请求携带预期修订号，冲突时返回 409，避免多个页面静默覆盖。
- 训练方案目标支持可选组数、次数区间、重量、时长、距离与备注；实际动作保存独立组记录，并允许只标记状态而不填写数值。
- API 第一批覆盖方案列表/创建/编辑/归档，以及训练开始/读取/动作更新/额外动作/结束；尚未在首批加入复制方案、取消归档和删除实际动作。
- 首次 TypeScript 检查定位到结束训练路由的 JSON Schema 少一个闭合括号；该问题属于新增代码语法错误，已在同轮修正并登记。

### 首轮训练测试反馈

- 服务层 4 项规则测试已实际通过，包括方案快照、未知值不归零、修订冲突和跨时区本地日期。
- 路由测试在创建方案时被请求 Schema 拒绝为 400，两个用例因此连锁失败；这说明业务服务本身可运行，但 HTTP 合约仍需定位和修正，当前不能把 API 切片标为通过。
- 严格类型检查另发现一处测试断言 API 使用错误，不涉及业务实现；修正后仍需重跑类型检查。

### HTTP 合约修正结果

- 400 的根因是 Ajv 类型转换遇到 `anyOf: [string, null]` 时把 JSON `null` 转为空字符串；数值联合若同样把整数分支放前，还可能把未知值转为 `0`，直接违反领域规则。
- 所有可空输入都已改为先匹配 `null` 再匹配具体类型，并加入代码说明；这既修复方案创建，也防止“未填写”在 API 边界被悄悄改写。
- 修正后训练服务 4 项测试、训练路由 2 项测试和服务端严格类型检查全部通过；跨账号列表隔离与外部资源 404 已得到实际覆盖。

### PostgreSQL 持久化落点

- 已新增 PostgreSQL 训练仓储：方案及动作成组创建/替换，开始训练时成组复制快照，实际动作和每组数据在事务内更新，所有读取与写入都同时约束 `user_id`。
- 方案和训练使用递增修订号；PostgreSQL 更新在锁定根记录或带修订条件的单条更新中完成，失败后区分资源不存在与修订冲突。
- 生产 API 入口、浏览器 E2E 内存服务和 OpenAPI 合约服务均已装配训练模块；服务端严格类型检查通过。
- Drizzle 已从当前 schema 生成迁移 `0003_gifted_warbird.sql`，共登记 14 张表；迁移文件只证明结构已生成，尚未在真实 PostgreSQL 上执行。

### 迁移静态复核

- 生成迁移包含三类枚举、方案/方案动作、训练/训练动作/实际组共 5 张新表，外键删除策略符合快照语义：原方案动作删除时实际训练仅清空来源引用，动作名称和目标快照仍保留。
- 方案动作和训练动作都以父记录加排序号建立唯一索引；实际组以动作加组序号建立唯一索引，可稳定恢复页面顺序并防止重复序号。
- `git diff --check` 通过，未发现空白符或补丁格式问题。
- 现有 PostgreSQL 集成测试在专用测试库迁移全部 Drizzle 文件；训练仓储需要加入一条实际事务/快照用例，但该用例只能在配置安全测试数据库时执行，不能用内存结果冒充。

### 服务端完整本地验证

- 已把 PostgreSQL 训练仓储的真实迁移、方案修改后快照不变和实际组保存加入专用数据库集成测试；当前环境未配置安全测试数据库，因此该测试只完成编译，等待服务器验收时执行。
- 普通服务端测试现为 9 个文件、32 项全部通过；严格类型检查与 OpenAPI 合约生成均通过，训练端点已出现在合约中。
- OpenAPI 复核发现实际动作的 `target` 子对象曾多暴露一个非必填 `exerciseName` Schema 属性，虽然运行响应不会输出该字段，但契约表达不够精确，已改为只列目标字段。

### Web 真实训练页的接入方式

- 当前导航外壳全部写在 `Dashboard.vue` 内，没有共享布局组件；为了先形成真实闭环，训练页可以复用相同 class 和导航数据实现独立视图，之后再在界面重构时抽取公共壳层。
- `/training` 应改指向真实 `TrainingPage.vue`，其余 `/today`、营养、历史和设置继续使用现有原型，确保本轮变更范围可控。
- 真实训练页要保留桌面侧栏和移动底栏，否则从原型切换到业务页会丢失主要导航；现有 `prototype-shell`、`desktop-rail`、`mobile-dock` 样式足以复用。

### Web 首个训练闭环实现

- `/training` 已切换为真实页面：可新建/编辑/归档单次方案，选择方案或空白开始训练，标记计划动作完成/跳过，逐组记录次数与重量，添加额外动作，并完成或提前结束训练。
- 页面保存的是服务端返回的当前修订；每次动作写入后用新训练实体刷新本地状态，从而使后续写请求继续使用正确修订号。
- 页面通过浏览器 IANA 时区开始训练，不再把训练时间固定为晚间；顶栏持续显示已完成项和当前方案内仍待完成项。
- Web 严格类型检查与生产构建均已通过，当前构建产物约为 146 kB JavaScript、26 kB CSS（压缩前）。

### 浏览器测试接入点

- Playwright 会先构建全仓库，再启动使用内存仓储的真实 Fastify E2E 服务，并分别运行桌面 Chrome 与 Pixel 7 视口；适合验证训练页从登录到持久保存的完整交互。
- E2E 账号和内存训练仓储在每次测试服务启动时重建，因此训练用例应自行注册唯一账号，避免与管理员测试共享业务状态。
- 首个浏览器用例应覆盖“创建方案 → 开始 → 完成计划动作 → 加入额外动作 → 结束”，并同时确认移动端没有丢失主要控件。

### 首轮浏览器验收结果

- 沙箱内全仓库构建成功，但本地 Fastify 监听被 `EPERM` 阻止；获得限定权限后 Playwright 正常启动并实际运行 7 项。
- 既有注册、管理员和改密共 5 项在桌面/手机视口继续通过，说明训练路由接入没有破坏身份与运维流程。
- 新增训练用例在两个视口都于“保存方案”后失败，页面显示通用客户端错误且编辑器未关闭；失败一致而非响应式布局差异。需要从网络响应或页面异常继续定位，当前训练浏览器闭环尚未验收通过。

### 浏览器失败诊断材料

- Playwright 错误上下文确认表单值完整保留，失败发生在提交处理而非定位器或按钮未触发；页面只显示非 `ApiError` 的通用错误，倾向于成功响应解析、列表刷新或前端运行时异常。
- Trace 包同时保存 `0-trace.network` 与资源响应；初次只检索 `test.trace` 没有命中网络详情，后续应直接读取 network 事件或在用例中等待方案 POST 响应。

### 方案保存失败根因

- Network trace 证实点击保存后没有发出方案 POST，请求之前即在前端转换阶段失败；第二轮通过受控浏览器错误日志定位为 `trim is not a function`。
- Vue 对 `type="number"` 输入的 `v-model` 运行值可成为数字，而表单接口与转换函数只按字符串处理；这是静态类型检查无法覆盖的 DOM 运行时差异。
- 修复方式是让整数转换函数显式接受字符串或数字，同时继续把空字符串转换为 `null`；不改成 `Number("")`，避免空值重新变为 0。

### 浏览器闭环最终结果

- 首次修复后方案创建和开始训练已经通过，第二处同类问题出现在实际组过滤：次数输入也是数字运行值，仍被调用 `.trim()`；统一字符串/数字非空判断后消除。
- 最终 Playwright 7 项全部通过，其中新增训练闭环在桌面 Chrome 与 Pixel 7 两种视口均完成“建方案、开始、保存计划动作实际数据、添加额外动作、结束训练”。
- 临时测试控制台监听已移除；产品页面保留受控 `console.error` 便于定位非预期运行错误，用户界面仍只显示安全的通用提示。

### 首个训练闭环收口

- 当前切片已从静态训练演示升级为可持久化的真实业务流程，且产品、领域、服务、数据库、API、Web 和浏览器测试的快照/未知值/账号边界一致。
- 工作区仍叠加了尚未提交的增量 0 改动；本轮没有替用户提交或推送。服务器上的 PostgreSQL 迁移与仓储集成用例也仍未执行，不能表述为部署完成。
- 按已确认的训练内部顺序，下一项是周期训练计划，而不是重新讨论单次方案或直接跳到营养；周期单元应复制单次方案内容，且只允许用户主动重新导入尚未开始的单元。

### 最终本地复核

- 收口时补正额外动作备注的标准化：与计划动作相同，前后空白会清理，空字符串仍保存为 `null`；对应服务测试已覆盖。
- 最终根目录 `npm run check`、`npm test`、`npm run build` 与 `git diff --check` 全部通过；普通服务端测试仍为 9 个文件、32 项。
- 本轮没有运行需要 PostgreSQL 的 `npm run test:integration`，原因是当前环境没有配置名称以 `_test` 结尾的安全测试库；等待目标服务器验证，不以跳过冒充通过。

## 2026-08-25：周期训练计划开发恢复

- 当前有效规划、Agent 规则与进度记录一致：单次方案到实际训练已本地验收完成，下一条增量 1 能力是周期训练计划，真实服务器验收继续并行待办。
- 本轮继续使用现有 `training` 模块扩展，不新建第二套训练域，也不提前进入营养或图片分析。
- 周期计划必须保持“任意周数和训练日”“从单次方案复制而非实时引用”“只有未开始单元可由用户主动重新导入”的已确认边界。
- 工作区仍包含前序增量 0 和单次训练闭环的未提交改动；后续修改只能增量叠加，未经要求不提交或推送。

### 周期计划已确认的体验和数据语义

- 周期计划是面向复杂用户的独立层级，训练页默认仍优先展示单次方案；用户不需要先理解周期结构才能训练。
- 周期长度不固定，各周可以有不同数量的训练日；一个周期训练单元可以从空白编辑，也可以从单次方案复制名称、动作、顺序和目标量。
- 复制后单元内容独立，源方案修改不会自动同步；页面只需提示源方案有更新，并由用户主动重新导入。
- 重新导入是整体替换而不是合并，必须明确提示会覆盖本地调整；只有尚未开始的周期单元允许此操作。
- 开始训练时应把周期名称、周次/训练日和单元动作复制为实际训练快照；后续历史仍以实际训练为主，不把整个周期计划复制进历史。
- 现有 `training` 模块已经具备可复用的动作目标类型、修订冲突、账号隔离和训练快照机制；周期能力应扩展这些接口，而不是复制一套动作模型。

### 周期计划首个实现切片

- 周期计划根记录需要名称、备注、周数、当前修订和归档状态；周期单元需要周次、该周内的显示顺序、名称、动作副本、可选来源方案、来源方案导入时修订号和导入时间。
- “训练日”先作为周期内的有序单元而不是固定星期枚举，这样既支持周一/周二式安排，也支持用户只按第 1、2、3 个训练日执行；页面可让用户自行命名。
- 是否可重新导入不能只看周期单元本身，还要判断是否已经被实际训练引用。首个实现将由仓储查询引用关系，已有训练快照的单元拒绝重新导入。
- 开始实际训练需要扩展来源：现有 `sourceTemplateId/sourceTemplateName` 保留，另加可空的周期计划、周期单元、周次和训练日名称快照；从单次方案和从周期单元开始共用同一实际动作模型。
- 首个周期页面范围为创建/编辑/归档周期计划、空白单元、从方案复制、来源更新提示、明确确认后重新导入，以及从单元开始实际训练；可选日期安排留在增量 1 后续切片。

### 周期计划服务端模型落点

- 已在训练域增加周期计划、周期单元与单元动作类型和仓储契约；实际训练同时增加周期计划名称、单元、周次和训练日名称快照字段。
- 内存仓储与 PostgreSQL 仓储都使用周期根修订号保护元数据和单元变更；增加、编辑和重新导入动作副本在事务内完成。
- PostgreSQL 读取周期单元时会根据是否已有实际训练引用派生 `started`，而不是依赖可被漏写的布尔状态；内存仓储在创建实际训练时同步派生状态用于等价测试。
- 编辑或重新导入单元前都拒绝已有实际训练引用的单元；开始周期单元时复用现有实际训练动作快照和组记录结构。
- 分层实施中首次类型检查如预期发现 PostgreSQL 仓储未闭合接口；补齐 8 个周期方法和实际训练来源映射后，服务端严格类型检查已通过。

### 周期计划服务与路由验证

- 周期 API 已覆盖列表、创建、元数据更新、归档、增加空白/导入单元、编辑单元、重新导入和从单元开始实际训练；响应会返回来源方案修订和是否已开始。
- 服务测试确认源方案修改不会自动改变单元，只有显式重新导入才整体替换名称和动作；从周期单元开始后，实际训练保存周期/周次/训练日快照且单元进入不可覆盖状态。
- 路由测试确认周期计划列表按账号隔离；训练模块当前定向测试为 2 个文件、9 项全部通过，服务端严格类型检查通过。
- 创建导入单元的 HTTP 合约允许 `items: []`，服务端会从来源方案取得副本；空白单元仍要求 1–50 个动作，避免把导入和手工编辑的输入要求混为一谈。

### 周期计划迁移复核

- Drizzle 已生成 `0004_yielding_christian_walker.sql`：增加周期计划、周期单元、单元动作 3 张表，并给实际训练增加 5 个周期来源快照字段；当前 schema 共 17 张表。
- 单元按“周期 + 周次 + 顺序”建立唯一索引，动作按“单元 + 顺序”建立唯一索引；来源方案、周期和单元删除都只清空实际训练的可选来源外键，名称/周次/训练日快照仍保留。
- 迁移包含动作目标正数约束、周次与周数正数约束、修订正数约束；`git diff --check` 通过。
- 迁移仍只经过生成与静态检查，等待安全 PostgreSQL 测试库执行；这不改变增量 0 的服务器待验收状态。

### 周期计划前端接入检查

- Web API 客户端和训练页脚本已经接入周期计划的创建、编辑、导入、重新导入与开始训练操作，但页面模板仍只呈现单次方案，需要补齐两个计划层级的切换和周期内容。
- 训练页单位编辑表单中发现一个重复的 `targetRepsMin` 字段；本轮与周期页面一起修正，并以 Web 严格类型检查和端到端流程验证。
- 训练进行中标题需要优先显示周期名称、周次和训练日名称快照；单次方案与空白训练继续使用原有标题，避免用户开始周期单元后丢失上下文。
- 周期单元一旦产生实际训练，只禁止继续编辑或重新导入；仍保留“再练一次”，因为当前领域规则没有把周期方案限定为只能执行一次。
- Web 严格类型检查已在周期页面落地后通过；客户端请求体与服务端路由保持 `revision`、可选 `sourceTemplateId` 和单位内容的同一契约。
- 现有浏览器测试只覆盖单次方案闭环。周期页面至少需要补“建立周期、从单次方案复制到指定周、开始并看到周期快照”这一条桌面与移动回归；重新导入的替换/锁定语义继续由服务测试做更精确覆盖。
- PostgreSQL 集成测试已有单次方案快照基线，但尚未覆盖新增的周期三表和五个来源字段；新增用例应直接经 `PostgresTrainingRepository` 验证导入副本、显式重新导入和开始后的周期快照。
- 周期 E2E 首跑定位到前端输入差异：选择来源方案后，隐藏的空白动作表单仍被序列化为一个空动作，请求在进入服务前被 JSON Schema 拒绝。导入模式现在显式发送 `items: []`，由服务端按来源方案生成副本；空白模式仍发送并校验用户填写的动作。
- 修正后全仓库普通测试为 9 个文件、35 项通过，生产构建、OpenAPI 契约、`git diff --check` 通过；Playwright 桌面与手机共 9 项全部通过，包含周期导入与开始训练闭环。
- 周期训练计划首个纵向切片已完成本地验收。增量 1 仍有已确认能力未实现：提前按日期安排方案/周期单元/训练主题，以及训练建议、动作指导、消耗估算和提醒；不能把当前切片描述为整个训练增量完成。
- 单次方案复制已用现有创建契约实现为独立副本，名称自动加“副本”，动作、顺序、目标和备注复制，来源之后互不联动；桌面与手机周期 E2E 同时验证了复制结果仍可作为明确的导入来源。
- 下一条开发纵向切片确定为按日期安排训练，不提前跨入营养；它需要把单次方案、周期训练单元或纯训练主题安排到用户本地日期，并让“今天”页面读取真实数据。
- 交付基线明确把按日期安排放在增量 1，并把“今天还要练”限定为当天已安排或已选择内容；方案库中其他内容不能被误算为待办。训练建议与消耗估算属于增量 2，不应混入下一条排期切片。

## 2026-08-25：剩余开发持续目标启动

- 产品所有者要求完成当前规划中的剩余开发内容；该目标按既定增量持续推进，不把训练、规划、饮食、图片分析和部署验收压缩成静态演示。
- 首个继续实施的纵向切片仍是增量 1 的按日期训练安排，并接入“今天”和后续历史；完成后再按交付计划进入增量 2–4。
- 使用现有 `.planning/exercise-app/` 作为唯一持续计划，不创建第二套规划文件；每个可运行切片继续同步测试结果、局限和下一步。
- 当前规划仍明确处于 Phase 5；服务器验收门与本地业务开发并行，不能因缺少目标服务器连接而停止可独立完成的 Web 纵向切片。
- 完整剩余范围按交付依赖分为八个可运行切片：训练排期/历史、训练指导与提醒、官方规划、手工饮食、异步图片分析、长期运行、朋友开源安全、Android；每一项通过自身质量门后再推进下一项。
- 下一切片只实现训练安排事实和实际训练历史，不提前引入属于增量 2 的营养联动或训练消耗算法；这样“今天还要练”可以先建立正确事实源。
- 现有训练仓储没有排期实体，实际训练也没有排期来源；新切片需要在同一 `training` 模块扩展排期类型、仓储和来源关系，而不是创建独立日历服务。
- 当前 Web 没有独立 `HistoryPage.vue`，旧 Dashboard 路径也已变化；需要先按现有路由定位“今天/历史”原型，再替换为真实训练聚合页面。
- 当前原型壳位于 `apps/web/src/app/Dashboard.vue`，`/today` 与 `/history` 仍共用它；排期切片应新增真实 Today/History 视图并只替换对应路由，保留饮食和设置原型。
- 排期应增加私有根表并保存本地日期、时区、显示标题、可选单次方案/周期/单元来源和修订；实际训练增加可空排期来源。排期取消只改变排期状态，开始时由现有训练快照逻辑创建实际记录并关联来源。
- 周期单元当前通过实际训练引用派生 `started`；从排期开始仍需写同一 `source_program_unit_id`，保持锁定语义一致。
- 现有服务端 `startProgramSession` 会拒绝 `unit.started`，但 Web 已显示“再练一次”，与可复用周期计划语义冲突。排期切片应移除“开始过即不可再次开始”的限制，同时继续用实际引用禁止编辑/重新导入。
- 训练历史 API 目前只能按状态筛选，不能按本地日期范围查询；真实 Today/History 需要扩展仓储与路由的日期过滤，并保持服务端账号边界。
- 当前 Dashboard 同时承载多个原型状态；真实 Today/History 应拆成独立 Vue 视图，避免训练事实与饮食演示状态继续耦合。
- 排期 API 可以沿用当前公开映射和 Fastify JSON Schema 风格；新增日期查询时只接受 `YYYY-MM-DD`，服务层同时验证真实日历日期，避免 JavaScript 日期宽松解析。
- PostgreSQL 开始训练已经在单事务内创建训练根记录和动作快照；从排期开始应在同一事务内验证排期归属/状态、创建训练并把排期标为已开始，避免两个页面重复开始同一排期。
- 排期服务端首轮类型检查已在修正可选筛选属性后通过。下一组单元测试需要覆盖三种来源、取消、重复开始、无效日期、日期范围和跨账号访问；既有周期测试同时应确认同一训练单元可以再次开始但仍不能覆盖计划。
- Drizzle 已生成 `0005_pale_cannonball.sql`：新增排期表、实际训练的排期来源标题/ID和唯一来源索引，当前共 18 张业务表；真实 PostgreSQL 仍待服务器执行。
- 服务端定向验证现为 2 个文件、12 项通过，覆盖方案排期、纯主题取消、无效日期、日期筛选、单排期单训练和跨账号隔离。
- Web 训练页适合在单次方案卡和周期单元卡上提供“安排日期”，另设纯主题入口；Today/History 客户端只需要排期列表和按日期训练列表，不应复制整个训练编辑器。
# 2026-08-25：日期安排页面落盘复核

- `TodayPage.vue`、`HistoryPage.vue` 与路由修改均已完整落盘，没有出现大补丁被截断后只写入一部分的情况。
- “今天”页只把真实的当日安排、进行中训练和已结束训练作为训练事实；营养规划尚未接入时明确显示空值，不使用演示数字伪装完成状态。
- 历史页当前按日期聚合最近 90 天真实训练记录，并明确把饮食历史标为待接入；这保持了“优先回看实际做了什么”的既定方向。
- 下一验收门是 Web 严格类型检查、真实浏览器流程和完整回归；页面存在不等于切片已经完成。
- Web 的 `vue-tsc` 严格检查已通过；排期表单已有桌面双列与移动单列规则。今日卡片可以复用现有面板、列表和按钮体系，但仍需用浏览器验证具体布局。
- 现有 Playwright 只覆盖“方案直接开始”和“周期单元直接开始”，没有覆盖“安排到今天 → 今天开始 → 保存 → 历史回看”；该链路必须新增测试，不能由 API 单测替代。
- 日期安排编辑器默认使用浏览器本地日期和 IANA 时区，方案卡片通过带来源快照的安排按钮进入；因此端到端测试可以从真实 UI 建方案并安排当天，不需要测试专用接口或固定时区。
- Playwright 的 E2E 服务已经组合内存身份仓储与内存训练仓储，新增日期安排路由会随同一真实 Fastify 应用暴露；它适合验证前后端契约和移动/桌面交互，但不能替代 PostgreSQL 迁移验收。
- 日期安排的桌面与移动定向 E2E 均已通过；两次失败都由 Playwright 严格定位暴露的测试选择器歧义导致，没有 API 或产品状态错误。
- PostgreSQL 集成测试已经覆盖身份、实际训练快照和周期计划快照，但尚未覆盖新 `training_schedules` 表与“安排只启动一次”唯一性约束；应把该场景放入同一真实仓储测试，待有数据库环境时一并执行。
- 当前工作树包含此前多个已确认增量的未提交修改；本轮只继续在相关训练、测试和规划文件上增量修改，不清理或覆盖其他已有内容。
- PostgreSQL 启动安排采用“锁定安排行 → 检查取消/既有会话 → 插入会话”的同一事务，并由 `training_sessions.source_schedule_id` 唯一索引兜底；这支持 at-most-once 的业务语义。需要真实数据库集成测试验证状态回读与日期筛选。
- 服务层日期历史查询已经统一为 `{ status, dateFrom, dateTo }` 筛选对象，并拒绝倒置日期范围；集成用例可以直接验证指定日期只返回由该安排产生的实际训练。
- 已新增 PostgreSQL 集成用例，覆盖安排持久化、来源快照、只启动一次、按日查询实际训练及 started 状态回读；服务端严格类型检查通过。由于本机没有隔离 PostgreSQL，该用例保持“已编译、待服务器真实执行”。
- 根脚本的普通 `npm test` 明确排除 `*.integration.test.ts`，真实数据库用例只由 `npm run test:integration` 在 `_test` 数据库中运行；最终汇总必须分别报告这两种验证，不能把普通测试通过写成数据库通过。
- 日期安排切片最终回归通过：全仓库检查、38 项普通测试、API 契约、生产构建、`git diff --check` 以及桌面/手机共 11 项 E2E 全部通过；真实 PostgreSQL 集成仍按既定服务器门待执行。
- 下一训练切片不能只做“动作指导链接”：增量 1 尚欠的正式范围还包括训练提醒、计划动作跳过/替换的对应关系、历史实际量详情与用户主动修正。提醒属于独立规则，不应和规划引擎或饮食提醒耦合。
- 动作指导内容必须带来源、许可、版本、审阅状态及适用限制；无法确认开源许可的外部视频不能作为仓库随附内容。首个实现应提供元数据边界和用户自建/许可明确的内容入口，而不是抓取第三方视频。
- 当前实际动作只有计划名称、状态、备注和组数据；“换动作”无法区分“原计划是什么”和“实际做了什么”。最小且可解释的模型是在同一计划项实际记录上增加 `performedExerciseName`：照做时等于计划动作，替换时保存真实动作，跳过时为空，避免再建一条额外动作造成重复统计。
- 已结束训练的服务端动作更新路径目前可以工作，但 Web 历史只显示动作名称摘要，没有详情与修正入口；修正体验应复用同一个带 revision 的动作更新契约，并明确这是修正历史事实，不改原方案。
- 实际核对后发现服务层 `updateSessionItem` 和 `addExtraSessionItem` 都显式要求 session 为进行中，因此历史修正并未真正可用；需要把“训练中录入”和“结束后修正”统一为同一 revision 写入能力，而 `finishSession` 仍保持仅进行中可调用。
- 计划项完成请求当前没有实际动作名称字段。服务端应按状态规范化：pending/skipped 清空实际动作名和组数据，completed 必须有实际动作名（默认由客户端填原计划名称）；额外动作始终以自身名称作为实际动作名。
- 首轮字段补丁的失败位置已确认：数据库字段误落在 `training_template_items`，其检查约束却落在 `training_session_items`；Web 字段误落在 `TrainingTemplateItem`。正式修复应只移动字段，不改模板/周期来源结构。
- 实际训练输入原本只在 Web 暴露次数和重量，尽管服务端已支持时长与距离。训练中表单现同步暴露四类实际量，空字段仍保持未知；移动端需要继续把组输入降为单列，避免四列在窄屏压缩。
- 历史详情与修正复用同一实际动作 revision 契约：用户可把原计划动作标为跳过，或填写不同的实际动作形成替换关系；保存后只更新该次训练，不修改来源方案。
- 现有历史 CSS 使用 `.history-list article` 的宽泛选择器，新加入嵌套动作详情后会继承外层日期网格。需要用更具体的 `.history-item`/`.history-session` 规则重置嵌套布局，避免桌面三列规则污染详情。
- 仅递增 session revision 不能满足“旧修订可追溯”：值被覆盖后无法解释历史。训练项修正应在同一数据库事务内先保存旧状态和旧组数据快照，再写当前值；内存仓储也要实现同一契约，避免测试与 PostgreSQL 行为分叉。
- 修订存储采用每次动作更新前的旧状态快照，记录当时 session revision、状态、实际动作名、备注和完整组数据；查询先按 session 的账号归属鉴权，避免仅凭 item ID 越权读取旧数据。
- 动作指导首批目录采用仓库原创 MIT 文字草案，只覆盖深蹲、卧推、硬拉和平板支撑及明确别名；元数据明确 `draft`、版本和限制，无许可明确视频时返回空链接。未知动作显示“暂无合规内容”，不影响训练记录。
- 指导 API 仍要求登录，但内容本身不读取其他用户数据；这样既保持应用入口一致，也避免为了公共内容额外引入匿名 API 边界。
- 当前设置页仍是 Dashboard 中的原型开关，刷新即丢失且没有服务端语义。训练提醒应拆成真实 `SettingsPage`：保存账号级开关、时间和时区；“今天”页只在当日仍有未开始安排且达到提醒时间时显示，并支持当天稍后提醒或不再提醒。
- API 应把提醒服务作为可选应用依赖注入：生产和 E2E 显式提供，其他只测试身份/健康的应用构造无需被迫创建无关仓储。提醒错误需有受控 4xx 映射，不能落入通用 500。
- 训练提醒已形成真实纵向切片：账号级设置、基于当日未开始安排的 due 判断、当天 snooze/dismiss、设置页与“今天”页显示，以及用户授权时的前台浏览器通知。提醒状态不修改安排，普通测试与两个视口的定向 E2E 已通过。
- `0006_sleepy_tempest.sql` 只新增提醒规则/当天状态、训练动作旧版本表和实际动作名称列，外键、唯一索引与检查约束完整；真实执行仍等待隔离 PostgreSQL。
- 增量 1 收口前仍应补“训练归属日期/备注修正”和历史补记额外动作。否则虽然动作内容能修正，但跨午夜归属错误仍只能直接改数据库，不符合已确认领域规则。

## 2026-08-26：规划引擎证据复核

- 已完整复核 `docs/domain/calculation-evidence.md`；规划引擎必须保存方法版本、证据编号和输入快照，并明确适用范围、停止条件、单位与舍入规则。
- 已开始重新核验中国营养学会、国家卫生健康委员会和 National Academies 的一手官方资料；任何公式都不会凭记忆实现。
- 一次同时打开多份官方长文档时输出被截断，后续改为逐份、逐章节定位原文；若无法精确提取公式，只实现已有充分证据支持的安全子集，不输出伪精确数值。
- National Academies 2023 正式报告 Summary 表 S-3 已逐行取得 19 岁及以上男性、女性在 inactive、low active、active、very active 四类 PAL 下的 8 条 EER 方程；原文同时确认年龄单位为年、身高为厘米、体重为千克，体重稳定成人的 EER 等于 TEE。
- 首个实现只覆盖非孕期、非哺乳期的 19 岁以上成人；孕期和哺乳期虽然报告另有方程，当前产品没有足够健康状态输入与照护边界，必须停止自动计算，而不是静默套用普通成人公式。
- 中国营养学会官网正文确认 DRIs（2023 版）面向中国居民膳食评价和计划，18–49 岁成人公开数据位于表 9–12；表格以官网 JPG 提供，网页抓取层批量读取超时，必须逐张核对，不能只凭旧研究笔记写入固定参考量。
- 已从官网第二页源码定位 50–64 岁男性、女性的表 13、14 直接图片；成人 18–64 岁所需 6 张表均可逐张核对，且只在临时目录读取，不复制进 MIT 仓库。
- 官方表 9–11 已视觉核对：三组共同给出蛋白质 AMDR 10%–20%、碳水 50%–65%、脂肪 20%–30%；18–29 岁男性蛋白质 RNI 65 g/日、女性 55 g/日，30–49 岁男性 65 g/日。表中能量按中国 DRIs 自身 PAL 档位给出 MJ/日，只用于中国分组基线交叉检查，不与 National Academies 个体 EER 静默平均。
- 官方表 12–14 已视觉核对：30–49 岁女性和 50–64 岁女性蛋白质 RNI 均为 55 g/日，50–64 岁男性为 65 g/日；成人核心 AMDR 仍为蛋白质 10%–20%、碳水 50%–65%、脂肪 20%–30%。至此 E-001 中首个实现覆盖的 18–64 岁六张成人表已逐张复核。
- 中国 DRIs 分组能量交叉检查值（MJ/日，PAL I/II/III）已核对：18–29 男 9.00/10.67/12.55、女 7.11/8.79/10.25；30–49 男 8.58/10.46/12.34、女 7.11/8.58/10.04；50–64 男 8.16/10.04/11.72、女 6.69/8.16/9.62。该表的 PAL 数值随年龄组变化，不能把表脚的分类系数当成 EER 乘数。
- 国家卫健委《体重管理指导原则（2024 年版）》已从官方域名下载到临时目录，下一步用可用 PDF 运行时抽取第 1、4–5、11–14 页；系统缺少 `pdftotext` 不影响证据来源，但需要换用已提供的文档工具。
- 已用内置 PDF 运行时完整抽取指定页：第 4–5 页把常规自动减重参数限制在多数超重或轻度肥胖成人（BMI 24.0–32.5）语境，初始目标约每周 0.5–1 kg，并给出每日减少 500–1000 kcal 或总能量减少 30%，同时保持碳水 50%–60%、脂肪 20%–30%、蛋白质 15%–20%。首版应从范围保守端开始并保留来源，不能用于所有用户。
- 第 11–14 页进一步证明儿童、孕产妇、65 岁及以上老人、慢性病患者与体重过低者需要不同处理；首个通用规划引擎应在这些状态停止个性化自动减脂或运动蛋白质计算，并保留记录与一般说明。
- 国家卫健委正文及附表 1 已精确核对中国 18 岁及以上成人 BMI：`体重 kg / 身高 m²`，低于 18.5 为体重过低，18.5–23.9 正常，24.0–27.9 超重，28.0 及以上肥胖。BMI 只作适用边界和分组提示，不替代体成分或医学判断。
- 增量 2 继续沿用现有模块化单体方式：规划模块作为可选应用依赖接入 Fastify，内存与 PostgreSQL 仓储共享同一服务契约；新表从创建起绑定 `user_id`，计算输出保存不可变输入快照与方法版本。
- 现有设置页已经是真实训练提醒页，个人档案区仍是占位；增量 2 应在同一页面加入独立档案、身体测量和策略表单，不恢复原型状态，也不把饮食提醒与训练提醒合并。
- 规划模块数据最小分层确定为：当前档案（出生日期、公式所需生理分类、身高、健康资格与 PAL 选择）、身体测量时间序列、当前目标策略，以及按本地日期保存的每日参考修订。每日参考不能只保存最终数字，必须同时保存输入快照、BMI 分类、公式/证据版本、停止原因与限制说明。
- 现有 OpenAPI 合约测试只固定要求核心路径存在且不泄漏密码哈希；规划路由仍应像提醒模块一样为每个请求和响应声明 JSON Schema，避免为了“测试没要求”而留下无契约接口。
- 身体测量修正使用与训练事实相同的模式：更新前保存旧值快照，当前记录递增 revision；读取旧修订前先按账号确认测量归属。
- National Academies Summary 给出可直接复算的官方样例：22 岁女性、165 cm、63 kg、low active，方程结果为 2,275 kcal/日；确定性计算器已用该样例锁定单位和逐 kcal 舍入，避免公式系数正确但单位错误。
- 首批规划计算测试 6 项通过：官方 EER 样例、PAL 未确认范围、适用减脂的保守差额和宏量分配、BMI 边界停止、孕哺停止、增重不编造盈余，以及方法/证据版本。全仓库严格类型检查同步通过。
- `0008_mixed_mimic.sql` 已由 Drizzle 从正式 schema 生成，新增档案、策略、测量/修订和每日参考表及四个受控枚举；外键、账号索引、revision 约束、数值边界和每日参考唯一修订约束均存在。真实 PostgreSQL 执行仍属于服务器验收门。
- “今天”的营养卡和饮食路由仍在读取原型占位值；增量 2 要把二者改为同一个真实每日参考 API。饮食记录入口可以保留“下一切片接入”状态，但不再显示可点击的假分析结果或本地策略单选。
- 设置页现已接入真实档案、测量时间序列/修正和目标策略；“今天”与新的独立饮食页读取同一每日参考 API。原 `Dashboard` 不再承载 `/nutrition`，因此旧示例识别按钮不会出现在当前产品路由。
- 合约测试应用原先只注入身份、训练和运行状态服务；要让生成的 OpenAPI 真实覆盖规划接口，必须给合约应用注入内存规划服务，并把新路径加入固定断言。
- 新增规划浏览器流程在桌面和手机两种视口均通过：注册 → 保存档案/PAL → 记录体重 → 保存策略 → 复算官方 2,275 kcal 样例 → 修正体重 → “今天”采用修正后的同日有效测量。首跑失败只是断言忽略了同一元素内的 `kcal` 单位，页面与计算值当时已经正确。
- PostgreSQL 集成用例已加入源码，覆盖规划输入、官方样例结果、测量修订、每日参考新修订、旧输入快照保留和跨账号空结果；本机没有 PostgreSQL，因此只能报告已编译，不能报告真实执行通过。
- 已按产品所有者最后一次明确纠正清理旧“训练完成后重算饮食”规则：产品、领域、证据、体验、技术架构、交付计划、任务计划、README 与决策记录现统一为两项独立结果；D-038 正式替代旧 D-017，归档稿不改。

## 2026-08-26：手工饮食闭环设计收敛

- 手工餐食的最小事实模型不是四个全天数字，而是“餐食 → 当前有效营养贡献项”。贡献项分为单个食物、整餐总量和补充未覆盖部分；每项都保存份量基准与四项核心营养的已知/未知值。
- 整餐总量与食物条目不能静默重复计入：保存整餐总量时必须显式替代现有条目；已有整餐总量时再加条目也必须显式替代。被替代或修正的旧值保留修订痕迹。
- 日汇总逐营养素保留完整性：有已知值时可以合计并计算剩余量，但任一餐或当前贡献项缺该营养素时标为部分汇总；全天覆盖状态默认未知，只有用户自愿确认后才变为已覆盖。
- 中国常见食物平台尚无可兼容 MIT 整库再分发的许可，因此本切片不复制第三方大菜单；先实现手工食物条目、份量和用户自己的可复用条目，公共食物库仍需合法数据源。
- 跨文档复核发现体验稿仍有一句“完成或放弃后更新当日参考”；这里的“参考”是训练页的进行中参考，但容易被误读为营养参考。应明确改成“训练进度”，与 D-038 保持同一术语。
- 饮食模块可沿用规划模块的纵向边界：类型、仓储契约、内存/PostgreSQL 实现、服务和带 JSON Schema 的路由各自分离，再作为可选依赖接入应用。这样身份或健康检查的小型测试构造不会被迫创建饮食仓储。
- 当前数据库 schema 已统一从 `apps/server/src/db/schema/index.ts` 导出并由 Drizzle 生成顺序 migration；饮食表应继续使用账号级外键、日期索引、正数 revision 和营养值非负检查。前端饮食页当前只有真实规划参考与明确占位，适合直接替换为真实餐食汇总，不需迁移旧假数据。
- PostgreSQL 仓储现有约定是把 `numeric` 在模块边界转换为 `number`，修正时在同一事务中锁定当前行、保存旧快照并用 revision 条件更新；饮食餐次和贡献项修正将沿用该并发控制。每日汇总不另存派生数值，而是读取当日有效贡献项即时计算，避免目标或记录修正后产生旧缓存。
- 饮食持久化采用软删除/替代：删除餐食或常用项只从当前视图排除，整餐与条目互换时旧贡献标记为 superseded，并把其当时值写入修订表。Drizzle 已成功生成 `0009_curly_black_tarantula.sql`，包含 6 张饮食相关表；真实 PostgreSQL 执行仍等待服务器验收。
- `0009_curly_black_tarantula.sql` 已人工核对：账号外键、营养非负/至少一项已知、revision、用户日期索引和全天覆盖唯一键均已生成。OpenAPI 合约测试当前只枚举规划路径，需要把餐食、日汇总和个人常用项路径纳入固定断言。
- 历史页已有“全部/训练/饮食”筛选但饮食仍是占位；最小一致改法是同时读取最近 90 天餐食，按日期与训练合并，饮食卡提供当天汇总和“打开这一天修正”入口。具体修正继续复用饮食页的日期查询，避免在历史页复制完整编辑器。
- 首次饮食 E2E 已证明注册、官方规划资料、建餐次和页面状态均正常；失败发生在提交营养贡献之后，页面保留空餐并落入非 `ApiError` 的通用提示，说明需要继续检查客户端在同一保存动作中的后续运行时错误，而不是先改汇总公式。
- Playwright 当前 trace 的网络文件名是 `0-trace.network`；初步过滤只看到饮食页初次 GET，请求失败需要用精确 POST 过滤继续定位。身份路由测试已有可复用的 Fastify inject + 会话 Cookie 模式，也应补一条饮食路由契约测试以覆盖序列化边界。
- 饮食 E2E 已实际走通餐次、未知营养、常用项、修正和历史入口；历史能量条目本身完整时应显示 `250 kcal`，不能因脂肪未知而把能量也标成部分。手机端失败来自测试点到了底部导航而非历史筛选，现已限定筛选容器。
- “今天”页仍只展示系统营养参考，尚未消费新的日汇总；这与已确认的“持续展示还能吃多少”不符。当前切片收口前必须把今天页改为读取同一个 day-summary，同时继续保留参考来源和未知状态。
- “今天”现已改读与饮食页相同的 day-summary，并保留体重日期和方法版本；完整 E2E 首轮 13/15 的唯一回归是饮食页重写时遗漏规划消息/限制与方法标签，已恢复且定向规划 E2E 2/2 通过。
- 现有 PostgreSQL 集成测试集中在单一受保护测试库文件，规划用例后可直接追加饮食仓储用例；它应覆盖软删除、贡献修订、日覆盖、账号隔离和 migration 0009，但本机仍不能宣称真实执行。

## 2026-08-26：DeepSeek 视觉契约复核

- 直接打开产品所有者提供的视觉与价格页均在抓取层超时；官方域名搜索可以读取当前价格页。当前官方页面显示模型已是 `deepseek-v4-flash` / `deepseek-v4-pro`、OpenAI 格式 base URL 为 `https://api.deepseek.com`，两者支持 JSON Output；旧的 `deepseek-chat` 不能继续写死为新实现默认值。
- 官方价格页和首次调用页只证明当前模型名、JSON 输出、基础地址与文本调用格式，尚不能单独证明图片内容的具体编码结构。视觉适配器必须等待视觉页的请求示例或可执行契约验证，不能根据“OpenAI 兼容”自行假定所有多模态字段完全相同。
- 官方域名当前可检索的 GitHub Copilot 集成页明确称 DeepSeek V4 为纯文本模型，图片由其他 Copilot 模型代理描述；这与产品所有者提供但暂时无法抓取的 `/guides/vision` URL 形成直接冲突。现阶段对“DeepSeek API 原生接图”的置信度应降为低，不能把真实适配器当成已确认能力。
- 经受控直连后，产品所有者提供的官方视觉页已确认存在，标题为“图像理解”，专用实验模型是 `deepseek-v4-flash-vision-exp`；它与纯文本 `deepseek-v4-flash` / `deepseek-v4-pro` 是不同模型，因此前述 Copilot 文档并不否定这个新增实验端点。原生视觉能力置信度现可提升为高，但仍应把 `-exp` 视为可变实验模型并保持配置化。
- 官方视觉页确认 JPEG、PNG、GIF、WebP，按实际内容判断格式；Chat Completions 使用块数组，可通过 base64 data URL、外部 URL 或 Files API `file_id` 传图，Responses API 使用 `input_image`。本项目的短生命周期原图最适合服务端读取后内联 base64，不需要把临时私有照片暴露成公网 URL，也不必为首版引入远端 Files 生命周期。
- 官方视觉页已临时下载到 `/tmp` 以继续核对限制；文件不进入仓库。HTML 含空字节导致普通文本扫描失效，后续必须显式按文本读取，不能因为初段已看到 48 MiB 就遗漏单图、像素或数量限制。
- 官方限制表已精确提取：base64/外部 URL 单图上限 32 MiB，请求体 48 MiB，单边最长 8192 px，支持最多 600 张；外部 URL 还受 8192 字符和 60 秒下载限制。产品首版应远低于平台上限（单张、受控 MIME、较小应用上限），避免手机上传、base64 膨胀和模型成本失控。
- 官方说明图片会按规则缩放且单图视觉 token 最高 384；这进一步证明“能识别图片”不等于“营养估算准确”。应用仍必须保存模型原始估算、依据与不确定性，让用户修正后才采用。
- 官方页面列出 `detail` 的 `low/high/original/auto` 和 Chat/Responses 两种结构，但本次正文抽取未完整取得各档具体语义。首版可配置使用 `auto`，不应凭名称推断营养识别准确度等级。
- 现有临时媒体存储已经具备流式大小限制、SHA-256、原子落盘、0600 权限、防路径穿越、打开/存在/删除接口；缺的是 MIME 魔数校验、媒体数据库登记、账号路由和分析完成后的删除编排，无需重写文件存储。
- 现有 pg-boss 抽象已经支持队列定义、入队、并发 worker 和事务内入队，但 worker 目前只有心跳，没有业务任务处理；背景任务/尝试/临时媒体表也只有 schema。图片增量应补一个分析领域服务作为权威状态，而不是让 pg-boss job 自身成为产品状态。
- 配置当前只有临时媒体根目录，没有 DeepSeek key/model/base URL 或应用级上传上限；Compose 的 API/worker 已共享 temporary_media volume，适合 API 写入、worker 读取和删除。DeepSeek secret 需使用 `_FILE` 机制同时注入 API/worker，不能写进 `.env.example` 实值或数据库。
# 2026-08-26：图片异步估餐实现恢复检查

- 中断前新增的 `PostgresImageAnalysisRepository` 已完整落盘，包含创建、读取、尝试状态、重试、采用和临时媒体状态更新；恢复开发无需重写该文件。
- 当前生产 API/Worker 还没有装配图片分析模块，配置也尚未加入 DeepSeek 视觉模型参数；因此现阶段只能视为领域层半成品，不能宣称图片估餐可用。
- 仓库已有临时媒体存储、任务队列抽象、pg-boss 实现和 API/Worker 共用媒体卷，可在这些既有边界上闭合，不需要新增 Redis 或第二套任务系统。
- 生产 API 当前没有启动任务队列，Worker 也只写心跳；图片上传要能真实工作，API 必须负责入队且在停机时关闭队列，Worker 必须注册 `meal-image-analysis` 消费者。
- 图片接口采用原始图片请求体和真实 `Content-Type`，可避免只为单文件上传引入 multipart 依赖；服务仍会读取文件魔数并拒绝声明类型与实际内容不一致的请求。
- DeepSeek 配置应是可选能力：未提供 API Key 时普通训练、手工饮食和规划功能继续可用，图片上传接口明确返回“尚未配置”，而不是阻止整个服务启动。
- E2E 已有内存任务队列，可在同一测试服务中注册图片消费者并异步处理上传；测试不需要真实 DeepSeek Key，使用固定分析器验证产品闭环，真实供应商契约另以 mock-fetch 合约测试约束。
- 临时媒体存储已经用流式大小限制、随机对象键、`0600` 权限和路径穿越校验保护落盘；图片服务在此之上再校验文件魔数，并默认采用后删除原图。
- 生产装配现在采用同一个 PostgreSQL 营养服务作为图片采用入口：模型候选只有经用户确认后才成为 `model_adopted` 贡献，原始候选和当前采用值仍保持分离。
- API 与 Worker 分别持有 pg-boss 客户端：API 只保证队列并入队，Worker 注册单并发消费者；没有 DeepSeek Key 时 Worker 不消费图片任务，API 也不会创建新任务。
- Drizzle 已生成 `0010_awesome_edwin_jarvis.sql`：新增图片分析/尝试表、贡献来源字段和每次分析最多一个采用贡献的部分唯一索引；迁移未包含删除或重命名现有用户数据的操作。
- 现有饮食页的餐次卡片是拍照入口最自然的位置：照片属于一顿已建立的饭，分析完成后仍通过同一“整餐总量/补充项”规则计入汇总，不另造第二套营养累计逻辑。
- 前端 API 客户端允许调用方显式设置图片 `Content-Type`，因此可以直接发送 `File`；错误响应仍统一走现有 `ApiError`。
- 当前 Compose 默认只挂载数据库密码、会话密钥和管理员初始密码；DeepSeek 密钥若直接加入公共锚点会让未启用图片能力的部署也强制要求密钥文件。部署定义需要采用“始终存在但可为空”以外的安全方案，或清楚要求启用该能力时增加只读 secret 覆盖，不能提交占位 Key。
- 项目已经忽略 `deployment/secrets/*` 且只放行 `.example`；因此可以新增 `deepseek_api_key.example` 并把真实 Key 作为 Compose secret 文件挂载，既不会进入镜像，也不会进入 Git。
- 图片浏览器用例第一次失败发生在图片流程之前：注册后缺少导航完成等待，主动进入设置页取消了注册请求。失败快照仍是登录页，说明应修测试同步而不是修改认证逻辑。
- 图片异步估餐本地闭环最终通过：固定分析器验证了真实上传/异步/修正/采用交互，mock-fetch 合约验证了 DeepSeek `image_url` Base64 请求结构；这证明应用契约，不等同于真实 Key、模型营养准确度或供应商稳定性验收。
- 全量 Playwright 的 `.last-run.json` 为 `passed` 且无失败 ID；桌面与手机共 17 条流程全部通过，新增图片入口未破坏既有训练、规划、手工饮食、历史、提醒与管理流程。
- 长期使用增量已有可复用地基：`background_tasks`/attempts、临时媒体、运行心跳、审计事件和级联账号外键均已存在；导出/删除/清理不需要新增 Redis 或微服务。
- 当前提醒模块只有训练提醒，设置页明确把饮食提醒标为待接入，身体测量提醒也尚无实现；增量 5 需要把三类提醒保持独立，而不是扩展训练开关的含义。
- 当前管理员运行页只有 API、数据库和 Worker 健康状态；模型配置、任务、临时媒体、磁盘及备份/恢复验证摘要仍未实现。
- 确认后的增量 5 要求导出与删除走后台任务；现有 `background_tasks` 可作为权威状态源，导出产物可复用临时媒体接口但必须排除照片、凭据和会话。
- 现有训练提醒已经包含设置、当日派生状态、暂缓/忽略和浏览器通知闭环；饮食与测量提醒可复用其“独立设置 + 应用内状态”交互模式，但应拥有独立表和接口，避免开关互相影响。
- 复核 schema 聚合对象发现既有训练提醒表此前未列入 `schema` 对象；Drizzle migration 扫描顶层导出仍能发现表，但运行时 `createDatabase` 的 schema 元数据不完整。新增两类提醒时一并补齐训练提醒表，避免继续扩大遗漏。
- “今天”页当前只取训练提醒状态；新增饮食和测量状态可以与训练卡并列显示，且各自调用独立的暂缓/忽略接口，不改变下方“还能吃/还要练”的实时汇总。
- 账号导出不能简单转储全部用户关联表：`credentials` 与 `sessions` 必须明确排除，临时媒体只能导出生命周期元数据而不能导出对象键或原图；没有直接 `user_id` 的动作、修订和分析尝试需要通过父记录归属筛选。
- 账号删除请求接受后会立即撤销当前会话并停用账号，再由 Worker 删除临时文件和数据库账号；前端不能再调用普通 logout，应直接清空本地会话状态并回到登录页。
- 认证页由同一个 `AuthPage.vue` 同时承载登录与注册；账号删除请求后的提示应只在登录模式且带受控 query 时显示，不新增独立页面。
- 管理页现有健康卡片网格可以承载模型、任务、媒体、磁盘、备份和恢复摘要；新增摘要接口只暴露计数、时间和非敏感模型 ID，不暴露 Key、对象键、照片或其他账号记录。
- 长期运行部署配置已加入导出大小/保留期和媒体清理间隔；备份脚本采用自定义格式 `pg_dump`、临时文件完成后原子改名、SHA-256 清单、可选异地镜像和限定文件名的保留期清理，明确不包含临时照片。
- 备份与恢复验证脚本已经落盘，但当前文件尚无可执行位；在脚本语法和 Compose 配置检查前需要补 `chmod +x`。真实备份、还原和维护事件写入仍只能在服务器 Docker/PostgreSQL 环境验收。
- 两个运维脚本已补可执行位并通过 `sh -n`；Drizzle 已生成仅新增 `maintenance_events` 的 `0012_hesitant_psylocke.sql`，全仓库严格检查以及 18 个测试文件、71 项服务端测试均通过。
- 自托管文档仍保留手工 `pg_dump` 为主要备份步骤，尚未说明新的原子备份脚本、清单、保留期、异地镜像、恢复验证记录和管理页摘要；长期运行切片收口前需要以脚本为唯一推荐入口并保留手工命令作为原理说明。
- Web E2E 没有仓库内独立 server 源文件；Playwright 服务由配置/服务端测试入口装配，规格按 `auth-and-admin`、训练、规划、饮食和图片流程拆分。长期使用流程应新增独立 spec，复用现有注册方式，不复制测试服务。
- 实际 E2E 服务位于服务端 `src/testing/e2e-server.ts`，生产构建后由 Playwright 调用编译产物；它已装配内存可移植性队列、固定无敏感内容导出器、三类提醒与固定运行摘要，因此可以无 Docker 验证设置页完整交互和下载内容。
- 新增长期使用 E2E 在桌面与手机共 4 条流程全部通过：饮食/测量提醒独立保存并在刷新后保留，后台 JSON 导出可下载且不含密码哈希、会话令牌、对象键或原图，账号删除要求用户名/密码/明确确认并立即终止会话与阻止重新登录。
- 运维摘要目前已在管理员页面展示，但 API 响应 schema 仍是 `additionalProperties: true` 且没有专门测试；收口前应固定模型、任务、媒体、磁盘、备份和恢复字段，防止将来意外序列化敏感扩展数据。
- 运维摘要 API 已改为逐层 `additionalProperties: false` 的字段白名单，管理员可读、普通用户拒绝，并验证服务实现即使意外多返回敏感字段也不会序列化。
- 全量类型检查、18 个文件 71 项服务端测试、生产构建、OpenAPI 生成、shell 语法和 `git diff --check` 已通过；README、AGENTS、任务计划和交付方案仍把增量 5 写成“正在开发”，需要在全量浏览器回归后统一改为“本地完成、服务器验收待执行”。
- 桌面与手机全量 Playwright 共 21 条流程全部通过，生产依赖官方审计为 0 个漏洞；增量 5 已同步为“本地完成、真实服务器验收待执行”，当前开发范围切换到朋友与开源安全收口。

## 2026-08-26：朋友与开源安全收口预检

- 当前所有外部业务服务都显式接收 `userId`，训练路由、规划/饮食服务和图片服务已有若干跨账号拒绝测试；但没有一份覆盖全部私有根表、子表、导出下载和修改路径的统一访问矩阵，因此“全量隔离”还不能宣称完成。
- PostgreSQL 40 张表的 RLS 当前全部关闭。直接在现有表上启用并强制 RLS 会破坏 API/worker：应用和 migration 使用同一表所有者连接，PostgreSQL 表所有者默认绕过 RLS；强制后，现有查询又没有事务级 `app.user_id` 上下文。不能只生成看起来有策略、运行时实际被 owner 绕过的迁移。
- 真正有效的 RLS 需要区分 migration owner、受限 API role 与可信 worker，并让每次 API 业务查询在固定连接/事务内设置账号上下文；管理员也不应因此获得健康数据读取权。该改造必须与真实 PostgreSQL 集成测试一起实施，不能用内存仓储替代。
- 当前 Fastify 应用没有请求限流；已有单图 8 MiB、导出 64 MiB 等单请求边界，但还缺注册/登录防滥用、每账号并发图片任务、临时媒体总量和总账号数上限。对不超过 10 人的单机部署可采用进程内请求限流加数据库权威容量检查，无需 Redis。
- 身份模块已有管理员启停账号和撤销会话，但没有忘记密码/管理员重置入口。朋友阶段适合由管理员设置一次性临时密码并强制下次登录改密，不需要引入邮件服务。
- 账号创建已经在 PostgreSQL 事务中锁定单行 `app_settings`，可在同一锁内检查可配置账号上限，避免两个并发注册同时越过“最多 10 个账号”；预置管理员应计入总数，但初始化需继续绕过上限。
- 当前 `updatePassword` 总会把 `passwordChangeRequired` 写为 false；管理员重置需要把仓储方法扩展为显式布尔参数，保存 Argon2id 临时密码、强制下次改密、撤销全部旧会话并写审计事件，响应绝不能返回临时密码或哈希。
- `MAX_ACCOUNTS` 已接入生产 API 配置，默认 10；E2E 明确使用 100，避免同一内存测试进程中的独立用例互相耗尽生产配额。账号上限和管理员临时密码重置仍需补服务/路由测试与管理页交互后才能收口。
- 账号容量检查、管理员临时密码服务/路由和管理页表单已实现：临时密码长度复用 12–128 边界、只保存 Argon2id、重置后强制改密并撤销旧会话；管理页提交后立即清空输入。仍需类型检查与桌面/手机 E2E 验证。
- 进程内限流已加入认证、所有写请求和图片上传三档可配置窗口；限流键使用 Fastify 解析的客户端 IP，适合当前单 API 实例且不引入 Redis。首次类型检查只发现训练路由测试残留手写 `AppConfig`，生产实现本身没有类型错误。
- 账号容量、管理员临时密码和限流的严格类型检查已通过，3 个聚焦测试文件共 16 项通过；管理页浏览器用例已补齐，复用真实“修改密码”页面，等待生产构建后的桌面/手机执行。
- 图片仓储现在可按账号统计 pending/running 数量和仍占空间的 available/deletion_pending 媒体字节；服务在写入前检查进行中任务，在写入后用实际字节检查总配额，超额时通过现有异常清理路径立即删除新文件。默认 3 个进行中任务、256 MiB，可配置且无需 Redis。
- PostgreSQL 18 官方文档确认：开启 RLS 后无策略默认拒绝，但 superuser、`BYPASSRLS` 和通常的表所有者会绕过；只有 `FORCE ROW LEVEL SECURITY` 才让表所有者也受策略约束。`set_config(..., true)` 的上下文只在当前事务有效，适合避免连接池跨请求泄漏。
- Drizzle 官方 RLS 文档确认当前版本可用 `pgTable.withRLS`/`pgPolicy` 管理策略，也可以用原始策略；但框架声明本身不解决角色/事务上下文。当前项目应以可审阅 SQL migration 建角色/策略，以事务级 `set_config` 驱动，而不是依赖表所有者默认行为。
- 仓库锁定的 Drizzle ORM 0.45.2 类型仍提供旧式 `.enableRLS()` 与 `pgPolicy`/`pgRole(...).existing()`，尚不是文档最新的 `.withRLS()` API。实现必须按已锁版本生成并审阅 migration，不能为追新语法临时升级依赖。
- 最小有效运行方案确定为：setup/worker 继续使用 owner 连接；API 使用固定的非 owner、无 `BYPASSRLS` 角色；API 数据库池通过 AsyncLocalStorage 获取已认证账号，并把 `set_config('exercise.user_id', ..., true)` 放入同一事务后执行查询。这样连接归还池后上下文自动消失，worker 仍能处理跨账号后台任务。
- API 数据库池的账号上下文包装已实现并通过 TypeScript：普通 Drizzle 查询自动在短事务内设置本地账号，仓储显式事务在 `BEGIN` 后设置同一上下文；每个 Fastify 请求先清空上下文，身份认证成功后才进入账号。Worker/setup 未启用该包装。
- Drizzle Kit 0.31.10 官方 CLI 支持 `generate --custom`；已生成带 journal 记录的 `0013_api_rls.sql` 空迁移，用于角色/GRANT/RLS 等 schema DSL 不适合表达的安全 DDL，待填入并由真实 PostgreSQL 集成测试验证。
- `0013_api_rls.sql` 已覆盖 18 张带直接 `user_id` 的训练/规划/饮食/提醒/图片根表和 11 张通过父记录归属的子表；策略只授予固定 `exercise_api` 非 owner 角色，身份、队列、临时媒体和运维内部表不伪装成用户 CRUD RLS。
- Compose 已把数据库 owner 与 API 密码拆成两个 secret：API 容器只挂载 `api_database_password`，setup/worker 才挂载 owner 密码。setup 在迁移前幂等创建无 `BYPASSRLS` 的 API 角色，迁移后授予 public/pgboss 所需 DML；API 无法读取 owner secret。
- 直接 `db:migrate` 与真实 PostgreSQL 集成入口也必须先确保 API 角色存在，否则含 `TO exercise_api` 的迁移会失败；相关入口已接入角色创建/授权，待补 RLS 隔离用例。
- 新增可执行访问分类：30 张账号拥有表（18 张直接、12 张继承）必须出现在 RLS 迁移，10 张身份/任务/媒体/运维内部表必须有明确豁免理由；测试从实际 `schema` 读取全部 40 张表，新增表若未分类会立即失败。
- 复核分类时发现 `schema` 聚合对象遗漏训练动作修订和训练元数据修订两张既有表，已补齐；这不改变 migration，但避免运行时 schema/安全清单少算。
- 账号容量、临时密码、限流、图片配额、提醒隔离、导出下载隔离和 RLS 分类共 7 个聚焦测试文件 34 项通过；真实 RLS 行为测试已写入 PostgreSQL integration，仍等待 Docker/PostgreSQL 环境执行。
# 2026-08-26 — 朋友与开源安全文档落盘复核

- `docs/security/data-access.md`、`PRIVACY.md`、`SECURITY.md` 与 `CONTRIBUTING.md` 均已完整生成，不是截断文件。
- 文档已明确两层账号隔离、API/owner 数据库角色分离、管理员与主机运维者的信任边界、DeepSeek 照片发送与临时保留、账号导出/删除及开源贡献约束。
- 当前仍需同步自托管文档和 Compose DeepSeek overlay，避免新增 `api_database_password` 未进入部署步骤，或 overlay 覆盖基础 secret 列表。

# 2026-08-26 — 部署安全收口复核

- 基础 Compose 已把 API 限制为 `exercise_api`，且 API 不挂载 owner 密码；setup 同时持有 owner/API 密码，worker 只持有 owner 密码，职责边界正确。
- DeepSeek overlay 当前为 API/worker 各自重新声明单元素 `secrets` 列表；Compose 多文件合并对此类列表的结果不应靠部署者猜测，宜显式列出服务所需的完整 secret 集合。
- 自托管文档仍称“必须替换三个文件”，漏掉新增的 `api_database_password`，也尚未解释账号上限、三类限流和图片容量参数。
- `backup.sh` 接受 `BACKUP_RETENTION_DAYS=0`，与报错文案“positive integer”不一致；应明确拒绝 0。
- 限流器单元测试已存在，但还需要一个通过 Fastify hook 发请求的路由级回归，证明实际返回 429 与 `Retry-After`。

# 2026-08-26 — 安全测试入口定位

- 服务端测试与源码同目录，OpenAPI 回归为 `apps/server/src/openapi.test.ts`，真实数据库集成为 `apps/server/src/db/postgres.integration.test.ts`，migration 入口为 `apps/server/src/entrypoints/migrate.ts`。
- `app.test.ts` 仍维护一份完整手写 `AppConfig`；新增安全参数后容易再次漏字段，宜切换到统一 `createTestConfig`。
- 管理员临时密码 API 已有服务路由测试，但 OpenAPI 路径清单尚未包含 `/api/v1/admin/accounts/{userId}/password`。
- 迁移入口的 `node:fs` import 被第三方 import 隔开，功能不受影响，但应按 Node 内置模块分组整理。

# 2026-08-26 — 安全收口定向验证

- 严格 TypeScript/Vue 检查通过。
- Fastify 请求级登录限流、OpenAPI 管理员临时密码路径、限流器、数据访问矩阵、管理员路由、导出跨账号与三类提醒共 7 个文件 24 项测试全部通过。
- 自托管文档已经补齐 API 专用数据库密码、单机内存限流、账号上限、图片并发与临时空间参数；DeepSeek overlay 也显式保留基础 secret。

# 2026-08-26 — 管理员临时密码 E2E 根因

- 失败快照仍停留在登录页，管理页代码和“设置临时密码”按钮本身均已存在。
- 新用例点击管理员登录后立即 `page.goto('/admin')`，可能取消尚未完成的登录请求；此前图片流程也出现过同类同步错误。
- E2E 服务正确预置桌面/手机两个独立管理员，测试限流阈值为 10000，不是账号冲突或限流导致。
- 修复应在点击登录后先等待 `/today`，再进入管理页；不修改产品认证或管理界面。

# 2026-08-26 — “剩余开发”范围复核

- 朋友与开源安全收口的代码和文档基本齐全；全量服务端 20 个文件 82 项测试、生产构建、OpenAPI、Shell/YAML、diff 与敏感模式扫描通过，生产依赖审计为 0 漏洞。
- E2E 原有 21 项通过；管理员临时密码新流程因测试跳页过早失败，修正后桌面/手机 2 项定向通过。
- 对照已确认交付方案仍有两项 Web 产品能力未形成真实闭环：系统训练建议候选，以及可搜索的公共食物目录/最近使用。当前饮食只有账号私有“常用项”，规划引擎当前只生成营养参考。
- 公共食物目录受数据许可和内容来源约束，不能为“完成”而把薄荷健康等第三方数据库内容复制进开源仓库；可先实现可替换目录边界与明确许可的小型种子集，但完整数据覆盖仍需合法数据源。
- Android 明确依赖 Web 实际使用满意和真机验证，不属于当前可以在本地伪完成的剩余 Web 代码。

# 2026-08-26 — 训练建议实现边界

- E-013/E-014 已达到“可实现”，可支撑健康成人的总体活动边界和目标特异抗阻候选；领域规则已明确力量、肌肥大、功率三种候选及停止条件。
- 当前训练模块没有建议实体、接口或页面；只有用户方案、周期、安排、实际记录和动作指导。规划模块只生成营养参考。
- 若补训练建议，必须保存输入快照、方法版本、证据 ID、生成时间和过期判断；采用时复制成现有 `TrainingTemplate`，不让建议长期成为训练主页的第三套计划。
- 具体动作选择还缺少器械、经验、时间偏好等结构化输入。当前档案只有自由文本 `trainingIntent` 和健康边界字段，不能据此生成伪精确动作表；安全实现应生成参数化候选框架，并由用户编辑动作后采用，或先补训练偏好输入。

# 2026-08-26 — 食物搜索可交付切片

- 当前仓储已有按账号和日期范围读取餐食、读取个人常用项的能力，不需要新增表就能提供“个人常用 + 最近实际吃过”搜索。
- 搜索结果可以直接复用每次餐食已保存的营养、份量和基准快照，且继续受现有应用过滤与 RLS 保护；不把第三方数据库伪装成内置公共库。
- 初始实现应按名称查询、对同一份量快照去重、标明“我的常用”或“最近吃过”，并只负责带入表单，用户提交后才计入摄入。
- 公共中国食物目录仍需可再分发许可或合法运行时接口；个人历史搜索不是对这一许可缺口的掩盖，而是立即可用且合规的独立能力。

# 2026-08-26 — 食物搜索接入位置

- 饮食页面每个餐次已有同一份营养贡献表单与“我的常用项”下拉；搜索结果应直接带入这份表单，避免创建第二套保存逻辑。
- 现有样式已有 `.food-results` 列表，可在餐次表单前增加轻量搜索框、来源标签和“带入”按钮，只补少量布局样式。
- 现有饮食 E2E 会先保存米饭为常用项并修正为 250 kcal，可在修正后搜索“米饭”并验证带回表单，从而覆盖搜索、去重和不自动计入。

# 2026-08-26 — 食物搜索实现验证

- 新增 `/api/v1/nutrition/food-search`，按当前账号搜索近 90 天实际采用记录和个人常用项，最多返回 50 个去重快照；没有新增第三方内容或许可风险。
- 页面按餐次展示来源并把结果带入现有录入表单，选择结果不会自动计入或改变“还能吃多少”。
- 严格检查通过；服务/路由/OpenAPI 3 个文件 7 项测试通过；饮食完整流程桌面与手机 2 项 E2E 通过。

# 2026-08-26 — 系统训练建议持久化方案

- 为避免把候选塞进已经归用户所有的训练方案，训练建议应独立存为账号私有根表；候选被采用后再调用现有训练服务创建普通单次方案。
- 建议实体只需保存状态、revision、方法版本、证据 ID、档案/策略/偏好输入快照、候选模板和采用后的方案 ID；无需修改成熟的训练方案仓储接口。
- 新表必须进入统一访问分类与 RLS。由于 `0013_api_rls.sql` 已是历史 migration，应新增后续 migration 为新表创建策略，不回改旧 migration；访问矩阵测试也需支持跨多个 RLS migration 检查。
- 当前建议可以安全生成一个可复用的全身训练起点：频率、目标负荷/组数只采用 E-013/E-014；具体动作按用户选择的器械提供可编辑示例，不宣称官方指定动作。18 岁以下、65 岁以上、孕哺期、医疗营养状态或用户声明伤病/医疗限制时停止自动生成。

# 2026-08-26 — 训练建议模块装配约束

- 当前 TrainingService 已有完整方案校验与创建能力，建议采用应复用它，而不是在新模块复制输入校验。
- 规划服务可提供档案、目标策略与按时间排序的身体测量；建议快照可记录这些 revision 和偏好，列表时据此标记旧建议是否过期。
- 建议表作为第 41 张 schema 表加入；后续 RLS migration 和烟雾检查计数必须由 30 更新为 31 个账号私有表。
- 跨多个入口的一次性补丁两次因猜测锚点失败，后续严格按路由、app、生产、contract、E2E 单文件装配并即时类型检查。

# 2026-08-26 — 训练建议入口装配复核

- 生产 API 和 contract app 已正确共享 PlanningService、TrainingService 与 TrainingSuggestionService。
- E2E 服务的首次文本匹配把新字段放进 ReminderService 构造参数，已定位为装配错误；应移到最终 `buildApp` 依赖对象。
- 路由采用独立 `/api/v1/training-suggestions` 边界，避免把候选重新塞回训练方案路由；统一错误处理已接入 app。

# 2026-08-26 — 训练建议后端定向验证与前端位置

- 新表 migration 已由 Drizzle 生成并追加 `exercise_api` RLS；访问分类更新为 41 张总表、31 张 RLS 表，部署烟雾计数同步。
- 训练建议后端严格检查通过，生成/停止/过期/采用/跨账号和 OpenAPI 共 4 个测试文件 6 项通过。
- 训练页当前一次并行加载方案、周期和进行中记录；可同时加载建议。体验上建议入口应放在“训练计划”区顶部，采用后刷新普通方案列表并从候选区消失，不增加第三个长期 plan tab。
- 现有方案创建与编辑逻辑无需改写；新建议采用后返回的就是普通 `TrainingTemplate`，用户可继续使用原编辑、安排、复制与开始训练流程。
# 2026-08-26：饮食计划服务测试收口

- `NutritionService.createDietPlan` 是本轮唯一未声明为 `async` 的饮食计划写操作；校验错误因此同步抛出，而更新、归档及仓储接口都采用 Promise 语义。
- 保留现有日期范围校验，把创建方法统一为异步即可修复测试契约；无需把越界条目放宽，也无需修改调用端。

# 2026-08-26：新增体验的浏览器回归缺口

- 现有图片 E2E 仍断言旧流程“等待确认/按这些数值计入/已采用”；产品现已改为在没有现有值时先生成“暂定计入”，用户再确认同一条贡献，测试必须验证确认前汇总已出现且确认后没有重复计入。
- 现有饮食 E2E 已覆盖手工记录、修正、未知值和历史回看，但尚未覆盖独立的“我的饮食安排”以及历史页新增的 90 天趋势表。
- 上传用例使用只有 PNG 文件头的极小夹具，浏览器无法解码时会安全保留原图；它适合验证压缩失败回退与上传进度收口，不适合验证实际压缩率。
- 饮食安排表单具有稳定的无障碍标签，可在既有营养 E2E 中覆盖“范围、整体原则、餐次条目、保存后展示”，并同时断言实际摄入汇总未被计划改变。
- 历史趋势表按最近 90 天实际出现过记录的日期生成，最多展示 14 行；训练为零、营养未知、体重缺失分别显示 `—`，而汇总只计算真实保存的数据。

# 2026-08-26：PostgreSQL 回归覆盖复核

- 现有 PostgreSQL 集成测试已覆盖训练快照、周期、日期安排、提醒、规划快照、饮食修订、事务入队和 worker 被杀后的恢复，但新增饮食安排、训练消耗快照和图片暂定值尚未进入真实数据库用例。
- 营养路由测试原先只验证创建者可列出饮食安排；已补第二账号返回空列表，作为 HTTP 层账号隔离的快速回归。真实 RLS 仍应由 PostgreSQL 集成环境验证。
- 图片暂定采用的原子性位于 `PostgresImageAnalysisRepository.succeed()`：它在同一事务内锁分析、尝试和餐次，检查当前有效贡献，必要时插入唯一的 tentative 模型贡献并递增餐次 revision，最后才把尝试和分析置为成功。
- 训练消耗快照已由 PostgreSQL 仓储作为 JSONB 随场次及场次修订保存；新增集成用例应验证档案体重快照和旧 revision，而不是只验证计算函数。
- 根 `test:e2e` 会先重建全部 workspace，再由项目内存服务监听 `127.0.0.1:4174`，因此可避免旧 `dist` 造成的假失败；沙箱若拒绝监听，应按权限流程原样重跑。

# 2026-08-26：安全分类与迁移复核

- 全量测试捕获了新增 `diet_plans` 未加入账号表分类清单；表本身已经在 `0018` 中启用 RLS 并绑定 `exercise_api`，缺口是安全回归测试只读取旧的 `0013/0014` 迁移。
- 已把 `diet_plans` 加入直接账号归属表，并让安全测试同时核对 `0018`；迁移 journal 从 0016、0017 到 0018 连续，新增训练消耗、图片确认状态和饮食安排的顺序一致。
- OpenAPI 生成命令通过，包含饮食安排与训练消耗路由；其标准输出非常大，最终验收只记录命令状态和路径断言，不保存整份终端 JSON。
- 部署脚本实际全部位于 `deployment/scripts/`；根目录和服务端目录没有重复脚本。Compose 使用 PostgreSQL named volume、临时媒体 volume、只读应用容器、独立 setup/API 数据库身份及 secrets 文件，新增业务表不需要额外容器。
- 本地 PostgreSQL 集成命令有意要求显式的独立测试库凭据；Compose 的 `verification` profile 已提供隔离的 `${POSTGRES_DB}_test` 入口，因此本机无配置失败不能通过连接默认业务库规避。

# 2026-08-26：完成性缺口本地验收结论

- 严格类型检查、生产构建、23 个服务端测试文件 92 项、OpenAPI 生成、Compose YAML 解析、部署脚本 Shell 语法、`git diff --check` 均通过。
- Playwright 桌面与手机共 25 项全部通过；更新后的图片流程验证 620 kcal 暂定计入后修正为 590 kcal 且不重复，饮食安排和历史趋势也在两个视口通过。
- 生产依赖在线审计结果为 0 个已知漏洞。当前不能完成的是依赖外部运行环境的 PostgreSQL/Docker/真实 DeepSeek/备份恢复，以及 Web 实际使用认可后的 Android 真机与 APK。
- README、AGENTS、当前 task plan 与交付方案仍保留“正在补 Web 缺口”的旧状态，需要同步为“Web 本地实现与自动化验收完成，目标服务器验收待执行”，避免把外部门槛误写为产品代码未完成。
- README 中的 `TEST_DATABASE_URL=..._test` 示例仍与测试加载器一致；本轮无环境运行失败是因为两种合法配置（完整 URL 或分项变量）都没有提供，并非文档入口失效。
- 自托管验收脚本在部署目录内按 database/backup/persistence/full 分级，真实数据库与容器重建都带隔离名称或显式开关，可以直接作为下一阶段交接入口。

# 2026-08-26：照片确认的故障恢复复核

- 图片分析成功与暂定贡献已在 PostgreSQL 单事务内完成，但用户确认仍是“先更新餐食贡献，再标记分析已采用”两个仓储步骤；如果第二步并发冲突，第一次调用可能已把暂定值确认，而分析仍显示未采用。
- 当前重试路径只查找 tentative；若已经存在同一 `sourceAnalysisId` 的 confirmed 贡献，会误走新增分支并可能重复计入。最小安全修复是把同一分析的 confirmed 贡献视为幂等成功，再让重试只补齐分析状态；这不会削弱既有修订或用户值优先规则。
- 修复后新增故障注入测试：第一次确认让餐食成功、分析状态写回冲突；第二次确认只补分析状态，餐食 revision 不变且贡献仍只有一条。定向 14 项、全量 93 项和最终 25 条桌面/手机 E2E 均通过。
# 2026-08-26：目标服务器验收续接

- 当前 Web 本地实现与自动化验收已经完成；持续目标的下一项权威门槛是实际自部署环境，而不是继续增加未规划的本地功能。
- 本轮先只读探测本机 Docker/PostgreSQL 与既有 SSH 配置；只有确认存在安全、明确的目标环境和授权后，才执行会写入远端服务器的部署命令。
- Session catch-up 没有报告额外未同步上下文；task plan、progress 与 findings 的最新段落一致，当前唯一可推进的 Phase 5 队列项是自部署真实验收。
- 本机仍只有 `/usr/bin/ssh`，没有 Docker 或 `psql`，因此无法在本机补真实 PostgreSQL/Compose 验收。
- `~/.ssh/config` 确实存在目标服务器条目；解析后的目标是 `root@<server-ip>:22`。下一步只读取该条目的非秘密选项，并做无命令、短超时的 SSH 连通性验证。真实 IP 不写入公开仓库。
- 该 SSH 条目没有额外 IdentityFile、ProxyJump 或端口覆盖，使用系统默认密钥发现；首次连接未到远端，失败原因是本地沙箱禁止网络，而不是认证或服务器状态。
- 放开网络后服务器可达，但拒绝当前 shell 的公钥认证。`~/.ssh` 没有任何 `id_*` 默认私钥或公钥文件，只有 config/known_hosts；沙箱内也不能读取 ssh-agent，因此最可能是桌面会话密钥只存在于受保护 agent/钥匙串，或该 Codex 进程没有继承 agent 凭据。
- 受控读取确认当前 ssh-agent 实际为“no identities”，所以失败不是单纯的沙箱 socket 权限：当前进程没有任何可用于服务器认证的私钥。未授权前不能尝试密码、生成新密钥或把凭据写到配置。
- `config.bak` 也没有该服务器可用的身份配置，不能从旧配置恢复凭据。
- Git 当前位于 `main`，`HEAD` 与 `origin/main` 都是 `fd5cf95`、前后差异为 0；全部 Phase 5 实现仍是大量已跟踪修改和未跟踪新文件。远端基线一致，但本轮没有新的提交授权，不能把未提交工作误报为已同步 GitHub。
- 部署入口的结构合理：`full` 依次执行 smoke、隔离测试库、隔离备份恢复、仅重建 PostgreSQL 容器的 volume 持久化，再次 smoke；所有破坏性检查都有显式开关和受限数据库命名。
- 审查发现实际部署阻塞缺口：`smoke-check.sh` 仍硬编码 31 张 RLS 表，而新增 `diet_plans` 后安全分类已经是 32 张。若直接部署最新迁移，烟雾检查会错误失败；需要更新为 32 并让安全测试自动约束脚本常量。
- RLS 计数已更新为 32，并新增测试从 `rlsTables.length` 约束烟雾脚本；定向安全测试 3/3 与全部部署脚本 Shell 语法通过。
- 自托管文档仍保留旧图片语义“确认后才计入”，与当前“无现有值时先暂定计入、之后核对”冲突；部署前必须修正文档，避免真实用户误判当天剩余量。
- 首次部署依赖 `git clone`，但远端 GitHub 仍只有旧 foundation commit；即使 SSH 恢复，目标服务器也无法从 Git 获得当前代码。由于当前没有新的提交/推送授权，部署链还同时受 Git 发布状态约束。
- Compose 和应用只会拒绝过短 session/admin secret；所有仓库示例占位符本身足够长，数据库也会接受示例密码。如果部署者忘记替换，当前 `docker compose up` 可能用公开占位符成功启动，这是实际安全缺口。
- 应增加无副作用的部署 preflight：验证 Docker/Compose/Git/curl、Linux 架构、端口格式、真实 secret 文件存在且权限受限、未包含 `.example` 占位符、四个本地 secret 互不相同，并可选验证 DeepSeek Key；不在脚本中生成或打印任何 secret。

# 2026-08-26：当前完成范围由产品所有者收口

- 产品所有者明确：本轮不考虑 SSH 或实际服务器部署，只需验证代码开发正常；真实部署作为额外目标继续。
- 因此 Phase 5 的当前完成条件是所有已规划 Web 能力有实现并通过本地代码、构建、API 和浏览器验收。Docker/PostgreSQL/DeepSeek/备份恢复仍保留为独立部署目标，不能再写成当前代码完成的阻塞项。
- `docs/product-decisions.md` 尚无这次范围拆分，task plan 和 AGENTS 仍把服务器验收写成当前阶段工作，需要在最终验证后同步。
- 最终代码验收全部通过：严格类型检查、生产构建、OpenAPI、23 个文件 94 项服务端测试、Compose/Shell/diff、安全审计，以及桌面/手机 25 项 E2E。D-039 与阶段文档已经同步，当前 Web 代码目标有完整完成证据。
- 最终一致性搜索未在当前 README、AGENTS、task plan、交付方案或决策日志中发现“服务器验收仍阻塞当前完成”的旧表述；progress 中的两处 93 项/待服务器文字属于随后已被新记录取代的历史节点。
- `git diff --check` 再次通过；工作区有 123 条修改/新增状态，`HEAD` 与 `origin/main` 仍 0/0 对齐。代码验证正常，但尚未提交或推送这一事实保持明确。
# 2026-08-26：快速部署与问题报告增量

- 现有 `docs/deployment/self-hosting.md` 已经覆盖容器拓扑、secret、DeepSeek、持久化、备份恢复、更新和故障定位，适合作为完整运维手册；缺口是首次部署时可直接照做的短路径，而不是重写完整手册。
- 页面问题与服务器问题需要两种互补报告：设置页生成浏览器侧脱敏报告；部署脚本收集 Compose 状态、健康检查和近期容器日志。两者都只在用户主动操作时生成，不自动上传。
- 浏览器侧记录只保留有限数量的 API 结果和未处理错误，不记录请求体、响应体、Cookie、账号标识、照片或业务记录；API 路径去掉查询参数。复制功能必须兼容 IP + HTTP 下 Clipboard API 不可用的情况。
- 部署侧报告不得读取 `.env`、Compose secret、数据库表内容、临时照片或导出文件；容器日志仍可能包含 IP、请求路径或错误上下文，因此教程必须要求分享前人工检查。
- 最终实现把最近 60 条脱敏前端事件限制在当前浏览器标签页的 `sessionStorage`；接口只记录方法、去查询参数并隐藏资源 ID 的路径、状态、耗时、稳定错误码和 request ID，不记录请求体或响应体。
- IP + HTTP 可能无法使用现代 Clipboard API，因此复制功能带有传统本地复制回退；桌面与手机 Chromium 验收均实际读取剪贴板并核对内容。
- 部署报告默认收集每个服务最近 200 行日志，报告目录权限为 `700`、文件权限为 `600`；日志行数可通过 `DIAGNOSTIC_LOG_LINES` 调整。

# 2026-08-26：首次完整提交审查

- 当前改动是从 foundation commit 到完整 Web 版本的首次整体提交：52 个已跟踪文件修改、165 个新增文件；新增路径都属于源码、迁移、测试、部署或长期文档。最大新增文件为 164 KiB 的 Drizzle schema 快照，没有异常大文件。
- 候选集合没有图片、数据库、dump、备份、日志、压缩包、私钥或证书；`deployment/secrets/` 只有 `.example` 占位文件，真实 secret 仍被忽略。
- 高置信凭据规则（私钥头、GitHub token、AWS access key、长 `sk-` key、Bearer token）零命中；潜在赋值规则只命中环境读取、哈希字段、测试假密码、表单和 Compose secret 文件引用。
- 公开性扫描发现 3 处真实目标服务器 IP 只存在于内部规划历史，已统一替换为 `<server-ip>`；复扫后服务器 IP、本机路径和远程工作目录均零命中。
- 非测试源码没有 `eval`、动态 Function、child process、同步执行、TODO/FIXME/HACK 或遗留 `console.log/debug`。前端保留的 `console.error` 用于捕获页面操作异常，API 失败同时由脱敏诊断层记录。
- migration 从 `0000` 到 `0018` 连续，journal 尾部与新增 SQL 命名一致；下一步用机器检查 snapshot JSON、journal/SQL 一一对应和安全策略计数。
- 机器复核确认 19 份 SQL migration 与 19 条 journal 一一对应，全部 snapshot JSON 可解析；安全策略数组、migration 和烟雾检查由测试相互约束。安全说明中遗留的“31 张 RLS 表”已同步为当前 32 张。
- 部署审查确认所有公开示例 secret 本身都能满足原有长度门槛，忘记替换时存在用公开密码启动的风险。已新增无副作用 `preflight.sh`，在启动前检查主机工具、端口、文件归属与权限、Git 忽略、长度、占位值、重复 secret 和 Compose 配置；服务端同时拒绝任何含公开 `replace-with-` 占位值的必需或可选 secret。
- 最终候选集合为 52 个已跟踪修改和 166 个新增文件，共 218 个文件；本地 Markdown 链接、高置信凭据、个人主机/IP/路径、真实部署 secret、脚本语法/权限、Compose YAML 和 `git diff --check` 均通过。
- 最终全量验证通过：严格类型检查、23 个测试文件 95 项服务端测试、生产构建、OpenAPI 合约、桌面/手机 27 项 Playwright，以及生产依赖在线审计 0 个已知漏洞。本机没有 Docker/PostgreSQL，因此预检真实执行、migration/RLS/pg-boss 故障和备份恢复仍按既定边界留给服务器验收。

# 2026-08-26：首次真实服务器部署验收启动

- 产品所有者已在目标服务器配置项目级 Git 并 clone 仓库，明确授权联系远程 Codex 任务开始部署测试。
- 本地提交 `95f1bc3` 已推送到 `https://github.com/Syriii/exercise-app` 的 `main`；远端不能再因 GitHub 只有 foundation 代码而阻塞。
- 远程任务固定为先执行只读阶段 0：核对仓库和目标提交、主机规格、Docker/Compose/Git/curl、端口、相关容器和 volume；在返回结果前禁止创建 secret、容器、数据库或修改防火墙。
- 远程首次 `git fetch` 只因 Codex 沙箱把 `.git/FETCH_HEAD` 视为只读而等待批准，并非 Git 冲突、认证失败或 GitHub 故障。
- 修改审批规则并重新发起后，服务器成功 fast-forward 到完整 `95f1bc3`，`main` 与 `origin/main` 一致且工作区干净。
- 主机为 OpenCloudOS 9.2、x86_64 KVM、4 vCPU、3.6 GiB 内存和 1 GiB Swap；Swap 几乎用尽。根盘剩余约 13 GiB，`/newdata` 剩余约 76 GiB，因此 Docker 数据目录在安装前需要明确选择，优先评估 `/newdata`。
- Docker Engine、Compose、containerd、Moby 和 Podman 均未安装；端口 3000/5432 空闲，80/443 已由现有 Nginx 占用。服务器不是空白主机，后续不得覆盖现有 Nginx、宝塔或其他应用配置。
- 所有部署脚本语法与可执行位、Compose YAML、三个 package.json 和 `git diff --check` 通过；项目预检按预期停在缺少 `docker`。真实 `.env` 和 secret 尚未创建，阶段 0 没有修改容器、volume、数据库、防火墙或 Nginx。
- 存储审计确认主机有两块 KVM 虚拟数据盘：`/dev/vda` 60 GiB，单分区 `/dev/vda1` 以 XFS 挂载根目录；`/dev/vdb` 约 100 GiB，整盘 ext4 直接挂载 `/newdata`。另有未挂载的虚拟光驱 `sr0`；无 LVM、MD RAID、ZFS 或 Btrfs。
- `/newdata` 不是网络盘，剩余约 76 GiB、inode 使用约 4%，适合 Docker/containerd；推荐目录固定为 `/newdata/docker-data`，不放进 Git 仓库目录。
- `/etc/fstab` 当前用易受枚举顺序影响的 `/dev/vdb` 挂载 `/newdata`，没有 `nofail` 且 fsck pass 为 0。使用 Docker 前应改为文件系统 UUID，并通过 systemd `RequiresMountsFor` 或等价依赖保证数据盘未挂载时 Docker 不会退回根盘创建同名目录。
- 现有 1 GiB Swap 是根盘 `/www/swap` 文件且几乎用尽；新增 Docker/PostgreSQL 前需要持续观察内存压力。任何备份不能只与 Docker volume 同存 `/newdata`。
- 产品所有者明确目标服务器不属于自己，短期磁盘重挂载风险可以接受，并认为没有权限修改服务器基础配置。因此 `/dev/vdb` 挂载、UUID、`fstab`、systemd 和 Docker 数据目录只保留为管理员参考，不再作为 Agent 可自行执行的步骤。
- 即使当前登录账号技术上可能拥有较高权限，也不等于获得修改第三方服务器基础设施的授权。后续必须由服务器管理员提供 Docker/Compose、PostgreSQL 或明确的系统修改许可；否则只能进行仓库内和用户目录内的无副作用验证，不能把内存测试服务冒充长期部署。

# 2026-08-27：Docker Hub 替代镜像调研与服务器验证

- Docker 官方支持通过 Dockerfile 全局 `ARG` 参数化 `FROM`，Compose `build.args` 可在部署时传入该参数；这使开源默认值继续指向官方镜像，同时允许受限网络的部署者显式切换镜像来源。
- Docker daemon 的 `registry-mirrors` 会修改宿主机全局配置并需要 reload/restart；共享服务器上可能影响其他 Docker 项目，不作为本项目首次部署的首选。
- 阿里云官方文档说明传统 ACR 镜像加速已停止同步最新镜像，并建议生产环境把所需上游镜像同步到自有仓库；华为云 SWR 加速则限制在华为云用户和指定云产品/区域，不适合当前主机作为通用默认值。
- DaoCloud 公共代理声明支持 `m.daocloud.io/docker.io/...` 前缀并保持上游摘要语义，但存在缓存、白名单和限流边界，仍应固定明确版本并在目标主机实际验证。
- 目标服务器只读验证通过：`m.daocloud.io` DNS、直连 TCP 443、TLS 1.3 与 Registry challenge 正常；Node `24.19.0-bookworm-slim` 和 PostgreSQL `18.6-bookworm` manifest 均在约 2 秒内取得且包含 `linux/amd64`。该验证没有拉取镜像层，不能替代完整构建。
- 当前仓库只有两个外部基础镜像入口：Dockerfile 的 Node 标签和 Compose 的 PostgreSQL 标签。推荐分别用 `NODE_BASE_IMAGE` build arg 与 `POSTGRES_IMAGE` 环境变量参数化，默认保持原始 Docker Hub 短名。
- 服务器使用参数化后的 `m.daocloud.io/docker.io/library/node:24.19.0-bookworm-slim` 实际构建时，BuildKit 在 metadata HEAD 阶段收到 401；没有下载镜像层。DaoCloud 项目 issue #35515 记录了相同的 `m.daocloud.io/docker.io/library/...` BuildKit 401 形态，说明只读 imagetools 成功不足以证明 BuildKit 构建兼容。
- DaoCloud 官方 README 同时提供 `docker.m.daocloud.io/library/...` 的 Docker Hub 前缀替换地址，并明确说该方式由人工规则维护、优先级低于统一前缀。下一步只能先在目标机只读验证该固定 Node/PostgreSQL 地址；不能直接把一次 manifest 成功当作构建成功。
- 腾讯云直连 Node 基础镜像后，真正瓶颈转为镜像内部的 Debian 官方软件源：索引约 9.4 MB 下载耗时 5 分 42 秒，随后 83.2 MB 编译依赖在一小时左右仍未完成。构建没有进入 npm、应用编译或最终镜像导出。
- 远程 Codex 平台后端 403 会使附着于该任务的长时间构建客户端上下文被取消；Docker 日志记录 BuildKit `Solve` 为 `context canceled`。这不是服务器 OOM或原有服务故障，但说明不能让慢速上游把单次受控构建拖到平台会话极限。
- 中断后审计确认目标镜像不存在、0 容器、0 named volume、5011 空闲、关键服务 active、无新 OOM；约 332.5 MB BuildKit 缓存保留，`/var/lib/docker` 仍不存在。
- 腾讯云官方说明 `mirrors.cloud.tencent.com` 是公网与内网统一域名，腾讯云 VPC 默认 DNS 会优先解析到内网链路。最小方案是在 Dockerfile 与 Compose 增加项目级 `DEBIAN_MIRROR` build arg，默认仍为 `http://deb.debian.org/debian`；目标服务器只在私有 `.env` 覆盖，不修改宿主机 `/etc/apt` 或 Docker daemon。
- 本机普通 SSH 进程没有远程项目所用密钥，直接只读连接被公钥认证拒绝；后续服务器操作仍必须通过 Codex 远程项目。常规等待界面曾遗漏归档任务的工具结果，但读取完整任务记录后确认腾讯云内网 HTTP 源配置已经完成并通过静态验证，不能再把该步骤当作未知状态。
- `node:24.19.0-bookworm-slim` 构建阶段在切换到 `https://mirrors.cloud.tencent.com/debian` 后约 8 秒失败：基础镜像当前没有可用 CA 证书，APT 无法验证 HTTPS 证书，索引失败后无法定位 `build-essential` 与 `python3`。这属于 HTTPS 自举顺序问题，不是镜像站不可达或仓库缺包。
- 腾讯云官方文档给出的内网软件源地址是 `http://mirrors.tencentyun.com`，并说明腾讯云 VPC 可使用内网地址；腾讯云常用内部服务端口文档也登记该域名使用 80 端口。对当前基础镜像，项目级 HTTP 内网源可避免在安装 CA 之前依赖 CA。
- Debian `apt-secure` 默认验证仓库签名的 Release 信息，并拒绝未认证仓库；使用 HTTP 不等于关闭仓库认证。下一方案不得设置 `allow-insecure`、`trusted=yes` 或跳过签名验证，只替换传输地址并由 APT 继续执行 Debian archive 签名校验。
- 目标主机进程环境存在 loopback HTTP/HTTPS 与 SOCKS 代理，但 Docker 默认客户端 config.json 不存在，Docker CLI `proxies`、daemon 代理字段和 systemd unit 代理 Environment 均未配置。按 Docker 官方规则，当前 `docker compose build` 不会自动把该进程代理注入 RUN 阶段。
- `mirrors.tencentyun.com` 经 NSS/DNS 解析到 `169.254.0.3` link-local 地址。普通 curl 因目标域名不在进程 `NO_PROXY` 中而走 loopback 代理并收到 503；显式 `--noproxy '*'` 后三个 Bookworm InRelease 均直接返回 200，耗时约 0.017、0.013 与 0.080 秒。构建没有代理注入，因此 HTTP 内网源会走该直连路径。
- 归档任务完整执行记录证明服务器私有 `.env` 已从 HTTPS 统一域名精确切换到 `http://mirrors.tencentyun.com/debian`：文件 inode、root:root 0600、其他配置和反向校验均保持；preflight 与 Compose verification 静态展开通过。该结论来自实际修改和后验审计，不再只是网络可行性推断。
- 使用该 HTTP 内网源的真实 API 镜像构建已成功：APT 索引、83.2 MB 编译依赖、npm、应用编译和镜像导出均完成，生成约 110.8 MiB 的 `exercise-app:5516fc3`。这证明当前目标主机的项目级源配置可用，但不代表该内网域名适用于腾讯云之外的主机。
- 首次获批重试没有进入 build：硬门发现三个既有 unit 为 failed 后在 preflight 前停止。只读时间线证明这些状态分别形成于 2026-05 至 2026-06，早于 2026-08-27 的 Docker 安装，不能用 unit 的静态 failed 状态判断本项目是否影响服务器。
- 当前 Nginx 真实运行并监听 80/443，但不在旧 `nginx.service` cgroup；mysqld 没有进程或 3306 监听，属于历史 OOM 后的真实停止状态；IPMI 在 KVM 虚拟机启动时退出，是否需要它不属于本项目判断范围。后续门禁必须保留这些既有状态并比较增量，不能擅自 reset、启动或接管。
- 目标服务器最近两次已定位 OOM 都早于 Docker；Docker 启动后内核 OOM 数为 0。后续构建应以“新增 OOM/killed”作为停止条件，不能把 Docker 清理 cgroup 后读取 `oom_kill event` 文件失败误判为发生 OOM。
- 首次启动只读审查连续两次被远程 Codex 后端 `403` 中断；完整任务记录显示唯一完成的命令是读取规划技能说明，没有 manifest、Compose 或服务器状态查询结果。该失败属于远程执行通道，不是目标服务器或 Docker 错误，也没有造成部分 pull/up。
- 替代远程任务确认腾讯云 PostgreSQL `linux/amd64` 子 manifest 可通过内网直连读取，压缩层合计约 149.96 MiB；manifest 不携带可靠的解压尺寸。进程级代理会让该内网 registry 出现 TLS EOF，而直连正常；项目后续不得为此修改共享 Docker daemon 代理。
- Compose 5.4.0 的 `--no-build` 只禁止构建，不禁止拉取缺失镜像。完整 `up` 虽能按 healthy/completed 依赖排序，但 setup 失败后盲目重跑可能再次执行 migration；首次部署采用明确的一次 setup 和后续 `--no-deps` 启动，更容易审计和保留失败现场。
- `5011:3000` 当前未指定 host IP，因此 API 启动后会绑定全部宿主机接口；这符合已确认的 IP+端口访问目标，但启动批准必须明确其网络暴露影响。PostgreSQL 和 worker 没有宿主机端口。
- PostgreSQL 镜像实际拉取成功且只执行一次，约 150 MiB 镜像最终占用使 `/newdata` 减少约 492.6 MiB；说明 manifest 压缩层总量不能替代本地 content store 与解压/快照实际占用评估。拉取没有创建容器、volume 或网络，也没有触发进程代理 TLS 问题。
- PostgreSQL 单服务启动在约 6 秒内 healthy，数据库 volume 初始化约 48.5 MB，容器内存约 55 MiB；Compose secret 以额外只读 bind mount 出现在容器挂载中，后置审计必须把它作为预期安全挂载，不能只允许数据 volume。
- Compose 本地 file-backed secrets 在目标 Docker/Compose 中保留宿主机 root:root 0600 元数据，非 root 镜像用户不能直接读取；`uid/gid/mode` 对 file bind source 不能作为可移植修复。把宿主机 secret 改为 0644、让应用长期以 root 运行或把 secret 填入环境变量都会削弱当前边界。
- 当前修复采用短暂 root 入口 + tmpfs 副本 + gosu 降权：原始 bind secret 继续 0600，副本 root:node 0440 且只存在容器 `/tmp`；API/setup/worker 的实际 Node 进程仍为非 root，直接入口同时减少 npm PID 代理。真实验证必须检查副本权限、主进程 UID/GID、无 secret 回显和 setup 幂等边界。
- root UID 在容器 `cap_drop: ALL` 后不能完成 chown/setgroups/setgid/setuid；入口需要的最小启动能力是 CHOWN、SETGID、SETUID。它们只用于准备 tmpfs secret 并由 gosu 降权，实际 Node 进程必须通过 `/proc/self/status` 验证 `CapEff=0`，同时保持 `no-new-privileges`，不能为方便而恢复默认 capability 集合。
- 服务器隔离探针验证了完整运行边界：root:root 0600 原始 mount 对 node 不可读，tmpfs 副本 root:node 0440 可读，实际进程 UID/GID 1000、CapEff=0、NoNewPrivs=1；无网络、数据库、volume 或端口副作用。该证据闭合了静态镜像检查无法证明的实际降权行为。
- API 首次单服务启动没有触发容器重启或应用退出，而是镜像内置 healthcheck 连续返回 Docker 执行码 `-1`，最终使 Compose `--wait` 超时。该状态优先指向健康检查进程无法创建或执行，不能在读取 healthcheck 状态输出、容器进程身份和必要的限量日志前推断 API 业务启动失败，也不能通过增加等待时间掩盖。
- 后续只读诊断修正了上一条的“没有应用退出”判断：API 的 PID 1 确实曾以退出码 1 结束并由 `unless-stopped` 自动拉起一次；Docker 不会仅因 unhealthy 自动重启容器。重启后的 PID 1 是预期 Node API，UID/GID 1000、CapEff=0、NoNewPrivs=1，容器内重新出现 3000 监听。
- healthcheck 每次恰好运行到 5 秒 timeout，而镜像命令的成功分支只检查 `r.ok`、没有显式 `process.exit(0)`。Node 内置 fetch 的连接池可继续保持事件循环，因而“HTTP 已成功但探针进程不退出”是当前最强候选；应先用可单测的探针函数或显式成功退出修复，不应放宽健康超时。主进程退出码 1 仍需单独用生命周期和应用代码证据解释。
- 最终只读证据确认 readiness 的 31 个请求都有进入日志但没有完成日志，容器内 3000 和宿主机 5011 映射正常；`pg.Pool.query` 源码明确通过 `this.connect(callback)` 取连接，而项目包装忽略 callback。每 30 秒泄漏 1 个连接、池上限 10、连接等待超时 5 秒，与约 5 分 36 秒后的退出码 1、无业务异常日志和自动拉起完整吻合，因此连接池契约缺失为高置信根因；显式 healthcheck 退出仍是必须单独修复的次要缺陷。
- 远程任务变为 `notLoaded` 后，连续两个构建指令与一个无命令状态探测都产生 completed 但 items 为空的回合；这证明问题位于任务执行通道，而不是 Docker/build。服务器在最后一个有证据的回合仍为 `adf00b0`、preflight 通过、API stopped、5011 空闲、新镜像不存在；不得把空回合当成构建尝试或构建失败。
- 后续镜像创建时间、Docker缓存和磁盘增量证明第一个空回合实际完成了 `adf00b0` 构建，只是任务记录丢失；因此“空回合没有服务器变化”这一推断被更强运行时证据修正。门禁因镜像已存在而停止重复构建，随后通过隔离行为探针确认镜像真实性。
- Docker Compose 5.4.0 的 `compose port` 可对未发布端口输出非空占位 `invalid IP:0`，不能再用“输出非空”判断私有服务是否发布。运行态应查询具体容器的 `docker container port <id> <port>/tcp`，并以 PortBindings/宿主机监听作为后验；当前 PostgreSQL 与 Worker 实际均未发布端口。
- 镜像保持 `USER root` 仅用于入口复制 secret 后由 gosu 降权；`docker compose exec` 仍默认按镜像用户启动，而不是复用 PID1 的 UID。cap_drop ALL 后 root 没有任意写入能力，因此 smoke 的媒体探针必须显式 `--user 1000:1000` 模拟实际 API，而不是把 0755 volume 改成全员可写或给 exec 恢复 capability。

# 2026-08-28：首次真实页面视觉基线

- 已部署应用会从 `/` 正常重定向到 `/login?redirect=/today`，DOM 完整、控制台无 warning/error、根元素无横向溢出；不是空白页问题。
- 当前内置浏览器实际内容宽度约 623px。登录卡片约占页面宽度四成并靠左上放置，标题、说明、字段标签、输入内容和底部注册链接整体明显偏小，远低于日常手机/桌面舒适阅读尺度；用户对字号失衡的反馈可复现。
- 登录页没有发生可见重叠，但它暴露了全局视觉尺度问题：品牌行和说明文字过小、卡片最大宽度偏窄、大片空白与表单密度不匹配。后续应从全局 token、容器宽度和正文/标签字号基线修复，而不是逐个页面随意放大。
- 全新 4175 内存测试服务使用假管理员成功进入 `/today`；桌面 DOM 无控制台错误和横向溢出，内容区 992px、侧栏 288px，主要卡片本身没有相交。
- 首页字号层级存在可量化断层：H1 70.4px、H2 31.25px、正文/按钮 16px，但日期、数据说明和侧栏说明只有 12.8px，状态 chip 只有 11.52px。至少五级尺度同时高频出现，使标题过强而关键解释和状态难读。
- 全页截图出现固定侧栏与页面长画布的拼接重复，这是浏览器 full-page capture 对固定元素的表现，不直接算产品重叠；后续重叠判断必须使用元素 bounding box 与普通视口截图，不能只凭拼接截图。
- 训练页桌面空状态没有横向溢出或卡片相交；可见面板依次位于不同纵向区间。真正的层级问题是超大的两行 H1 占据首屏约一半高度，而日期/标签和选项副说明仍为 12.8px，主次比例过激。
- 训练页在桌面把“新建方案”拉成约 320px 宽的按钮，但同层标题/正文左侧占更大空间；这是可用但视觉重量失衡。方案类型 tab、副说明、空状态和建议卡之间留白较多，导致内容显得散而不是重叠。

# 2026-08-28：并行审计的关键缺陷

- 数据库智能体用失败测试锁定真实根因：身份服务在内部 `authenticate()` Promise 中调用 `AsyncLocalStorage.enterWith(userId)`，不会把上下文反向传播到外层已经注册的 `await userId(request)` continuation；因此读取被 RLS 隐藏为空，直到 `daily_planning_references` INSERT 以 null 用户上下文被 RLS 拒绝。
- 当前 PostgreSQL 集成测试使用 migration/owner 连接，Playwright 使用内存仓储，均没有覆盖 `exercise_api + RLS + Fastify Cookie + AsyncLocalStorage` 的生产组合；这就是本地 99 项测试仍漏过真实错误的直接原因。
- 独立 QA 在 320/375/414/768/1440 视口复核五个主页面：普通同级内容块没有横向溢出或彼此穿插，但固定 `.mobile-dock` 会覆盖正文卡片；设置页 320px 视口下甚至盖住出生日期输入，用户的“两框重叠”反馈可稳定复现。
- 移动端需要把页面改为 header / 独立滚动 main / dock 三行布局，使 dock 占据自己的布局空间；只增加 main 底部 padding 仍会让滚动中的内容显示在 dock 后面，不能消除视觉重叠。
- 全局 `--text-xs: 0.72rem` 与 `--text-sm: 0.8rem` 让大量真实说明、日期、导航标签和状态落在 11.52–12.8px；Nutrition 日期输入约 23.84px，Settings 时间/时区/间隔输入约 30.8px，未达到 44px 触控目标。
- CSS 还存在孤立闭合括号、未定义 `--color-danger` 加内联 hex fallback、过多 card-in-card 与 side-stripe；当前“slop 1–58 全过”stamp 与代码不符，应修复后重新审计，不能保留虚假声明。
- 修复验收必须增加几何门：每个页面在 320/375/414/768/960/1440 检查横向溢出、同级语义块相交、固定/粘性导航覆盖控件、正文小于 14px、主要正文小于 16px以及交互目标小于 44×44；同时收集 console error、pageerror、requestfailed 与非白名单 HTTP 4xx/5xx。
- 主 Agent 在 320×900 的 Nutrition 实际截图再次复现：`.mobile-dock` 位于 y=818–884，而 `.recommendation-panel` 从 y=386 延伸到 1082，导航框直接压在“系统参考”卡片中部；这正是用户看到的两个框重叠，不是 full-page 截图伪影。
- Nutrition 日期 input 只有 23.86px 高、288px 宽；按钮已有44px，但原生 date、设置页时间/数字/select仍需统一最小高度。移动截图还显示 H2 因窄列强制把“系统参考”拆成两行大字，旁边 CTA 与标题挤在同一行，信息头部需在窄屏改成单列。
- `daily_planning_references.createReference()` 已使用按账号和日期的 PostgreSQL advisory transaction lock，因此用户错误中的 revision=1 INSERT 更符合 RLS 上下文缺失，而不是唯一键竞争；仍安排补充并发幂等核查，避免相同输入生成不必要的第二个修订。
- 客户端新增 5xx 二次脱敏：即使异常服务端返回了内部 message，浏览器也只展示统一提示并保留 requestId，不显示 SQL、参数或账号标识。Today 与 Nutrition 的首屏聚合读取改为分区接收结果，单个接口失败不再把训练、餐食等已成功内容一起清空。
- 五个业务页重复的 rail/header/dock 已抽为 `AppShell.vue`；移动布局改为“固定头部 + 独立正文滚动区 + 正常布局底栏”，从结构上消除底栏覆盖正文。
- 远程只读日志任务因 Codex 服务请求被 403 中断，未执行服务器命令、未产生服务器改动；最终仍需在新代码更新服务器后验证受限数据库角色与首次登录路径。
- 同日 planning 仓储原有 advisory lock 已阻止两个请求同时写 revision 1，但完全相同的连续请求仍可能产生无意义的 revision 2；在锁内比较方法版本、证据清单与输入快照后直接返回最新记录，可同时保持真正输入变化时的新修订语义。
- 最终几何回归证明移动底栏结构修复有效：320px 下 `.app-main` 底部为 818px、`.mobile-dock` 顶部为 826px，两者不相交；设置页辅助文字最小 14px，饮食页可见主面板相交数为 0。
- 全站业务页现在共享同一个导航 Shell，活动项由 route meta 派生并设置 `aria-current`；长用户名在移动头部省略显示，同时保留完整 `title` 与可访问名称，避免再次出现页面间结构漂移。
- 外部 Google Font 会让中国网络、自托管和离线环境出现字体回退与视觉跳变；当前改为系统已有的苹方、Noto Sans CJK、微软雅黑及本地窄体标题回退，不再依赖运行时字体网络请求。
- 模块级 `Promise.allSettled` 不能只“保留成功结果”：当查询作用域从日期 A 切到 B 时，失败模块必须先清空 A 的旧状态，并用 request date / generation 拒绝迟到响应，否则会在 B 标题下展示甚至允许操作 A 的数据。
- 写操作也需要同样的作用域保护。饮食覆盖确认、图片轮询和汇总刷新均捕获发起时日期；成功、失败和提示只有仍属于该日期时才回写。定向 E2E 通过延迟 A 请求、强制切换 B、再释放 A 响应验证 B 不受污染。
# 2026-08-29：共享 PostgreSQL 上的集成验证隔离缺口

- `${POSTGRES_DB}_test` 只能隔离数据库对象，不能隔离 PostgreSQL 集群角色；`exercise_api` 是实例级角色。
- 原集成测试调用 `ensureApiDatabaseRole(..., "integration-only-api-role-password")`，会在共享生产 PostgreSQL 容器中改变生产 API 角色密码。实测测试后 API ready=503，标准幂等 setup 恢复生产密码后连续恢复为 200。
- 服务器 verification 容器必须只读挂载现有 `api_database_password` 并通过 `TEST_API_DATABASE_PASSWORD_FILE` 使用相同凭据；测试初始化发现 `exercise_api` 已存在时必须保留其密码，只通过实际受限连接验证，只有全新测试实例没有该角色时才创建。不得在共享实例上对该角色执行 `ALTER ROLE`，即使输入密码看起来相同也不行，因为 PostgreSQL 会重写 SCRAM verifier。
- `verify-database.sh` 原先的 `compose up -d postgres` 在配置漂移时理论上可能重建生产 PostgreSQL，不能作为共享服务器的安全验证入口。修正后它要求 Postgres 已运行，否则失败；只允许 exec 创建/删除 `_test` 数据库，不负责生产容器生命周期。
- integration 临时容器的 `compose run` 也必须使用 `--no-deps`；否则 Compose 仍可能根据 `depends_on` 尝试启动 PostgreSQL。加上该参数后，构建期间若数据库停止，测试只能失败，不能自行改变生产容器生命周期。
- 集成文件会创建超过 10 个账号，不能用产品默认账号上限验证互不相关的数据路径；测试构造的 `IdentityService` 使用测试专用上限 100，生产默认值不变。
- pg-boss 强杀恢复测试原 `expire=30s`、总超时 40s，在低资源服务器上几乎没有调度余量。pg-boss 12.27.0 要求 `heartbeatSeconds >= 10`，且 heartbeat/timeout 扫描还受默认 60 秒 `monitorIntervalSeconds` 节流；只缩短 expire 会让普通过期冒充心跳恢复。修正后测试实例使用 monitor/supervise=1 秒、heartbeat=10 秒、expire=120 秒、总超时 60 秒，并断言子进程确由 SIGKILL 结束；因此测试窗口内只能通过 heartbeat 回收，不能靠普通 expire。
- 2026-08-30 线上真实使用发现：用户认为在保存体重时收到统一 5xx。服务器脱敏日志确认实际失败请求是 `PUT /api/v1/planning/profile`，违反 `personal_profiles_revision_positive_ck`：路由把包含并发控制字段 `revision: 0` 的整个 request body 作为领域输入传下去，PostgreSQL 首次 insert 的对象展开又用该值覆盖了数据库默认 `revision: 1`。身体测量 POST 本身不是这条日志的失败路径。相同运行时字段泄漏也可能影响首次目标策略、饮食提醒和身体测量提醒保存；修复同时在路由剥离传输字段，并在服务层使用显式字段白名单。
# 2026-08-30：第三轮真实使用体验审计

- `NutritionService.getDaySummary()` 在当天没有餐食时把 `recorded` 设为 `null`，因此即使每日参考已经 ready，`remaining` 也被连带设为 `null`。正确边界是：零餐食时已记录为 0、剩余等于完整参考、完整性仍为 false；已有餐食但缺少营养值时继续保持未知。
- 设置页当前把个人档案、身体测量、目标策略、三类提醒、问题报告、导出和账号删除全部展开在一条长页面中。首次使用与日后修改没有区分，导致每个区块都需要自己的保存按钮，也无法快速进入单一修改任务。
- `trainingIntent` 当前只在目标策略中清洗、保存并写入每日参考输入快照；规划计算器、营养分配和训练建议算法都不读取该字段。继续让用户填写会错误暗示它能改变计划。
- 动作指导 API、许可字段和四项原创草案已经存在，但训练页只在进行中的计划动作上提供入口；单次方案、周期训练日和系统建议的动作列表都没有入口。目录只做精确名称匹配，带“如……”或“……类”的建议动作无法命中已有指导。
- Hallmark 结构审查未发现需要推翻现有设计系统的 critical 模板问题；主要问题是设置区块全部同层、同间距展开，任务层级不清，以及训练方案卡只显示名称和组数，缺少动作层面的交互入口。

# 2026-08-30：动作示范视频来源初筛

- Wikimedia Commons 的“Videos of physical exercises”“Videos of fitness”和 CDC physical activity 分类包含可直接浏览的 WebM/OGV 视频，其中能看到深蹲/弓步、过顶推举、胸推、划船机、腿举、腿屈伸等单动作素材。Commons 允许站外复用，但官方复用说明要求逐个文件读取作者、许可证、署名、许可证链接和修改标记，且提醒 Commons 不担保上传者的版权判断。
- Commons 的项目范围要求媒体允许任何目的使用，包含商业使用和改编；因此它比只允许研究或非商业的数据集更符合本项目未来公开开源与可能被第三方使用的目标。实际接入仍需保存每个文件的固定页面、作者、许可证、来源修订和审查状态。
- `onezlabs/jo-workout` 虽有 433 个动画视频，但仓库只允许教育和非商业用途，并声明图片、视频归各自权利人所有；不能作为本项目可再分发的媒体源。
- 小黑盒文章《1324 个健身动作带 GIF 全免费！做健身 App 直接抄》链接的原始仓库是 `hasaneyldrm/exercises-dataset`。仓库虽有 1,324 个 GIF，但 LICENSE 明确设置 media exception：MIT 只覆盖代码、工具、数据结构和说明文字，不覆盖 `images/`、`videos/`；克隆仓库不会取得 Gym visual 媒体许可。NOTICE 再次说明原作者只有单独书面分发许可，复用者需按 Gym visual 条款自行取得许可。
- Gym visual 标准 N-CRFL 许可允许购买后的非水印媒体用于 Android/iOS 应用和网页，但禁止转售、再分发、向第三方转让权利或让媒体可下载。开放源码仓库会把文件分发给不特定第三方，因此不能把该仓库的 1,324 个 GIF 直接提交到本项目；若希望使用，必须取得覆盖本项目开源再分发方式的单独书面许可，普通购买许可也不能想当然视为足够。
- 该数据集的非媒体部分仍有价值：动作名称、分类、器械、目标肌群、多语言说明和 JSON Schema 按 MIT 可作为动作目录候选输入。但仍需去重、中文名称校正、技术内容审查和与本项目动作标识映射；不能用其 GIF 授权问题否定元数据价值，也不能因元数据是 MIT 就连带使用媒体。
- wger 的应用代码是 AGPL，练习数据按条目授权；其公开 issue 对视频明确指出需要逐上传审核许可。wger 可参考数据模型和许可证管理方式，但目前不是一个可直接批量取得动作视频的统一开源媒体包。
- CDC 在 Commons 的力量训练系列有 16 个左右的短视频，已逐项抽查腿举、划船机、家庭肩推、器械胸推和俯卧撑。文件页均列作者为 Centers for Disease Control and Prevention，并标记为美国联邦政府雇员公务作品、美国公共领域；文件大小约 2.9–6.3 MB，分辨率 320×240，时长约 29–71 秒。授权风险低、动作说明清楚，但画质和动作覆盖有限。
- `Forward lunge training.webm` 是作者自行上传的 853×480、2.21 MB、68 秒视频，明确采用 CC BY-SA 4.0。可以共享和改编，但必须署名、链接许可证、标注修改；如果剪辑或制作衍生视频，衍生媒体需要使用相同或兼容许可证。它适合补足弓步，但许可处理比公共领域 CDC 视频更复杂。
- Wikimedia 官方复用指南不建议直接 hotlink Commons 原始文件。产品更稳妥的方式是：对审核通过的小批文件自托管优化版本，同时保留原文件页、作者、许可证、是否修改、固定修订和校验值；页面展示来源/许可，仓库增加媒体登记表。公共领域仍建议标注 CDC 与来源，以保证可追溯性。
- wger 官方 changelog 记录其曾由贡献者 Goulart 提供约 150 个动作视频，并以 CC BY-SA 发布；这使 wger 成为覆盖量更高的候选。但项目 README 明确说明练习数据按单条许可，模型也分别保存 video 的 license/author/source 字段，因此不能仅凭 changelog 批量导入，必须从 API 或导出逐条筛选明确的 CC BY-SA 视频。
- 直接用网页读取工具访问 wger JSON API 被安全 URL 校验拒绝，尚未取得实际视频条目与许可字段。这是研究工具限制，不是 wger API 不可用；后续改用官方仓库模型/fixture或只读 HTTP 客户端核查，不重复同一网页打开方式。
- 改用只读 HTTP 请求后，wger 官方 `/api/v2/video/` 当前返回 78 个公开视频。抽查条目字段为 `license=2`、`license_author=Goulart`；官方 `/api/v2/license/2/` 明确解析为 Creative Commons Attribution Share Alike 4（CC BY-SA 4.0）。这条证据来自实时官方 API，比只引用历史 changelog 更可靠。
- wger 抽查视频是 1920×1080、10–18 秒的 HEVC/H.265 `.MOV`，单条约 34–52 MB；动作与 exerciseinfo 正常关联，例如 exercise 512 是 `Rowing seated, narrow grip`。视觉质量和动作库适配优于 CDC，但 HEVC MOV 的网页兼容性和体积不适合直接交付，需要转成 H.264 MP4 或 VP9/WebM 并生成 poster。转码是改编，应把衍生媒体继续按 CC BY-SA 4.0 发布，注明 Goulart、wger 条目和转换说明。
- Creative Commons 官方 FAQ 明确：CC 素材可以作为独立艺术元素与软件一起提供；合集本身可采用自己的许可，原视频仍保持各自许可。因而 MIT 代码与 CC BY-SA 4.0 视频可以按“代码与媒体分别授权”的方式共存，但被裁剪、转码或加字幕后的衍生视频仍须遵守 BY-SA、署名、许可链接和变更说明，不能把媒体一并声明为 MIT。
- CDC 官方重用政策确认大部分 CDC/ATSDR 内容属于公共领域，可自由使用和复制，但仍要求标明 CDC 来源、展示非背书声明，并逐项排除承包商、受资助方或第三方素材。Wikimedia 文件页的公共领域标记不能替代这一逐文件检查。
- wger 实时 API 的 78 个视频覆盖 46 个不同动作，全部标记为 Goulart 制作、license 2。覆盖动作包括卧推、哑铃卧推、箭步蹲、腿举、前蹲、髋推、坐姿划船、哑铃/器械肩推、罗马尼亚硬拉、引体向上等，能覆盖现有动作目录的大部分器械与自由重量动作。
- wger 原文件不宜直接打包：78 个视频合计约 3.57 GB，54 个为 HEVC，71 个为 1080p、5 个为 4K；浏览器兼容性和部署体积都要求先精选、审阅并转为 H.264 MP4（必要时同时提供 WebM）和封面图。
- 许可置信度高不等于动作教学质量高。来源视频在进入内置动作库前仍需逐条做动作对应、完整动作阶段、镜头可读性、安全提示与技术准确性审查；未经审查时只能标记为“动作参考”，不能替代文字要点或安全边界。
- 对当前 10 个文字指导动作的覆盖判断：wger 可直接候选卧推、硬拉、划船、肩推、弓步、臀推和引体类；CDC 可补器械胸推、腿举、肩推、俯卧撑与半蹲。平板支撑、标准徒手深蹲、臀桥和高位下拉仍需从 Commons 逐项补充或自行拍摄，不能因为名称相近就把腿举当深蹲、髋推当臀桥。
- Commons 搜到的高位下拉错误示例虽然是 CC BY 3.0，但文件仍处于“许可待管理员复核”，并带推广文字，不作为首批内置来源；公共领域的海军平板挑战只是活动记录，不是标准教学镜头，也不采用。搜索命中不等于适合产品。
- 建议首批只接入 8–12 个审核通过的单动作视频：wger 为主要高清候选，CDC 公共领域视频作低风险补充；缺口宁可继续显示现有文字指导，也不接入授权含糊或动作不对应的素材。长期最稳妥的完整方案仍是自行拍摄并用统一开放许可发布。
- 对 wger 78 条实时视频元数据的完整统计：全部 78 条都是 license 2 / CC BY-SA 4.0，作者全部为 Goulart；覆盖 46 个不同 exercise。54 条 HEVC、24 条 H.264；71 条 1080p、5 条 4K、2 条 640×352；时长 6.37–34.69 秒；原始媒体合计约 3.57 GB，最大单条约 423 MB。不能原样打进 Web 仓库或 Docker 镜像，应只选产品已支持的少量动作，转成移动端短循环并独立存储。
