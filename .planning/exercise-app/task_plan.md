# Exercise App 简明规划 v3

> 2026-08-26 测试记录：饮食计划首轮类型检查只发现新增的 `diet_plan_not_found` 尚未登记到营养错误码；同时人工复核发现归档路由的 response schema 被误放进 body schema。两处均为新增接口契约问题，已在继续生成迁移前修正。

> 2026-08-26 编辑记录：历史趋势的组合补丁因 CSS 中不存在预期的 `@media (max-width: 640px)` 精确锚点而整体未应用；拆成 Vue 逻辑、模板、基础 CSS 和实际媒体查询四个小补丁，避免部分落盘。

> 2026-08-26 测试记录：图片暂定采用首轮定向测试的两处失败均来自旧断言：一处仍期待成功后零贡献，另一处仍提交分析前的餐食 revision。新规则会自动增加一条暂定贡献并递增餐食 revision；测试将先读取当前餐食，再确认同一贡献由 tentative 修订为 confirmed。

> 2026-08-26 浏览器验收记录：训练消耗下拉框在可访问性快照中有正确名称，但当前浏览器实现的 `getByLabel` 没有匹配到该原生 `select`；改用唯一 `combobox` 角色继续验证，并将是否需要显式 `for/id` 作为界面检查项。

> 2026-08-26 浏览器验收记录：当前内置浏览器的 `waitForLoadState` 不支持 `networkidle`，页面导航本身已执行；改用受支持的 `domcontentloaded` 并读取新 DOM，不重复创建页面或账号。

> 2026-08-26 浏览器验收记录：沙箱禁止 E2E 服务绑定 `127.0.0.1:4174`（`listen EPERM`）；构建本身已成功。将仅为启动项目自带的本地验收服务申请受控的沙箱外运行权限。

> 2026-08-26 浏览器验收记录：本轮开始时 `127.0.0.1:5173` 没有正在运行的开发服务器；这不是应用故障。将使用项目自带的内存 E2E 服务启动最新构建进行页面验收，避免要求本机 PostgreSQL。

> 2026-08-26 测试记录：训练消耗定向测试首次失败是测试把同一毫秒内生成的两条场次修订假定为严格倒序；仓储按时间排序时二者时间相同，数据内容和修订号正确。断言改为不依赖同毫秒排列、同时验证各修订内容。

> 2026-08-26 测试记录：训练消耗第二轮类型检查只剩 Drizzle JSONB 写入类型不接受只读领域接口；数据库列保持通用 JSON 对象边界，在仓储写入处做显式序列化类型转换，避免 schema 反向依赖业务模块。

> 2026-08-26 测试记录：训练消耗首轮类型检查发现两个仓储漏导入新类型、严格数组解构无法证明日期段存在、新错误码未登记，以及布尔条件不能让 TypeScript 收窄可空时长；这些都是新增切片的编译期问题，正在原位修正，既有功能未受运行时影响。

> 2026-08-26 工具记录：首次向 findings 追加训练建议审计时误用了不存在的英文标题锚点，补丁未生效；读取真实标题后已改用中文标题锚点完成更新，未影响业务文件。

> 2026-08-26 工具记录：收口搜索误写了不存在的 `apps/server/test` 路径，且查询范围过宽导致输出截断；已改为只检查实际的 `apps/server/src`、迁移与当前文档，不影响代码。

> 2026-08-26 工具记录：按模块名猜测的 `postgres-exporter.test.ts` 不存在；导出器测试实际位置将通过 `rg --files` 定位，不再猜测路径。

> 2026-08-26 测试记录：训练建议并发采纳测试预期“一次成功、一次 409”，但内存仓储的两个请求都成功返回同一个幂等方案；真正需要保证的是不复制方案，而不是强制其中一个 HTTP 失败。测试将改为验证所有成功响应引用同一方案且仓储只有一份方案。

> 2026-08-26 测试记录：修正断言后确认内存仓储仍存在真实并发竞态——两个请求都在来源映射写入前通过检查，生成了两份方案；将增加按账号与建议键共享的进行中 Promise，使测试仓储与 PostgreSQL 唯一约束保持相同幂等语义。

> 2026-08-26 工具记录：本机 Ruby 2.6 的 `YAML.load_file` 不支持 `aliases:` 关键字，导致 Compose YAML 检查命令自身报错；将使用该版本兼容的 `YAML.load_file(path)` 重新解析，测试、构建、Shell 语法检查和 `git diff --check` 未因此失败。

> 2026-08-26 文档记录：阶段状态批量补丁因“继续后移”段的实际内容与预期上下文不完全一致而整体未应用；将读取该小段后拆分更新，未影响业务代码或既有文档。

## 目标

先做一个真正适合个人日常使用的健身辅助 Web 应用；确认实用后开放给朋友，逐步完善为可开源、可构建并可安装为 Android APK 的项目。

## 已确认的项目基线

- 产品包含训练和饮食两个独立模块，两者共享必要数据。
- 训练方向包括计划、今日安排、动作指导、训练记录、消耗估算和提醒。
- 饮食方向包括每日营养目标、拍照估餐、摄入累计和饮食提醒。
- 单日实际训练不改变当天营养参考；“还能吃多少”和“还需练多少”并列展示、独立更新。
- 食堂餐难以估算，是当前最重要的现实场景。
- 所有营养、热量和 AI 识别结果都属于估算，需要表达不确定性。
- 产品尊重用户自主性，不强制打卡或规定使用频率。
- 先做 Web，满意后提供 Android APK；先个人使用，再开放朋友和开源。

这些内容是重新规划的起点，不再通过重复提问重新确认。下一步先检查完整产品范围，而不是立即删减功能或设定使用指标。

## 工作方式

每个阶段只遵循四步：

1. Agent 先整理已经知道的内容。
2. Agent 给出完整方案或结构，不用连续小问题让用户从零设计。
3. 用户整体纠正、补充和选择。
4. 主题确认后再更新当前产品文档。

只有答案会改变当前产出，而且 Agent 无法采用安全、可逆的默认方案时，才向用户提问。验证用于检查产品是否有帮助，不用于考核或约束用户。

## 路线

| 阶段 | 目的 | 主要产出 | 进入下一阶段的条件 |
|---|---|---|---|
| 0. 规划重置 | 清除过早结论，建立单一讨论路线 | 本简明规划、待重建的产品基线 | **已完成：产品所有者认可讨论顺序** |
| 1. 产品全貌 | 完整整理产品要覆盖什么，不讨论实现先后 | 一页产品蓝图、训练/饮食/联动/通用能力地图 | **已完成：产品所有者于 2026-08-21 整体确认产品全貌** |
| 2. 领域规则 | 定义计算、记录、估算、修正、内容来源和安全边界 | 规则说明、数据词汇、典型与异常场景 | **已完成：产品所有者于 2026-08-23 整体确认领域规则，官方证据登记与异常路径完整** |
| 3. 体验设计 | 设计真实使用过程，而不是先列页面 | 核心流程、信息架构、移动端低保真原型 | **已完成：产品所有者于 2026-08-24 确认当前体验版本可作为基线** |
| 4. 技术与版本规划 | 根据完整需求和原型选择技术，并安排实现顺序 | 技术方案、首个可用版本、后续增量 | **已完成：产品所有者于 2026-08-25 确认完整架构与交付顺序** |
| 5. Web 实现 | 按确认的增量开发和测试 | 可构建、可运行并经过浏览器验收的 Web 应用 | **已完成：全部规划内 Web 代码与本地自动化验收通过** |
| 6. 使用与扩展 | 个人自由使用并调整，再开放朋友、开源和 Android | 稳定 Web 版、朋友版、开源版本、Android APK | 产品所有者认可实用性，其他人可独立使用和构建 |

## 当前阶段

- **阶段：** 6 — 使用与扩展
- **状态：** 2026-08-31：公开包装食品搜索提交 `96d195e` 已推送并完成受限服务器发布；公网资源包含目标版本，live/ready 通过。
- **当前产出：** 个人常用食物、最近餐食按项复用与 Open Food Facts 公开包装食品搜索可以并列使用；公开结果按实际克数换算、保留来源快照，上游失败不影响个人记录和手工录入。
- **下一步：** 由产品所有者在真实早餐/包装食品记录中检查搜索命中与份量换算是否顺手；继续从已确认未实现能力中安排下一批功能。HTTPS 等待域名申请完成后再处理，不阻塞功能推进。

## Phase 6.1 线上安全与可靠性收口

| 阶段 | 完成条件 | 状态 |
|---|---|---|
| 1. 代码与生产同步 | `origin/main` 的当前应用版本已完成受限发布，公网资源和 live/ready 可独立核对 | **已完成：`0789d24`，公网 JS 包含目标版本，live=`ok`、ready=`ready`** |
| 2. 传输安全 | 公共互联网使用受信 HTTPS 且安全 Cookie 开启，或明确只允许受信 VPN/私网访问 | **已确认后移：风险审计完成；产品所有者正在申请域名，域名就绪后再配置 HTTPS，本阶段不处理且不阻塞功能优化** |
| 3. 真实图片分析 | 真实 DeepSeek Key 下验证图片输入、结构化结果、用量、超时、限流、错误分类和原图删除 | **待专项验收：模拟供应商与本地生命周期已通过** |
| 4. 数据与任务恢复 | 验证容器重建持久化、隔离备份恢复、异机副本、数据库断线与任务恢复 | **部分通过：真实测试库 SIGKILL 恢复已通过；其余路径继续待验收** |
| 5. 实际使用反馈 | 产品所有者按自己的频率使用核心流程，问题进入可复现、可回归、可发布的收口循环 | **进行中：不设打卡或使用次数门槛** |

## Phase 6.2 功能优化

| 阶段 | 完成条件 | 状态 |
|---|---|---|
| 1. 真实流程审计 | 对照线上版本与当前代码复核“今天、训练、饮食、历史、设置”的主要任务、空白/失败/返回路径和移动端操作成本 | **已完成：确认训练与首餐入口过深，历史测量口径矛盾；设置框架无需重做** |
| 2. 优化方案定稿 | 按用户价值、问题证据、实现影响和回归范围整理完整批次，不重复已关闭问题 | **已确认：产品所有者要求实现第一批，并继续推进其他未实现能力** |
| 3. 功能实现 | 按确认批次完成前后端、状态、错误与必要迁移 | **已完成：训练主动作、快速记餐、今天页意图和历史测量；无 migration** |
| 4. 自动化与目视验收 | 类型、服务端测试、构建、API 合约、桌面/手机 E2E 与多尺寸目视通过 | **已完成：check/build/API 合约、108 项服务端测试、41/41 E2E、390×844 与 1280×800 目视通过** |
| 5. 发布与真实复核 | 自动提交推送、受限部署、公网版本/健康验证，并由产品所有者使用真实账号复核 | **自动发布已完成：`00a4725`，公网 JS `index-2HyeEwvO.js` 包含目标版本，live/ready 通过；等待产品所有者按需使用真实账号复核** |
| 6. 未实现能力审计 | 对照产品、领域、体验、交付文档与真实代码，区分未实现、仅本地通过、受外部依赖阻塞和已明确后移 | **已完成并经产品所有者纠正：真实缺口是单项常用食物管理与最近餐食部分复用，不是整份常用餐模板；公共食物库、后台通知、DeepSeek 真 Key、Android 分别受既有门槛约束** |

## Phase 6.3 餐食单项复用优化

| 阶段 | 完成条件 | 状态 |
|---|---|---|
| 1. 产品意图纠正 | 明确单项常用食物、最近餐食部分复用和当天差异补充的真实流程 | **已完成：产品所有者以“昨天包子/鸡蛋/豆浆，今天夹馍/鸡蛋/豆浆”为例整体纠正** |
| 2. 现有实现审计 | 核对个人常用项、最近吃过、餐食贡献快照、更新删除与 Web 入口，确认能否无 migration 实现 | **已完成：现有表、餐食范围查询、单项快照和模板更新删除足够；无需 migration** |
| 3. 事实源同步 | 清理“整份常用餐模板”为当前意图的错误表述，并同步产品、领域、体验与规划 | **已完成：产品、领域和体验基线统一为单项常用与最近餐食部分复用** |
| 4. 功能实现 | 支持常用食物快速加入、最近一餐按项选择复用、常用项编辑删除与当天独立调整 | **已完成：复用现有接口，无 migration** |
| 5. 验收发布 | 类型、测试、构建、API 合约、桌面/手机 E2E、目视、提交推送和受限部署通过 | **已完成：`00425bd` 已推送并受限发布；check、108 项服务端测试、build、API 合约、43/43 E2E、双视口目视均通过；公网 `index-kOcX7hTZ.js` 包含目标版本，live/ready 正常，API/Worker 正常且 PostgreSQL/其他项目未变** |

## Phase 6.4 下一批功能推进

| 阶段 | 完成条件 | 状态 |
|---|---|---|
| 1. 当前缺口复核 | 对照产品、领域、体验、交付文档与真实代码，排除域名/HTTPS 后移项并确认可独立交付的功能缺口 | **已完成：提醒主体已实现；后台推送与 Android 受 HTTPS/真机门槛约束；DeepSeek、备份属服务器专项；公共食物搜索是当前可独立交付的真实功能缺口** |
| 2. 批次定稿 | 按真实使用价值、现有基础、数据影响和回归范围选择一批完整能力 | **已完成：接入 Open Food Facts 公开包装食品搜索；个人结果优先、显式搜索、每 100g 换算、来源归属、外部失败降级；无 migration** |
| 3. 功能实现 | 完成前后端、状态、错误处理与必要事实源同步 | **已完成：可替换提供方、鉴权 API、个人/公开分组、实际克数换算、错误降级与事实源均已落地** |
| 4. 自动化与目视验收 | 类型、测试、构建、API 合约、桌面/手机 E2E 和多尺寸目视通过 | **已完成：check、110 项服务端测试、生产构建、API 合约、43/43 E2E、390/1280px 目视与无横向溢出/日志检查均通过** |
| 5. 发布验证 | 自动提交推送、受限部署、公网版本与健康检查通过 | **已完成：`96d195e` 已推送并受限部署；API/Worker 正常，PostgreSQL/其他服务未变；公网 JS/CSS 200、JS 含目标版本、live/ready 200** |

### Phase 6.4 错误记录

| 错误 | 尝试 | 处理 |
|---|---:|---|
| Web 读取器以安全检查拒绝直接打开 `search.openfoodfacts.org` 的 health 与带中文查询参数的搜索 URL | 1 | 不重复同一路径；改用受控只读 `curl` 验证精确公共端点，并仅记录状态与非敏感响应结构 |
| 组合检索再次包含已知不存在的 `apps/server/test`，并按旧路径读取不存在的 `apps/server/src/config.ts` 与根 `.env.example` | 1 | 搜索仍返回有效文件但命令部分报错；后续只使用已确认的 `apps/server/src/config/environment.ts`、`apps/server/src/entrypoints/` 和 `deployment/.env.example`，不再按历史路径猜测 |
| Web API 补丁假定 `FoodSearchResult` 在 `nutrition.ts` 内声明，实际该文件从共享类型导入，补丁未应用 | 1 | 先读取文件顶部真实 import 与导出结构，再把公共搜索类型放入实际类型源并只追加 API 方法，不重复原上下文 |
| 新增提示样式使用了仓库不存在的 `--color-warning` / `--color-warning-soft` token | 1 | 样式尚未构建；读取现有 token 后改用项目已有语义色，不为单条提示临时制造一套色值 |
| E2E 组合补丁对 `nutrition-flow.spec.ts` 写了两个独立 Update 区块，`apply_patch` 整体拒绝且未产生部分修改 | 1 | 合并同一测试文件的用例改名、新流程和旧定位器更新为单个 Update；服务端夹具与测试文件分别应用 |
| Phase 6.4 首轮 `npm run check` 失败：两个显式 `AppConfig` 测试夹具缺 4 个新配置字段；Vitest `toMatchObject` 不接受类型参数 | 1 | 补齐统一测试配置默认值和身份路由显式夹具，删除两处无效断言类型参数后原样重跑；Web 类型检查本轮未报错 |
| 公开食品定向 E2E 首轮 0/2：页面快照已有固定“原浆豆奶”，但普通 `div` 不具备测试所需的 `region` 语义；手机长页面还受双栏和滚动动画影响 | 1 | 改为具名 `section`，42rem 以下明确单栏；长流程使用产品已有 reduced-motion 路径。重新构建后桌面/手机 2/2 通过，实际 390px 浏览器点击与换算也通过 |

### Phase 6.2 错误记录

| 错误 | 尝试 | 处理 |
|---|---:|---|
| 样式组合补丁引用的 `@media (min-width: 40rem)` 锚点顺序与文件实际位置不一致，整份补丁未应用 | 1 | 改为按基础样式、历史样式、饮食样式和已有媒体查询分别应用小补丁，不重复原组合补丁 |
| 定向 E2E 的项目隔离服务在沙箱内监听 `127.0.0.1:4174` 返回 `EPERM` | 1 | 构建和类型检查已通过；按既有边界原样申请只运行项目自带 E2E 服务，不修改端口或测试逻辑 |
| 定向 E2E 16/18；训练既有用例仍断言已移除的私有动作媒体提示 | 1 | 页面原创文字指导状态正确；删除这一条已失效断言，不改业务行为，再原样重跑训练用例 |
| 浏览器组合操作在注册后设置手机视口时返回 `Cannot destructure property 'state' of 'undefined'` | 1 | 不重复整段组合操作；先读取当前页面确认注册结果，再按已验证的视口能力调用方式单独设置 |
| 首轮营养模块检索包含不存在的 `apps/server/test` 路径，`rg` 报 `No such file or directory` | 1 | 搜索结果仍返回真实文件；后续只使用已确认存在的 `apps/server/src/**/*.test.ts` 与 `apps/web/e2e/`，不重复错误路径 |
| 新餐食复用 E2E 8/10，失败页面仍显示修改前的“从常用和最近记录中找”界面 | 1 | 日期值正确；根因是只运行了类型检查，项目 E2E 服务复用了旧 `dist`。先运行生产构建生成当前 Web/Server 产物，再重跑同一用例，不修改通过类型检查的业务逻辑 |
| 最新构建下营养 E2E 6/10：连续录入测试未等待前一项异步保存完成，豆浆输入被表单重置覆盖；既有用例仍使用改名前区域/按钮 | 1 | 每次测试录入后等待该食物出现在当前餐次列表，再进行下一项；既有用例改用“搜索更多个人记录/调整后加入”的当前可访问名称，不改变业务实现 |
| 餐食复用 E2E 8/10：点击“编辑”后，带 `hasText: 鸡蛋` 的行定位器因文字被表单输入替代而不再匹配 | 1 | 编辑按钮已成功打开表单；后续改为从管理区域定位唯一 `.template-edit-form`，不继续依赖编辑前的行文字过滤条件 |
| 桌面浏览器目视时对定位器调用未提供的 `scrollIntoViewIfNeeded` | 1 | 前序“加入选中项、展开管理、打开编辑”均已成功；不重复该方法，改用定位器 `evaluate` 调用 DOM `scrollIntoView` 后截图 |
| 滚动成功后误把页面级 `domSnapshot` 当作定位器方法调用 | 1 | 不再调用定位器快照；沿用已验证的 `reuseTab.playwright.domSnapshot()` 获取页面语义，同时保持当前滚动位置截图 |
| 浏览器控制台收尾误用不存在的 `reuseTab.console.logs()` | 1 | 页面宽度测量已完成但本次组合在控制台调用处中断；先检查当前标签公开能力名，再使用实际接口读取日志并恢复手机视口，不重复错误调用 |

## 私有动作媒体部署入口

> 2026-08-31：该功能已随 1,324 动作目录一并移除，以下为存档记录。

| 阶段 | 完成条件 | 状态 |
|---|---|---|
| 1. 现状复核 | 确认媒体文件未进入 Git/镜像，现有接口已鉴权并按固定动作 ID 读取 | **已完成** |
| 2. 生产配置 | 生产只接受绝对应用路径；提供可选只读 bind mount 覆盖文件 | **已完成：配置测试与静态覆盖边界通过** |
| 3. 部署预检 | 宿主机路径必须绝对、已存在、非符号链接并含 JPG/GIF | **已完成：Shell 语法通过；真实 Compose 解析待服务器** |
| 4. 文档 | 快速教程、完整手册和第三方边界说明使用同一命令与目录结构 | **已完成：引用一致性复核通过** |
| 5. 发布 | 全量验收、提交推送；涉及服务器目录或 mount 时单独申请授权 | **已完成：`67897fc` 已推送；公网版本、live/ready 通过；未授权媒体挂载保持待执行** |

## 动作资料目录治理

> 2026-08-31：相关动作目录与媒体功能已移除，以下为存档记录。

| 阶段 | 完成条件 | 状态 |
|---|---|---|
| 1. 现状审计 | 找全旧目录引用，并区分源素材、生成数据、运行时媒体和文档 | **已完成：旧路径只影响开发默认值、生成脚本与工作记录** |
| 2. 边界定稿 | `docs/` 只存长期说明，`.runtime/` 存忽略的本地私有素材，应用数据和脚本跟随服务端 | **已完成：采用 `.runtime/exercise-catalog/source/` 作为原始资料根目录** |
| 3. 目录迁移 | 1,324 张 JPG 与 1,324 个 GIF 无损迁移，代码和文档不再引用旧路径 | **已完成：新路径数量与 137 MiB 总量一致，旧目录已不存在** |
| 4. 自动化验收 | 目录数量、类型检查、测试、构建、合约、目录校验与 diff 检查通过 | **已完成：113 项服务端测试、33/33 E2E、实际 JPG/GIF 解析均通过** |
| 5. 发布验证 | 代码提交推送，API/Worker 受限部署，公网版本与健康检查通过 | **已完成：`65df57a`，公网资源包含目标版本且 live/ready 通过；未上传或挂载媒体** |

## 本地动作资料接入

> 2026-08-31：相关动作目录与媒体功能已移除，以下为存档记录。

| 阶段 | 完成条件 | 状态 |
|---|---|---|
| 1. 资料审计 | 明确动作字段、媒体类型、规模、重复项和质量风险 | **已完成：1,324 条、1,324 JPG、1,324 GIF** |
| 2. 领域映射 | 对齐现有动作指导、计划动作、账号边界和媒体来源登记 | **已完成：公共文字目录 + 登录后可选私有媒体 + 媒体回退** |
| 3. 产品接入 | 动作资料能在现有训练计划与训练中被搜索、选择或查看，且缺失素材有安全回退 | **已完成：计划、训练、历史修正均可搜索；预览可切换静态图/GIF** |
| 4. 自动化验收 | 类型、测试、构建、E2E 与生成物检查通过 | **已完成：112 项服务端测试、33/33 E2E、浏览器与媒体边界检查通过** |
| 5. 发布验证 | 提交推送、Exercise App 受限部署、公网健康与版本验证通过 | **已完成：`b20f60a`，公网资源、live/ready 均通过** |

## 错误记录（本地动作资料接入）

| 错误 | 尝试 | 处理 |
|---|---:|---|
| 前端组合补丁因 `ExerciseGuidanceCard.vue` 的精确上下文不匹配而整体未应用 | 1 | 拆分为 API、组件、页面与样式小补丁；服务端与已生成目录不受影响 |
| 本地媒体接口首轮类型检查找不到 `TrainingError` | 1 | 新路由使用了已有错误类型但漏导入；补齐显式导入后继续检查，不改变错误语义 |
| 本地浏览器验收首次猜测服务端脚本名为 `start:e2e` | 1 | 实际脚本为 `e2e:serve`；未启动进程、未改文件，先读取 package scripts 后使用真实入口 |
| 本地媒体版验收服务在沙箱内监听 `127.0.0.1:4174` 返回 `EPERM` | 1 | 按既有边界仅为项目自带隔离服务申请本机监听权限；原命令成功启动并完成页面验收 |
| 完整 E2E 31/33，桌面和手机训练流仍断言旧的无媒体文案 | 1 | 页面实际状态正确；把旧断言更新为新的“本地未配置可用的动作媒体”，随后原样重跑完整套件 |
| 第二轮完整 E2E 32/33，桌面额外动作提交被迟到的搜索下拉层遮挡 | 1 | 输入框失焦时取消计时并使进行中请求序号失效，防止响应在失焦后重新打开下拉；手机已通过但仍按真实竞态修复 |
| 目录治理完整 E2E 首次启动在沙箱内监听 `127.0.0.1:4174` 返回 `EPERM` | 1 | 构建已先通过；按既有受控权限原样重跑，桌面/手机 33/33 通过，不修改端口或测试逻辑 |

## 自动发布流程

| 阶段 | 完成条件 | 状态 |
|---|---|---|
| 1. 本地发布门 | 类型、测试、构建、E2E 与 diff 检查通过 | **已完成：105 项服务端测试、33/33 E2E** |
| 2. Git 发布 | 一个范围明确的修复提交已推送 `origin/main` | **已完成：`48c13cb`** |
| 3. 受限部署 | 服务器仓库 fast-forward，按提交 SHA 构建镜像，只切换 Exercise App API/Worker | **已完成：远程任务无 error 结束，未授权数据库或共享服务变更** |
| 4. 公网验收 | 首页资源包含目标 SHA，live/ready 连续通过 | **已完成：公开 JS 包含 `48c13cb`，live=`ok`、ready=`ready`** |

## 第三轮真实使用体验收口

| 阶段 | 完成条件 | 状态 |
|---|---|---|
| 1. 数据与字段审计 | 确认无餐食时的剩余量来源；确认自由文本意图和动作指导的真实作用范围 | **已完成：意图字段不参与任何计算；动作指导 API 可复用但入口过晚** |
| 2. 营养剩余 | 有有效每日参考且尚未记餐时，剩余能量和营养素等于完整参考，不显示空值 | **已完成：零餐食按已记录 0 计算，未知营养值仍保持未知** |
| 3. 设置流程 | 首次设置使用可跳过的分步引导；完成后进入设置目录并按模块单独修改 | **已完成：首次四步引导，日后按资料、测量、目标、提醒、数据分别进入** |
| 4. 入口与文案 | 问题报告成为独立 Bug 反馈页；移除无计算作用的自由文本意图；逐项自然化训练文案 | **已完成：前端不再展示或接收自由文本意图，兼容字段固定保存为空** |
| 5. 动作指导 | 查看单次方案、周期训练日和进行中训练时都能打开动作预览或示范 | **已完成：系统草案、单次方案、周期训练日和实际训练均可展开文字动作预览** |
| 6. 发布验收 | 类型、测试、构建、E2E、桌面/手机目视、GitHub 推送、受限部署和公网版本健康全部通过 | **已完成：`43e8c2f` 已推送并受限部署；108 项服务端测试、33/33 E2E、公网 live/ready、新版脚本提交号与桌面/375px 目视均通过** |

## 动作示范视频来源研究

| 阶段 | 完成条件 | 状态 |
|---|---|---|
| 1. 候选发现 | 找到带原始仓库、官方网站或素材页的动作视频/动画候选 | **已完成：wger、CDC/Wikimedia Commons 与若干 GitHub 数据集** |
| 2. 许可核查 | 逐项确认视频文件本身的许可、署名、修改和再分发要求，而不只看代码许可 | **已完成：wger 为逐条 CC BY-SA 4.0；抽查 CDC 文件为公共领域但仍需来源和非背书声明** |
| 3. 产品适配 | 比较动作覆盖、内容质量、外链/自托管、移动流量和离线能力 | **已完成：wger 高清覆盖更好但原始包 3.57 GB 且多为 HEVC；CDC 低风险但仅 320×240、覆盖有限** |
| 4. 结论 | 给出可直接采用、需补充授权与不建议采用的分级结论 | **已完成：推荐审核后自托管小批 wger + CDC；拒绝非商业、第三方权利不清和待复核素材** |

## 线上体重保存缺陷

| 阶段 | 完成条件 | 状态 |
|---|---|---|
| 1. 线上诊断 | 确认保存请求的状态、脱敏错误类型和真实 PostgreSQL 路径 | **已完成：实际失败为 `PUT /planning/profile`；传输层 `revision=0` 覆盖数据库默认值并触发正数 CHECK** |
| 2. 回归缺口 | 覆盖首次档案、策略和提醒写入时传输字段不得进入领域输入 | **已完成：服务单测与 PostgreSQL 集成用例均加入同类输入** |
| 3. 修复与本地验收 | 根因修复后通过类型、服务端、构建和相关 E2E | **已完成：107 项服务端测试、构建、OpenAPI、33/33 E2E 与 diff 检查通过** |
| 4. 自动发布 | 提交、推送，只更新 Exercise App API/Worker，并在线验证 | **已完成：`096b502`；公开 JS 版本一致，live/ready 通过** |

## 第二轮界面与文案收口

| 阶段 | 完成条件 | 状态 |
|---|---|---|
| 1. 基线审计 | 逐页列出表单语义、可点击区域、圆角/平角、字号和文案问题 | **已完成** |
| 2. 资料表单 | 出生日期整框可点击；性别作为稳定基础信息展示，文案不再暴露实现术语 | **已完成** |
| 3. 视觉系统 | 控件、面板和状态组件使用明确且一致的半径与字号层级 | **已完成** |
| 4. 文案收口 | 全站逐页删除重复说明，保留必要边界、错误与安全提示，语气自然 | **已完成** |
| 5. 本地验收 | 类型检查、生产构建、E2E、320–1440 px 布局与浏览器目视通过 | **已完成：类型、构建与 33/33 E2E 通过；设置、饮食和提醒区域截图复核通过** |

## 真实使用缺陷收口

| 阶段 | 完成条件 | 状态 |
|---|---|---|
| 1. 缺陷基线 | 登录错误可解释；所有主要页面在桌面与手机视口完成结构、重叠、溢出、字号、交互和错误状态审查 | **已完成** |
| 2. 数据库修复 | `daily_planning_references` 在真实 PostgreSQL/RLS 下可写，缺少档案时仍返回正常 `needs_profile` 结果，具备回归测试 | **已完成：服务器隔离测试库在受限角色与 RLS 下通过，完整集成验证 14/14** |
| 3. 前端修复 | 页面结构稳定，无重叠或横向溢出；字号、间距和层级使用一致 token；不改变已确认产品信息架构 | **后继修复本地完成：历史页按日记录优先，空状态去重，手机趋势保持紧凑；完整 E2E 33/33** |
| 4. 主线验收 | 类型、测试、构建、OpenAPI、桌面/手机 E2E 与真实浏览器关键流程全部通过 | **已完成：102 项服务端测试、31 项 E2E** |
| 5. 独立复核 | 未参与主线修改的智能体复查代码、页面和测试；其有效问题全部关闭 | **已完成：两轮异步跨日期 blocker 均已修复并复核关闭** |
| 6. 发布验证 | 修复提交推送，服务器安全更新并验证登录后规划与主要页面 | **已部署：公网静态资源已切换并包含 `d9608cc`，live/ready 连续通过；等待产品所有者用真实账号手动检查历史页** |

## 首次完整提交审查

| 检查项 | 完成条件 | 状态 |
|---|---|---|
| Git 范围 | 所有修改和新增文件均属于当前产品、文档或部署代码，没有意外大文件或生成物 | **已通过：52 个已跟踪修改、166 个新增文件** |
| 私有数据 | 不包含真实 secret、照片、日志、数据库、备份、个人运行记录或本机配置 | **已通过：高置信凭据与个人主机信息零命中** |
| 工程一致性 | workspace、migration、Compose、文档链接、脚本权限和 Git 忽略规则一致 | **已通过** |
| 全量验证 | 类型、95 项服务端测试、构建、OpenAPI、27 项浏览器回归、脚本与安全检查通过 | **已通过** |
| Git 提交 | 只在全部审查通过后暂存明确文件并创建本地提交 | **已完成：`95f1bc3`，并按后续部署授权推送到 GitHub `main`** |

## 首次真实服务器部署验收

| 阶段 | 验收内容 | 状态 |
|---|---|---|
| 0. 仓库与主机基线 | 拉取当前部署提交，确认工作区、系统规格、Docker/Compose、端口和已有容器/volume | **已通过：服务器仓库干净同步到 `1bdf535`** |
| 1. 运行环境、本机配置与预检 | 逐条审查 Docker 软件包、数据目录和服务启动；每项变更后复验宿主机与现有项目，再创建项目私有配置并运行 preflight | **已通过：Docker/Moby 29.3.1、Compose 5.4.0、root 用户级 Buildx 0.31.1、`/newdata` 数据目录、四个私有 secret 与 preflight 均通过；现有服务保持正常** |
| 2. 首次启动 | 构建镜像、setup migration、API/worker/PostgreSQL 健康检查 | **已通过：四个服务达到预期终态，API/Worker restart 0，最终 smoke 与 70 秒稳定观察通过** |
| 3. 数据与故障验收 | 测试数据库、RLS、事务、worker 崩溃恢复、volume 持久化、备份恢复 | **部分通过：真实 migration、32 表 RLS、受限角色和任务心跳已验证；破坏性故障、重建持久化与备份恢复待执行** |
| 4. 模型与访问 | 真实 DeepSeek 请求、临时图片删除、手机 IP+端口访问 | **部分通过：外部 `IP:5011` live/ready 均为 200；真实 DeepSeek 与分析后原图生命周期待真实 Key 验收** |

## 当前可运维性增量

| 项目 | 目标 | 状态 |
|---|---|---|
| 快速部署教程 | 从空白服务器到手机可访问只保留必要命令，并链接完整手册 | **已完成** |
| 应用内问题报告 | 用户填写现象后，一键生成可复制、可下载的脱敏文本；包含环境、健康状态和近期前端错误 | **已完成** |
| 部署侧诊断报告 | 一条命令把 Compose 状态、健康检查和近期容器日志写入本地文件，不读取 secret、数据库内容或照片 | **已完成** |
| 验收与同步 | 类型、测试、构建、E2E、脚本语法、文档链接与规划状态一致 | **已完成** |

## Web 开发完成矩阵与后续目标

| 顺序 | 可运行切片 | 状态 | 本地完成门 |
|---|---|---|---|
| 1 | 按日期安排训练，并接入“今天”和训练历史 | **已完成（本地）** | 单次方案、周期单元和纯主题均可安排/取消/开始；今天只显示当日内容；实际记录可按日期回看 |
| 2 | 动作指导元数据、训练提醒和训练记录修正 | **已完成（本地）** | 正常、空白、取消、跨时区、并发和账号隔离测试通过 |
| 3 | 个人档案、身体测量、目标策略与官方规划引擎 | **已完成（本地）** | 公式证据、输入快照、停止条件、单位、舍入与官方样例测试通过 |
| 4 | 手工饮食、食物条目、份量、每日汇总与饮食历史 | **已完成（本地）** | 不使用照片也能完成全天记录和修正；未知值不归零 |
| 5 | 图片上传、DeepSeek 异步分析、候选修正与原图删除 | **已完成（本地；真实 API 待服务器验收）** | 模拟供应商和请求合约、重试、重复处理、账号隔离、候选修正与删除生命周期测试通过 |
| 6 | 提醒、导出、账号删除、备份恢复与长期运行管理 | **已完成（本地）；首次服务器运行通过，备份恢复待专项验收** | 服务器重启/升级/恢复后可继续使用，管理页显示真实运行摘要 |
| 7 | 朋友与开源安全收口 | **已完成（本地）；真实 RLS 与首次部署通过** | 全量跨账号测试、RLS、限流、隐私与静态部署检查通过 |
| 8 | 完成性审计发现的 Web 领域缺口 | **已完成（本地）** | 训练消耗、图片暂定采用/确认、饮食安排、历史趋势和上传体验逐项有实现与测试证据 |
| 9 | 自部署真实验收 | **首次启动已完成；专项故障与模型验收继续** | 空库/升级、持久化、队列故障、真实模型、备份恢复和手机访问通过 |
| 10 | Android Capacitor 验证与 APK | **等待 Web 实际使用认可与真机环境** | 真机安装升级、相机、通知、会话和弱网核心流程通过 |

真实 PostgreSQL、Docker、备份恢复和 pg-boss 故障测试属于独立部署目标；执行时仍不得用内存测试替代真实结果。

## Phase 5 已通过本地验证的范围

- Vue Web、Fastify API、PostgreSQL/Drizzle、pg-boss Worker、临时媒体和 Docker Compose 部署定义已形成完整工程；
- 训练计划、周期计划、日期安排、实际训练、历史修正、证据化系统训练建议和严格匹配官方活动模式的训练消耗已通过自动化验证；
- 官方营养规划、独立饮食安排、手工餐食、个人常用与最近记录搜索、图片异步分析、暂定采用/确认和每日汇总已通过自动化验证；
- 历史页按实际记录展示训练、饮食、体重趋势，缺失值不会补成零；客户端图片压缩失败可安全保留原图，上传过程显示真实进度；
- 独立提醒、导出、账号删除、管理员运行摘要、限流/配额、RLS、安全/隐私/贡献材料和备份恢复脚本已完成；
- 模型结果不自动覆盖用户采用值，照片不进入导出或默认长期备份，密码只保存 Argon2id 摘要。

## 独立后续目标

当前代码最终证据：严格类型检查、生产构建、23 个文件 95 项服务端测试、OpenAPI、Compose YAML、19 份 migration 与 journal/snapshot 一致性、全部部署脚本 Shell 语法及可执行位、本地文档链接、`git diff --check` 和生产依赖在线审计（0 漏洞）通过；Playwright 桌面与手机共 27 项通过。

- 独立部署目标中在目标服务器实际执行 Docker Compose、PostgreSQL migration、RLS、pg-boss 崩溃恢复、备份、异机恢复和升级回滚检查；
- 使用项目所有者的真实 DeepSeek Key 验证图片输入、结构化结果、用量、超时、限流和供应商错误，不能用模拟结果代替；
- 公共食物目录只在获得明确可再分发数据源或合规运行时接口后接入；当前个人常用项、近 90 天记录、手工录入和图片估餐可独立工作；
- 产品所有者实际使用 Web 并认可体验后，在 `apps/android/` 进行 Capacitor 相机、通知、会话、弱网和安装升级验证，再生成 APK/AAB。

## 错误记录

| 日期 | 错误 | 处理 |
|---|---|---|
| 2026-08-30 | 首轮服务端测试仍断言动作指导英文来源名，107/108 通过 | 实现只是把界面来源名自然化为中文；同步更新旧断言后重跑全量测试，不改回英文文案 |
| 2026-08-26 | 远程 Codex 阶段 0 的 `git fetch` 被其文件系统沙箱拦截写入 `.git/FETCH_HEAD`，仓库和 GitHub 本身尚未报告错误 | 保留远程任务在批准状态，由产品所有者在该任务中批准原命令；不绕过沙箱、不修改 `.git` 权限、不执行 reset/clean |
| 2026-08-26 | 首次暂存复核发现 `CONTRIBUTING.md`、`PRIVACY.md`、`SECURITY.md` 文件尾各多一个空行，`git diff --cached --check` 因格式警告停止 | 删除三个多余空行、重新暂存，并从头执行暂存文件数、敏感模式、产物扩展名和 diff 检查；不带警告提交 |
| 2026-08-26 | migration 一致性检查的 Node 单行命令把正则反斜杠重复转义，导致 19 份 SQL 被错误统计为 0 | 改用文件后缀和长度筛选后原样核对，最终为 19 份 SQL、19 条 journal 且全部 snapshot JSON 可解析；不把失败命令当成迁移问题 |
| 2026-08-26 | 人工复核潜在凭据赋值时，组合命令中的 `sed` 引号与 zsh 字符串边界冲突，命令在任何扫描执行前以 `unmatched \"` 退出 | 不再把复杂脱敏表达式嵌入同一命令；高置信扫描已独立通过，后续按文件名和安全模式分别复核 |
| 2026-08-26 | 首次敏感信息扫描中，高置信规则以 `-----BEGIN` 开头，被 `rg` 误当成命令选项；另一条含引号的规则被 zsh 解析为 glob，两个扫描项没有执行 | 改为显式使用 `rg -e`，并使用不依赖嵌套引号的赋值模式重新扫描；不把失败结果当成通过 |
| 2026-08-26 | 新增问题报告的定向 E2E 已完成生产构建，但测试服务被沙箱拒绝监听 `127.0.0.1:4174`（`EPERM`），用例尚未执行 | 按既有受控流程原样在沙箱外重跑同一项目本机监听；不修改端口或测试来绕过权限 |
| 2026-08-26 | 沙箱内 `ssh-add -l` 无法连接 ssh-agent（`Operation not permitted`）；同时 `~/.ssh` 没有默认 `id_*` 私钥文件，只有配置和 known_hosts | 仅为读取 agent 中已加载密钥指纹申请受控权限；不导出密钥内容、不添加密钥、不启动新 agent |
| 2026-08-26 | 获得网络权限后的只读 SSH 检查到达目标服务器，但 `root@<server-ip>` 返回 `Permission denied (publickey,...)`，未执行远端命令 | 只读检查本机 ssh-agent、默认私钥文件和详细认证协商，判断是密钥未加载、配置缺少 IdentityFile 还是服务器未授权；不提取、复制或改写私钥；真实 IP 不写入公开仓库 |
| 2026-08-26 | 对已配置目标 `root@<server-ip>:22` 的首次只读 SSH 连通性检查被本地沙箱以 `Operation not permitted` 拒绝，远端没有执行任何命令 | 按权限流程原样重跑短超时、BatchMode、严格主机密钥的只读检查；不修改 SSH 配置、不接受新主机密钥、不执行部署写入 |
| 2026-08-26 | 续接服务器验收时一次性读取三份长期规划文件，合计约 8.6 万 token，工具输出被截断 | 当前状态已从 task plan 前段和 findings 尾部恢复；改为分别读取当前阶段与最新进度，并运行 session catch-up，不再重复整文件输出 |
| 2026-08-26 | 最终生产依赖审计在受限网络中无法解析 `registry.npmjs.org`，同时 npm 不能写用户日志目录；类型、测试、构建、E2E 和静态配置检查不受影响 | 按权限流程原样重跑只读 `npm audit --omit=dev`；不执行自动修复、强制升级或扩大依赖范围 |
| 2026-08-26 | 部署静态验收定位命令同时传入了不存在的 `apps/server/scripts` 和根 `scripts`，`rg` 报路径不存在；实际脚本全部位于 `deployment/scripts` | 后续只对已定位的部署脚本目录执行 Shell 语法和敏感命令检查，不创建重复脚本目录 |
| 2026-08-26 | PostgreSQL 集成命令在收集测试前因未配置 `TEST_DATABASE_NAME`、`TEST_DATABASE_USER` 及独立测试凭据而退出，0 个集成用例执行 | 保持真实数据库验收为外部门槛；不连接未知本机/生产数据库，也不把 92 项内存测试替代为 PostgreSQL 通过。集成测试源码继续由严格类型检查覆盖 |
| 2026-08-26 | 全量 92 项服务端测试中 91 项通过；数据访问分类测试发现新增 `diet_plans` 表尚未登记到安全白名单 | 将该账号归属表加入 RLS 强制分类，并验证策略迁移后重跑全量测试；不删掉全表必须恰好分类一次的安全门 |
| 2026-08-26 | 完成性缺口的完整 E2E 首次运行已完成生产构建，但项目测试服务在执行任何用例前被沙箱拒绝监听 `127.0.0.1:4174`（`EPERM`） | 按权限流程原样重跑 `npm run test:e2e`，只放开项目本机监听和浏览器自动化；不修改应用端口或测试逻辑绕过权限 |
| 2026-08-26 | 饮食计划定向测试的无效日期用例在进入 `expect(...).rejects` 前同步抛错；服务方法在校验后返回 Promise，但未声明为异步，导致错误语义与其他写操作不一致 | 将创建方法声明为 `async`，让输入错误稳定表现为被拒绝的 Promise，再原样运行服务与路由测试；不降低日期必须位于计划范围内的规则 |
| 2026-08-26 | 为定位饮食计划失败行执行的编号输出读取范围过大，工具返回内容被截断 | 不依据截断内容修改测试；直接按测试栈和已知服务边界做最小修正，并用定向测试验证 |
| 2026-08-26 | 内置网页抓取器直接打开 E-003 官方公告并搜索正式 PDF 均返回空内容 | 改用只读 `curl` 请求官方公告并只解析附件链接；不从二手页面或模型记忆填写 MET 数值 |
| 2026-08-26 | 受限网络中的首次 `curl` 无法解析中国体育科学学会域名 | 按权限流程原样重跑只读请求，已取得官方公告和附件地址；没有更换为二手来源 |
| 2026-08-26 | 本机没有系统 `pdftotext`，后续 `rg`/`sed` 因文本文件未生成而连带失败 | 使用工作区自带的 PDF/Python 运行时读取，并按 PDF skill 渲染对应页；不安装全局依赖或重复原命令 |
| 2026-08-26 | 系统 `/usr/bin/python3` 没有 `pypdf`/`pdfplumber`，但已定位到 Codex bundled `pdftoppm` | 不向系统 Python 安装包；先用 bundled `pdftoppm` 渲染，随后定位 bundled Python，或用其他现成只读 PDF 工具提取文本 |
| 2026-08-26 | `pdftoppm` 的 override 包装器没有按系统 CLI 参数处理输出前缀，向工具输出写入大量 PNG 二进制并伴随 fontconfig 缓存警告 | 不重复该命令；改用已下载 PDF 的页面截图工具直接读取指定页，或调用 bundled 原始二进制并显式设置可写 fontconfig 缓存 |
| 2026-08-26 | 图片分析审计按常见名称猜测 `modules/image-analysis/processor.ts`，实际处理逻辑位于现有其他文件，`sed` 报不存在 | 通过 `rg` 返回的真实 `service.ts` 与 repository 路径继续审计，不创建重复 processor 文件 |
| 2026-08-26 | 训练建议 E2E 在候选生成成功后直接断言折叠的 `<details>` 内证据文本可见，桌面与手机都正确返回 hidden | 先点击“适用范围和依据”展开详情再断言证据 ID；不为了测试移除合理的折叠交互 |
| 2026-08-26 | 训练建议表单为三个字段写了无效的 `as const | ...` 类型断言，Vue 严格检查报 `Cannot find name 'const'` | 直接复用 API 的 `TrainingSuggestionPreferences` 类型定义整个 reactive 表单，不手写重复联合类型 |
| 2026-08-26 | 按共同文本 `trainingService,` 装配 E2E 时，新依赖被插入 `ReminderService` 参数而不是 `buildApp` 参数，生产与 contract 未受影响 | 删除提醒构造中的多余字段，并在 E2E `buildApp` 的相邻属性处显式加入 `trainingSuggestionService`；随后依靠严格类型检查验证全部入口 |
| 2026-08-26 | 拆分后的训练建议路由补丁仍与 OpenAPI 路径清单中的实际顺序不匹配，导致路由与 app 改动也被同一补丁回滚 | 彻底按单文件应用：先创建路由，再修改 app，最后读取 OpenAPI 清单精确插入；不再把不确定锚点混入功能补丁 |
| 2026-08-26 | 系统训练建议跨 app/API/contract/E2E 的组合补丁假设 E2E 服务中训练服务声明与生产入口顺序一致，实际装配位置不同，导致整份补丁未应用 | 先独立新增路由，再按每个入口的真实 import 和构造位置分别装配；避免单个测试入口锚点阻断其他文件 |
| 2026-08-26 | 朋友与开源收口的生产依赖审计在受限网络中无法解析 `registry.npmjs.org`，且 npm 不能写用户日志目录 | 按权限流程原样重跑只读 `npm audit --omit=dev`；不执行自动修复或强制升级，并以实际审计结果为准 |
| 2026-08-26 | 完整 E2E 21/23 通过；新增管理员临时密码流程在桌面和手机都找不到目标账号行内的“设置临时密码”按钮并超时 | 读取失败页与管理页真实 DOM，区分账号未加载、权限状态或按钮名称/位置问题后只修根因，再定向重跑两个视口 |
| 2026-08-26 | 朋友与开源收口的完整 E2E 首次运行在产品代码执行前被沙箱拒绝监听 `127.0.0.1:4174`（EPERM）；构建和其余全量检查已通过 | 按权限流程原样重跑 Playwright 本机监听；不修改应用端口或测试逻辑来绕过沙箱，也不把权限失败记为产品失败 |
| 2026-08-26 | 部署文档与 README 的组合补丁猜测 README 存在“文档”标题，实际入口结构不同，导致整份补丁未应用 | 将自托管文档与 README 拆开；先按已读取的部署文档精确更新，再读取 README 真实结构后添加链接 |
| 2026-08-26 | 安全收口复核沿用常见目录猜测 `apps/server/test` 与 `apps/server/src/db/migrate.ts`，当前仓库测试与迁移入口实际跟随源码或位于其他路径 | 使用 `rg --files apps/server` 定位真实文件后再补测试与整理 import，不创建重复测试目录或迁移入口 |
| 2026-08-26 | 新增管理员临时密码 E2E 时猜测强制改密页标题为“先设置你自己的密码”，现有真实页面标题是“修改密码” | 在运行浏览器前按既有 `preset-admin` 用例把断言改为真实标题；不改产品文案来迁就新测试 |
| 2026-08-26 | 新增账号上限和限流配置后，训练路由测试仍手写完整 `AppConfig` 且缺少 7 个新字段，严格检查失败 | 把该测试改为复用统一 `createTestConfig`，并检索是否还有手写完整配置；不把生产字段改成可选来迁就测试 |
| 2026-08-26 | 管理员临时密码界面补丁猜测 `.account-actions` 样式为多行区块，实际 CSS 上下文不同，跨 Vue/CSS 补丁整体未应用 | 先读取管理页脚本/模板锚点与真实样式定义，再按文件分别应用，避免一个 CSS 锚点阻断功能代码 |
| 2026-08-26 | 长期使用增量最终生产依赖审计在受限网络中无法解析 `registry.npmjs.org`，同时 npm 无法写用户日志目录；其余全量检查与 21 条 E2E 已通过 | 按权限流程原样重跑只读的 `npm audit --omit=dev`，不执行自动修复或依赖升级，并单独记录结果 |
| 2026-08-26 | 长期使用 E2E 预检沿用猜测文件名 `apps/web/e2e/e2e-server.ts` 和 `auth.spec.ts`，仓库实际采用其他名称 | 先用 `rg --files apps/web/e2e` 定位当前测试入口和规格文件，再在真实结构中补用例；不创建重复测试服务 |
| 2026-08-26 | 账号删除完成提示复核沿用猜测文件名读取 `views/LoginPage.vue`，实际认证页面位于其他目录或名称 | 使用 `rg --files apps/web/src` 定位真实登录页后再加提示；不创建第二个登录页面 |
| 2026-08-26 | 设置页可移植性补丁把多行 `planningApi` import 写成了不存在的单行上下文，整份补丁未应用 | 先按实际 import 区块单独加入依赖，再分别修改状态、加载、方法和模板，避免长 Vue 文件一次性匹配失败 |
| 2026-08-26 | 可移植性单测 3 项中 1 项通过、2 项失败：删除准备会先撤销会话所以正确错误码是 `authentication_required`；内存任务把下载可用性缓存为 true，媒体过期后没有派生刷新 | 修正测试采用真实会话语义；内存仓储与 PostgreSQL 一样从当前媒体状态派生 `downloadAvailable`，避免测试实现掩盖过期生命周期 |
| 2026-08-26 | 一次跨 API/Worker/E2E 的可移植性装配补丁假设 Worker 营养 import 顺序与 API 相同，单个锚点不匹配导致整份补丁未应用 | 按文件分别读取当前 import 和构造顺序，再逐个装配；不在同一补丁中绑定三个入口的不同上下文 |
| 2026-08-26 | 提醒 schema 补丁用猜测的导出顺序匹配文件底部，表定义上下文存在但导出列表顺序不同，整份补丁未应用 | 拆成“在训练提醒表后新增定义”和“读取实际 schema 导出尾部后追加”两步，避免大补丁因单一锚点失败 |
| 2026-08-26 | 图片 E2E 点击注册后立即主动导航设置页，取消了尚未完成的注册请求；桌面和手机最终都被认证守卫送回登录页并超时 | 与既有饮食流程一致，先等待注册成功跳转 `/today`，再进入设置页；这是测试同步错误，不是账号或图片功能错误 |
| 2026-08-26 | 从根脚本透传 Playwright `--grep` 时 npm 把筛选词拆成普通测试文件参数，构建成功但报告 `No tests found` | 已完成构建后改在 Web workspace 直接使用 `npm run test:e2e -- --grep ...`；这是命令透传问题，尚未执行产品流程 |
| 2026-08-26 | 部署复核沿用猜测文件名读取 `deployment/scripts/prepare-secrets.sh`，仓库实际没有该脚本 | 不新增无必要脚本；直接按现有自托管文档、Compose 和 secret 示例文件同步 DeepSeek 配置 |
| 2026-08-26 | 图片路由测试用 `typeof analysis[]` 描述 JSON 数组，受变量初值和联合类型影响被推断成 `never` | 提取明确的 `AnalysisResponse` 测试类型并用于 `response.json<AnalysisResponse[]>()`，不依赖自引用推断 |
| 2026-08-26 | 内存临时媒体仓储把 Node 可读流的字符串分支直接断言为 `Uint8Array`，严格 TypeScript 拒绝不相交类型转换 | 显式分别处理 string、Buffer 和 Uint8Array，保持测试存储与生产流读取语义一致 |
| 2026-08-26 | 新增 DeepSeek 可选配置后，3 个旧测试仍手写完整 `AppConfig`，测试配置工厂也没有新字段默认值，导致服务端类型检查失败 | 给统一测试配置补安全默认值，并让旧测试复用该工厂或补齐字段；不把生产字段改成可选来迁就测试 |
| 2026-08-26 | 图片 PostgreSQL 仓储首次类型检查发现 `count(*)` 查询结果在类型上可能为空，直接解构后读取 `count` 不安全 | 显式读取首行并在无结果时使用 0，再继续严格检查；不使用非空断言掩盖数据库返回边界 |
| 2026-08-26 | 同步“训练不改变当天营养参考”的跨文档补丁把领域标题写成“训练与饮食联动”，实际标题多了“的当日”，整份补丁未应用 | 读取精确标题后拆分为产品、领域和证据三个补丁重试，避免任何半完成同步 |
| 2026-08-26 | 饮食服务首轮类型检查失败：当前 Vitest `toMatchObject` 类型不接受显式泛型参数 | 删除测试断言上的多余泛型，只断言错误码结构后重跑 |
| 2026-08-26 | 饮食路由首轮解析失败：长行中的贡献项修订路由缺少一个闭合括号 | 定位精确行、拆开路由定义并补齐闭合结构后重跑类型检查 |
| 2026-08-26 | 用单个 `apply_patch` 同时删除并重建 `NutritionPage.vue` 被补丁工具拒绝，因为同一文件出现多个操作 | 拆成一次删除、一次新增，内容不变 |
| 2026-08-26 | 饮食浏览器测试首次启动被沙箱以 `listen EPERM 127.0.0.1:4174` 拒绝 | 按既有测试边界请求仅运行该 Playwright 命令的本机监听权限后重跑 |
| 2026-08-26 | 饮食浏览器流程能建餐次但保存食物后未出现“已记录 232 kcal”，桌面和手机同样失败 | 读取失败页和请求结果，先区分接口保存失败与前端汇总刷新问题，再只修根因 |
| 2026-08-26 | 直接从 Playwright trace 压缩包读取固定名 `trace.network` 失败，当前版本归档内没有该路径 | 不猜归档结构，先列出 trace 文件名再读取实际网络资源 |
| 2026-08-26 | 捕获到营养保存的真实前端错误：Vue 对 `type=number` 的 `v-model` 在运行时给出 number，而转换函数只接受 string 并调用 `trim()` | 让数值解析同时接受 string/number，保留空字符串为未知，再重跑两种视口 |
| 2026-08-26 | 数值修复后保存和能量汇总成功，但 E2E 找不到脂肪的“不完整”提示；页面只在能量行展示完整性 | 给蛋白质、碳水和脂肪各自展示完整/未知状态，忠实呈现逐营养素完整性 |
| 2026-08-26 | 饮食流程继续通过到常用项断言，但 `getByLabel("我的常用项")` 同时匹配下拉框和“保存到常用项”复选框 | 用 `combobox` 角色和名称限定常用项下拉框，不修改已经正确的页面行为 |
| 2026-08-26 | 饮食流程已完成修正并进入历史页，但桌面未显示预期的“部分”后缀，手机连饮食卡都未匹配 | 分别读取两种视口失败页，核对历史筛选点击目标和部分汇总判定，再修实现或断言 |
| 2026-08-26 | 历史失败页确认两项均为测试问题：能量本身完整所以正确显示 `250 kcal`；手机的全局“饮食”按钮点回了底部导航 | 断言精确能量值，并把筛选点击限定在 `.history-filters` 内 |
| 2026-08-26 | 完整 E2E 13/15 通过；规划流程发现新饮食页重命名了参考区，并在重写时遗漏方法版本与规划限制说明 | 恢复方法、消息和限制展示；规划测试只更新已明确变化的区块名称后重跑 |
| 2026-08-26 | 直接打开 DeepSeek 官方视觉与价格文档均在抓取层 400 timeout | 改用官方域名搜索定位可抓取页面/缓存入口；在契约核实前不编写真实模型适配器 |
| 2026-08-26 | 本地 `curl` 访问 DeepSeek 视觉文档被沙箱 DNS 拒绝，无法用命令行复核该页面 | 保留官方搜索证据，并请求受控网络权限直接读取用户提供的官方 URL |
| 2026-08-26 | 下载后的官方视觉 HTML 含空字节，普通 `rg` 把它判为二进制文件而未提取限制文本 | 改用 `rg --text` 只读扫描临时文件，不修改或复制官方内容进仓库 |
| 2026-08-26 | 给当前营养贡献增加来源后，内存修订快照写入 `source`，但修订类型尚未同步该字段 | 给修订类型补齐来源与分析 ID，再重跑类型检查 |
| 2026-08-26 | 规划 E2E 桌面和手机都已渲染正确的 `2275 kcal`，但断言用精确文本 `2275`，因单位在同一 `<dd>` 内而找不到元素 | 改为在“今天的系统参考”区域按 definition 角色和完整值 `2275 kcal` 断言；这是测试定位错误，不是计算或页面错误 |
| 2026-08-26 | 规划浏览器流程首跑在产品代码执行前被沙箱拒绝监听 `127.0.0.1:4174`（EPERM）；生产构建本身已完成 | 按权限流程原样重跑定向 Playwright；不把本地端口权限问题记为产品失败 |
| 2026-08-26 | 规划模块首次类型检查把 `PlanningService` 按训练服务的对象参数方式构造，但它的构造函数直接接收仓储 | 将生产与 E2E 两处改为直接传入仓储实例；业务类型和公式代码未报错 |
| 2026-08-26 | 国家卫健委 PDF 在网页抓取层超时；下载后本机又没有 `pdftotext` | PDF 已从官方域名成功保存到临时目录；改用工作区自带 PDF/Python 运行时或逐页渲染核对，不安装无关系统软件 |
| 2026-08-26 | 受限网络中的首次 `curl` 无法解析中国营养学会域名；官网第二页在网页抓取层也单独超时 | 按权限流程下载只读官方文件到 `/tmp`；成功取得页面并定位表 13、14 的直接图片地址，临时文件不进入仓库 |
| 2026-08-26 | 中国营养学会 DRIs 成人表为官网 JPG，批量点击 4 张图片均在网页抓取层超时 | 已取得官网直接图片地址；改为逐张下载到临时目录并做视觉/OCR 核对，仓库不保存受版权保护的原表 |
| 2026-08-26 | 一次并行打开 National Academies、中国营养学会与国家卫健委长文档，工具输出超过上下文限制而被截断 | 改为逐一打开并精确定位章节或页码；未完整读取的内容不用于公式实现 |
| 2026-08-26 | 规划文件同步补丁使用了不存在的标题“错误与处理记录”，导致整份补丁未应用 | 读取实际标题“错误记录”后按表格结构重新应用；未产生部分修改 |
| 2026-08-26 | 日期修正 E2E 首跑在读取安排日期时，`getByLabel('日期')` 同时命中 aria-labelledby 的“安排训练日期”区域和日期输入，两个视口均被严格模式拒绝 | 把定位限定到“安排训练日期”区域内的 `input[type=date]`；这次失败发生在任何保存请求前，不是产品数据错误 |
| 2026-08-26 | 动作指导样式首次使用了设计令牌中不存在的 `--color-warning` 和 `--color-warning-soft`；类型检查不会发现 CSS 自定义属性缺失，浏览器会丢弃对应声明 | 改用现有 `--color-accent` / `--color-accent-soft` 令牌，并在浏览器回归中检查指导卡片实际渲染 |
| 2026-08-26 | 动作指导方法补丁以 `export class TrainingService` 为锚点时被应用到文件末尾闭合括号之后，形成类外 `public` 方法 | 在构造函数后按精确上下文插入方法并删除文件末尾误插段，再由 TypeScript 检查；指导目录文件本身未受影响 |
| 2026-08-26 | 换动作定向 E2E 两个视口都等待“实际动作”超时；本次直接运行 Web workspace Playwright，没有先重建服务端静态目录，E2E 服务实际加载的是新增字段前的旧 `dist` | 先执行生产构建，再原样运行定向 E2E；后续有前端源码变化时优先使用根 `npm run test:e2e` 或显式先 build，避免用陈旧产物误判产品 |
| 2026-08-26 | 换动作与历史修正的定向 Playwright 首次启动未执行：本地端口/浏览器权限自动审查在创建进程前超时 | 按权限规则原样重试一次；不把权限审查超时记录为产品测试失败，也不假定流程已通过 |
| 2026-08-25 | 首次加入“实际动作名称”后全仓库检查失败：过宽的 `sortOrder/exerciseName` 上下文把字段插入了前面的动作表与前端模板项，而不是实际训练项；一个 PostgreSQL 集成调用也缺新字段 | 读取精确结构后把字段移到 `training_session_items` 与 Web `TrainingSessionItem`，补集成请求；随后重跑严格检查，不保留错误迁移 |
| 2026-08-25 | 日期安排 E2E 第二次运行仍是测试严格定位问题：“开始训练”同时部分匹配页头“安排或开始训练”和安排卡片按钮；其余 9 项继续通过 | 使用已限定的“今天还要练”区域并要求按钮名称精确匹配，再重跑完整 E2E；不通过 `.first()` 掩盖页面语义冲突 |
| 2026-08-25 | 日期安排 E2E 首跑 11 项中既有 9 项通过；新增流程在桌面和手机都因“今天的力量训练”同时出现在安排卡片与面板说明中触发 Playwright 严格定位失败 | 将断言限定到“今天还要练”区域内的安排卡片，不改变产品页面；随后原样重跑两个视口的完整 E2E |
| 2026-08-25 | 排期首轮服务端类型检查发现日期筛选对象在 `exactOptionalPropertyTypes` 下显式携带 `undefined` | 按存在性条件构造筛选对象，不把缺失日期序列化为属性；随后重跑服务端检查 |
| 2026-08-25 | 给 PostgreSQL 实际训练映射补排期字段时使用了过宽上下文，字段误插入 `toTemplate` | 立即读取映射区确认位置；从模板映射删除并加到 `toSession`，随后由严格类型检查复核 |
| 2026-08-25 | 排期切片预检沿用旧文件名读取 `apps/web/src/views/Dashboard.vue`，文件不存在；同时仓库没有独立 `HistoryPage.vue` | 用 `rg --files apps/web/src` 定位当前 Dashboard/路由结构；历史仍在原型仪表盘中，后续按真实路由拆出，不重复使用旧路径 |
| 2026-08-25 | 周期 E2E 首跑 9 项中既有 7 项通过，新周期流程在桌面和手机均于“保存训练日”后没有显示复制动作 | 定位为导入模式仍发送隐藏空白动作；改为明确发送空动作数组、由服务端从来源复制，随后两个视口 9 项全部通过 |
| 2026-08-25 | 根目录定向测试把仓库路径透传给服务端 workspace，Vitest 在 `apps/server/` 下按该重复前缀找不到测试文件；全仓库类型检查已先通过 | 改为在服务端 workspace 内使用 `src/modules/training/...` 相对路径重跑；这是命令路径错误，不是测试失败 |
| 2026-08-25 | 训练页脚本组合补丁因重复字段上下文与当前文件不一致而整体拒绝 | 拆分为计算辅助函数和具体字段两次精确修改；未产生部分写入 |
| 2026-08-25 | 追加周期前端检查记录时误用不存在的二级标题“周期计划迁移复核”，`apply_patch` 未命中 | 读取文件尾部后确认该标题实际是三级标题，按准确段落追加；没有产生部分修改 |
| 2026-08-25 | 周期类型、内存仓储和服务扩展后首次服务端检查失败：PostgreSQL 仓储尚未实现新增的 8 个周期接口，实际训练映射也缺少 5 个周期来源字段 | 这是按层实施中的预期未闭合状态；立即补齐 PostgreSQL 映射与事务方法，再重新检查，不降低接口范围 |
| 2026-08-25 | 修正方案数值后，E2E 已通过方案创建和开始训练，但实际组过滤仍对数字次数调用 `.trim()`，两个视口均在保存实际数据时失败 | 统一表单数值运行类型与非空判断，所有 number input 都走同一字符串/数字兼容路径，再次全量重跑 |
| 2026-08-25 | 训练方案保存的浏览器错误定位为 `TypeError: trim is not a function`：Vue 对 `type=number` 的 `v-model` 在运行时提供数字，而转换函数只接受字符串 | 让可空整数转换同时接受字符串和数字，保持空值为 `null`；随后重跑完整 E2E |
| 2026-08-25 | 获得本地运行权限后 Playwright 实际执行 7 项，既有 5 项通过；新增训练流程桌面和手机均在保存方案后找不到方案卡片 | 读取失败时页面快照和请求结果，判断是表单/API 错误还是列表刷新问题；修正产品代码后原样重跑两种视口 |
| 2026-08-25 | 新增训练 E2E 后首次运行，全仓库构建通过，但沙箱禁止 Fastify 监听 `127.0.0.1:4174`，Playwright 在测试开始前以 `EPERM` 退出 | 按权限流程仅为 `npm run test:e2e` 请求本地监听与浏览器运行权限，获准后原样重跑 |
| 2026-08-25 | 首轮训练测试中，测试断言错误地向 Vitest `toMatchObject` 传入类型参数；训练方案 API 同时因请求校验返回 400，导致后续跨账号用例使用无效 ID | 删除不受支持的断言类型参数；读取 400 响应详情定位 JSON Schema 问题，修正后重新执行两组测试 |
| 2026-08-25 | 首次检查训练路由时发现 `routes.ts` 第 275 行括号不匹配，TypeScript 报 TS1005 | 先定位对应路由闭合结构，修正语法后再继续处理类型问题 |
| 2026-08-25 | 更新增量 1 发现记录时使用了不匹配的标题上下文，`apply_patch` 未命中 | 用 `rg` 定位正式标题为“2026-08-25：增量 1 开始方式纠正”，随后按准确上下文追加，不改写既有内容 |
| 2026-08-25 | 验收脚本与文档同步补丁使用了不完整的自托管文档上下文“随后检查”，`apply_patch` 无法定位并整体拒绝 | 未产生部分修改；代码/忽略规则与各文档改为按精确标题和现有段落分别应用 |
| 2026-08-25 | 无跳过项的 Playwright 并行回归中，桌面与移动注册测试使用相同毫秒时间戳生成用户名，触发唯一性竞争；其余 4 项通过 | 用户名加入项目维度且保持在 32 字符限制内，使并行测试拥有独立账号后重新运行 |
| 2026-08-25 | 清除 Playwright 有意跳过项的补丁对 `e2e-server.ts` 分成两次更新，`apply_patch` 因同一文件重复操作而整体拒绝 | 未产生部分修改；合并该文件的 import 与账号夹具改动为一次更新后重新应用 |
| 2026-08-25 | 最终生产依赖审计在受限网络中无法解析 `registry.npmjs.org`，同时 npm 无权写用户日志目录；本轮安装时的审计结果为 0 漏洞，但仍需独立复核生产集合 | 按权限流程只重跑 `npm audit --omit=dev`；不执行自动修复或依赖升级，并以重跑结果作为最终记录 |
| 2026-08-25 | Playwright 获得本地监听权限后实际执行 4 项：1 项按设计跳过，3 项因测试定位/等待错误失败；“今天”匹配到多个标题，改密提交尚未完成就主动跳转管理页 | 首页断言改为精确标题；改密后先等待路由完成再访问管理页；这属于测试代码问题，未发现产品请求失败，修正后重新执行桌面与移动项目 |
| 2026-08-25 | Playwright 首次运行时，其专用内存测试服务监听 `127.0.0.1:4174` 被沙箱以 `EPERM` 拒绝；应用构建已先行通过 | 不修改产品端口或测试设计，按权限流程仅为浏览器自动化测试重跑本地监听；若随后缺少浏览器二进制，再单独记录而不冒充测试通过 |
| 2026-08-25 | 新增 OpenAPI 命令首次通过 `tsx` CLI 运行时，沙箱拒绝其临时 IPC 管道监听并返回 `listen EPERM`；此前检查、23 项测试和构建均已通过 | OpenAPI 与 E2E 测试服务改为运行已编译的 Node 入口，避免为只读契约生成引入 IPC 权限；随后重新验证 |
| 2026-08-25 | 为建立浏览器自动化入口查询 `@playwright/test` 当前版本时，受限网络下 `npm view` 30 秒没有返回 | 已终止挂起进程；按权限流程重试只读 npm registry 查询，不猜测版本，也不影响已经完成的本地检查与测试 |
| 2026-08-25 | `git add --dry-run` 仍需创建 `.git/index.lock`，在只读 Git 元数据权限下失败 | 敏感模式扫描先独立通过；随后按权限流程直接暂存，再用只读的 staged diff 和 `--cached --check` 审核后才提交 |
| 2026-08-25 | 尝试写入仓库级 Git 作者身份时，工作区允许修改源码但 `.git/config` 为只读，返回 `could not lock config file` | 按权限流程申请只针对该仓库执行 `git config`、暂存和提交，不改全局配置、不推送 |
| 2026-08-25 | Git 提交预检确认仓库尚无首个提交，同时仓库和全局均未配置 `user.name` / `user.email`；直接提交会因作者身份缺失失败 | 不伪造用户私人邮箱；从已确认的 GitHub 用户名采用仓库级公开 noreply 身份后再创建首个提交，并在交付说明中明确记录 |
| 2026-08-25 | 一致性检索把 Markdown 反引号放进双引号 shell 参数，zsh 将其中的 `app` 当命令执行，随后空模式产生大量无效结果 | 未产生文件修改；后续检索使用单引号或拆分固定字符串，避免 shell 命令替换 |
| 2026-08-25 | 用系统 Ruby 2.6 静态解析 Compose YAML 时传入了新版本 Psych 才支持的 `aliases:` 关键字，校验命令在第一步停止 | 文件为仓库内受信配置，改用 Ruby 2.6 支持的 `YAML.load_file` 默认解析后再继续其余独立检查 |
| 2026-08-25 | 重启临时身份服务后，浏览器登录验收预期固定跳到 `/today`，实际按登录前的 `redirect=/account/password` 正确返回改密页，导致导航等待超时 | 确认认证已成功且行为符合 redirect 设计；后续显式导航 `/today` 验收主应用，不把测试预期错误当成产品故障 |
| 2026-08-25 | 本地浏览器验收调用的页面等待接口声明包含 `networkidle`，当前实现实际不支持该值 | 不重复使用；改为 `domcontentloaded` 后读取新 DOM 快照确认页面状态 |
| 2026-08-25 | 浏览器验收用的本地内存身份服务首次监听 `127.0.0.1:3000` 被沙箱以 `EPERM` 拒绝 | 不改变应用实现；按权限流程仅为本地验收进程申请监听端口，验收结束后终止该临时进程 |
| 2026-08-25 | 全仓库检查、测试和构建通过后，生产依赖审计在受限网络中因 `registry.npmjs.org` DNS 失败而未完成 | 保留其余验证结果，按权限流程单独重跑只读的 `npm audit --omit=dev`，不把网络失败误报为依赖漏洞 |
| 2026-08-25 | 拆分后的 Dashboard 移动补丁首次匹配时漏写 `matchMedia` 查询字符串外层括号，补丁未应用 | 使用文件中的精确原文重新提交移动补丁 |
| 2026-08-25 | 一次 `apply_patch` 同时移动并重新创建 `App.vue`，被工具判定为同一路径存在多个操作而整体拒绝 | 拆为“移动并改造 Dashboard”和“新建根 App”两个独立补丁，避免任何部分写入 |
| 2026-08-25 | 首次加入改密流程后，TypeScript 检查发现 `invalid_current_password` 尚未加入 `IdentityErrorCode` 联合类型 | 补齐受控错误码后重新执行全仓库检查与测试 |
| 2026-08-23 | Phase 3 预检沿用 Vite 默认路径读取 `apps/web/src/App.tsx`，文件不存在 | 已按仓库架构改读 `apps/web/src/app/App.tsx` 与 `apps/web/src/styles/`，后续不再使用旧路径 |
| 2026-08-23 | 受限环境首次安装 Vue 时无法解析 `registry.npmjs.org`，并且无法用 `ps` 检查进程 | 停止挂起命令后取得明确 `ENOTFOUND`，按批准的 `npm install` 网络权限完成安装；不再依赖受限的进程枚举 |
| 2026-08-23 | Vue 迁移后 `vue-tsc` 从根目录解析到 TypeScript 7.0.2，无法访问已取消导出的 `typescript/lib/tsc`；只修改应用版本以及根 `overrides` 均未消除错误 | 发现 Vue 的可选 peer 把 TypeScript 7 提升到根目录；改由根 workspace 明确统一 TypeScript 5.9.3，检查与构建恢复通过 |
| 2026-08-24 | 首次大范围补丁同时删除并新增同一文件，`apply_patch` 拒绝多个操作指向同一路径 | 拆分为先替换源码、再单独重建样式文件，避免同一补丁重复定位文件 |
| 2026-08-24 | 浏览器首轮自动检查使用了过窄的按钮名称和不可用的页面构造器，导致选择与尺寸检查失败 | 改用实际无障碍名称和 DOM 几何属性复核；交互与 320、375、414、768、1280 宽度检查随后通过 |
| 2026-08-24 | 文案回归测试用“训练”做模糊按钮匹配，同时命中“查看训练”和底部“训练”导航 | 改为按完整无障碍名称精确匹配，随后逐页检查通过 |
| 2026-08-24 | 原浏览器标签引用在用户侧刷新后失效；随后误用了不存在的 `tabs.attach`、未声明赋值和元素级滚动/截图方法 | 按当前浏览器规范从 `user.openTabs()` 取得精确标签对象并通过 `user.claimTab()` 重新连接；后续只使用已验证的页面定位、状态选择和整页尺寸检查方法 |
| 2026-08-24 | 本轮视觉复核误把 `reload` 当作页面控制方法调用 | 停止重复调用，改用页面导航或已支持的定位与快照方法，并在后续复核前确认当前页面控制接口 |
| 2026-08-24 | 历史页的“训练”筛选与底部“训练”导航同名，浏览器严格定位命中两个按钮 | 改为先定位“筛选历史内容”分组，再在组内点击“训练”，同时保留两个控件各自清楚的无障碍上下文 |
| 2026-08-24 | 移动端复核误把页面对象挂在 `playwright.page` 下，实际接口未提供该属性 | 停止使用内部页面对象，改用浏览器封装公开的视口与页面求值方法完成检查 |
| 2026-08-24 | 第二次尝试的 `playwright.setViewportSize` 也不是公开方法 | 不再猜测接口；重新读取当前浏览器的完整公开文档后，只调用文档列出的视口方法 |
| 2026-08-24 | Phase 4 官方资料搜索一次发生连接失败 | 保留已取得的一手资料，随后缩小查询范围重试成功；没有用搜索摘要缺口代替官方依据 |
| 2026-08-24 | 更新 findings 与 progress 的组合补丁包含空的 progress hunk，`apply_patch` 拒绝执行 | 拆成有实际上下文的独立补丁，findings 随后更新成功 |
| 2026-08-24 | 首次 Markdown 本地链接检查递归进入 `node_modules`，报告了第三方包自带文档的缺失相对链接 | 检查器改为排除依赖目录，只验证仓库人工维护的 Markdown |
| 2026-08-24 | 复核 DeepSeek 视觉文档时，价格页直接打开超时，猜测的 Files API 路径被安全检查拒绝 | 不重复猜路径；以已成功打开的官方 Vision 指南为直接依据，Files API 后续从该页的实际导航链接进入 |
| 2026-08-24 | Phase 4 初稿只核对普通 V4 Chat API 和 Copilot 集成页，遗漏官方 `guides/vision`，错误判断 DeepSeek 没有直接图片输入证据 | 产品所有者提供准确链接后完整读取 Vision 指南；撤回错误判断，并同步修正架构、交付、证据、Agent 规则和进度记录 |
| 2026-08-24 | 从 Vision 页继续抓取 Files API 与 token 子页时再次超时 | 不重复请求；当前结论只采用已完整读取的 Vision 指南正文，远端文件保留策略保持待核对，不据此选择 Files API |
| 2026-08-24 | 临时照片生命周期同步补丁引用了技术架构中不完全匹配的上下文，整份补丁被拒绝 | 确认未产生半完成修改后按产品/领域/体验与技术/交付分段应用，并用冲突检索和 `git diff --check` 复核 |
| 2026-08-24 | pg-boss 决定同步补丁包含一个没有定位上下文的空追加区块，整份补丁被拒绝 | 确认没有文件被部分修改后，将正式文档与研究/进度记录拆分应用 |
| 2026-08-25 | 训练计划存档同步补丁在同一领域文件中按倒序定位两个区块，工具无法回到更早章节匹配并整体拒绝 | 确认没有部分修改后，按文档章节顺序重新组织补丁 |
| 2026-08-25 | Phase 5 增量 0 环境检查发现本机没有 `docker` 与 `docker compose` 命令 | 继续创建可审阅的 Compose 与持久化配置，使用 Node 层测试完成不依赖容器的验证；把真实 PostgreSQL 容器、migration、备份恢复和 pg-boss 故障测试明确保留为安装 Docker 后的待验收项，不伪造通过结果 |
| 2026-08-25 | 本机同时没有 `psql`；受限环境中的首次 `npm view` 在 30 秒内没有访问 registry，只返回 `psql` 缺失 | 不改用 SQLite；按权限流程使用获准的只读 npm registry 查询取得版本，真实数据库验收继续等待 Docker 或 PostgreSQL 环境 |
| 2026-08-25 | 首次安装服务端依赖后 npm 报告 4 个中等风险漏洞，并提示 `@esbuild-kit/esm-loader`、`@esbuild-kit/core-utils` 已废弃 | 不执行 `npm audit fix --force`；先读取审计依赖链，优先通过精确兼容升级或移除非必要依赖解决，未解决项必须保留风险说明与复现依据 |
| 2026-08-25 | 首次按包名设置的嵌套 npm override 没有替换 `@esbuild-kit/core-utils` 下的 esbuild 0.18.20 | 不重复相同安装；改用带父依赖与精确版本的嵌套 override 语法再验证一次，仍无效则不强行篡改依赖树，转为隔离 Drizzle Kit 本地执行并保留审计风险 |
| 2026-08-25 | 第二种带父依赖和精确版本的 npm override 仍未替换旧 esbuild | 移除无效 override，不再篡改不满足上游 semver 的依赖；Drizzle Kit 保持 devDependency、禁止对外启动其开发服务，并新增生产依赖审计命令，等待上游移除已废弃加载器 |
| 2026-08-25 | 服务端首次类型检查把仓库级 `drizzle.config.ts` 和 `vitest.config.ts` 纳入 `rootDir: src`，触发 TS6059；测试同时提示 Fastify `disableRequestLogging` 顶层选项将废弃 | 工具配置继续由各工具直接加载，服务端编译只包含 `src/**/*.ts`；删除等同默认值的废弃 Fastify 选项后重新验证 |
| 2026-08-25 | 第二轮类型检查发现 Fastify 5.12.1 的错误处理器参数按 `unknown` 处理，直接读取 `statusCode` 触发 TS18046 | 增加对象形状与数值类型收窄；只有已识别的 4xx 错误返回可读信息，未知或 5xx 异常统一返回非敏感说明 |
| 2026-08-25 | argon2 0.45.1 的 `verify` 类型不接受 `type` 选项，首次身份服务检查触发 TS2353 | 删除冗余选项；Argon2 摘要自身携带算法标识，验证函数按编码摘要识别 argon2id，哈希生成仍显式固定 argon2id 与成本参数 |
| 2026-08-25 | PostgreSQL 身份仓库补丁再次在同一 `apply_patch` 中对 `setup.ts` 使用两个更新操作，工具整份拒绝 | 确认无文件被部分修改；合并该文件的 import 与主体为单一更新操作后再应用，后续继续按每文件一次操作组织大补丁 |
| 2026-08-25 | 首次检查 pg-boss 类型时沿用错误的根路径 `node_modules/pg-boss/types.d.ts` | 同次检索已定位实际类型入口为 `node_modules/pg-boss/dist/index.d.ts` 与 `dist/types.d.ts`；后续按 package.json 的 `types` 字段读取，不重复旧路径 |
