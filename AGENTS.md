# AGENTS.md

适用于整个 `exercise-app` 仓库。这里保留项目事实、完成标准和授权边界，具体步骤按任务选择。

## 协作与完成标准

- 产品所有者决定目标和取舍。已明确的能力不得擅自删减、降级为假设或要求反复证明；验证产品是否有帮助，不给用户设置打卡或使用配额。
- 讨论/评估时给出有依据的结论，不自动实现；要求修改/开发时，完成已确认范围内的实现、必要验证和交付，不停在方案、第一版或“是否继续”。新需求先提交完整草案让用户整体纠正，已确认实现不重新逐项审批。
- 常规缺口采用安全、可逆、易修改的默认方案。只有缺失信息确实阻塞结果、现有文档无答案且无法合理默认时才提问，说明影响，先完成不受阻塞的工作。
- 已授权的本地编辑、假数据测试、修复本次引入的失败和重跑受影响测试可自主完成。持续推进不扩大权限；需要新授权时先准备具体改动、影响和验证方案，在越过边界前停下。
- 简洁中文，先说结果与剩余问题。更新报告重要发现、阶段结果或阻塞，不逐条播报工具操作或重复安全套话。

## 上下文与持久记录

先看当前请求、工作区改动和 `.planning/exercise-app/task_plan.md` 的当前状态。已有且未变化的上下文无需重读；按将要改变的行为查阅相关章节，不为小修复通读全仓或全部历史。

| 涉及内容 | 事实源/入口 |
|---|---|
| 产品能力与范围 | `docs/product.md`（唯一产品事实源） |
| 页面、交互和领域行为 | `docs/experience/experience-design.md`、`docs/domain/domain-rules.md` |
| 训练/营养数值、公式、适用边界 | `docs/domain/calculation-evidence.md`，不得凭模型记忆新增规划依据 |
| 工程目录、服务端与数据架构 | `docs/architecture/repository-layout.md`、`docs/architecture/technical-architecture.md` |
| 交付与验收 | `docs/architecture/delivery-plan.md` 的 Phase 6.6 清单 |
| 部署/恢复 | `docs/deployment/self-hosting.md`；首次部署另看 `docs/deployment/quick-start.md` |
| 决策/排障证据 | 按需检索 `docs/product-decisions.md`、`.planning/exercise-app/findings.md`、`progress.md` 和归档 |

项目规则和技能指南服从用户最新明确要求，系统/平台权限仍须遵守。旧实时训练、饮食安排、全天确认流程不再反推需求；实现状态只在当前计划维护，历史“下一步”不构成任务。产品纠正后同步受影响事实源，清理冲突，不逐句复制到多份文档。

- 需要持久跟踪时沿用 `.planning/exercise-app/`。当前计划只放目标、状态、必要证据和下一步，历史进入进度或归档、研究进入发现；简单修改不因工具次数新建计划。
- planning-with-files 在本仓库按重要进展更新，不机械执行“两次工具即写文件”、通读所有历史或把每次错误写进当前计划。恢复脚本仅在实际缺失上下文且环境支持时使用；重试依据风险与新证据，不以固定次数代替判断。
- 选用技能后按其要求读取相关内容，不额外加载无关技能和全部参考。若技能导致暂停或偏离用户要求，说明具体文件、相关规则及影响，区分硬性要求与自己的解释。
- release-skills 沿用本项目提交 SHA 发布，不自动引入 semver、tag、npm/GitHub Release，不重复请求已有授权；纯指令/文档修改不构建镜像或部署。
- 代理只用于用户授权的协作范围，任务应可独立完成且收益覆盖交接成本，不为了增加数量而委派。

## 工程与数据边界

- Vue 3 + TypeScript + Vite 位于 `apps/web/`，服务端位于 `apps/server/`；Android 达到既定门槛后放入 `apps/android/`。不重新选择前端框架；`packages/` 只承载真实跨应用复用。根目录保留全仓入口，应用专属文件跟随应用。
- `deployment/` 只放部署定义与可审阅脚本。照片、数据库、真实凭据、备份、用户记录及APK/AAB/dist/build/缓存不提交；原图不进入长期导出或默认备份。
- PostgreSQL是业务事实源；Drizzle生成可审阅SQL，生产只执行获授权的版本化migration，禁止schema `push`。pg-boss只负责执行，自有任务表权威，Worker保持至少一次执行与幂等写回，当前不引入Redis。
- 私有表保存账号归属并纳入访问分类、跨账号拒绝测试和PostgreSQL RLS；新增私有表同步这些边界及导出/删除。管理员不因此获得他人健康数据读取权。
- 数据库只保存安全基线规定的Argon2id密码摘要；明文/可逆密码不得进入数据库、日志、错误、任务、导出或备份清单，原文仅限单次认证请求内使用。浏览器认证令牌不进入`localStorage`；测试/容器/迁移骨架使用假数据和示例secret。
- 问题报告由用户主动生成、先预览，不自动上传；浏览器自动收集不含请求体、响应体、账号标识、凭据、照片或健康明细。部署诊断不读取secret、数据库记录或媒体内容。
- 用`apply_patch`编辑人工维护文件，保留已有改动，不覆盖无关文件，不执行未授权的破坏性Git/清理。测试库须显式核实为隔离的`_test`目标；重建/恢复使用已确认影响的可审阅入口，不删业务volume、不轮换共享角色密码。

## 验证与交付

按实际影响验证；通过后继续交付，只有新改动、失败或未解的具体疑点才扩大或重复检查。未变化的源码可沿用准确版本的有效证据。

| 改动 | 验证 |
|---|---|
| 指令/文档、无行为变化 | 差异、引用和规则一致性；不跑业务全套或编写只复述实现的断言 |
| 应用行为或可运行切片 | 类型检查、构建、受影响单元/API测试；核心交互补对应手机/桌面E2E，必要时目视核对 |
| 数据/事务/权限/迁移/任务恢复 | 对应真实隔离PG测试及RLS、回滚、幂等、安全边界；内存测试不能替代 |
| 跨模块或共享入口 | 按影响执行完整相关回归；E2E期间保持构建产物固定 |

根命令：`npm run check`、`npm run test`、`npm run build`、`npm run api:contract`、`npm run test:e2e`、`npm run test:integration`。开发用`npm run dev`，依赖变动时才安装；大输出保存临时文件，只读摘要或相关片段。

完成意味着请求范围内行为可用、相应验证有证据、相关文档同步且授权内交付收口。区分已实现、本地通过、真实PG/模型通过和已发布；环境缺失须标明，不以静态演示、固定模型结果或旧切片测试冒充。真实模型只用已授权样本；用量、限流/超时、准确性和恢复按任务分别验收，不每批重跑全部专项。

## Git、发布与远程协作

- 远程 `https://github.com/Syriii/exercise-app`，默认分支`main`。2026-08-30持续授权保留：每批用户可见应用修复通过本地验收后，自动提交main、推送origin/main、受限部署并验证公网健康/版本，无需重复确认。
- 授权仅覆盖本项目代码fast-forward、应用镜像构建、私有应用版本键更新和仅重建Exercise App API/Worker；不覆盖setup/migration、生产数据写入、PostgreSQL、Nginx、Docker服务、宿主机、端口、secret、volume、其他项目或清理。需要这些操作时说明准确影响并单独获授权；发布失败保留现场，不扩大范围。
- 2026-09-07对S1的0019/0020迁移、必要私有备份和专用`_test`恢复验证是已执行的单次授权，不作为后续迁移许可；完整setup、共享角色改密、初始化账号和旧备份清理不在其中。
- 2026-09-08本批f7cfd13单次授权已执行完成：本项目受限私有备份、专用`_test`隔离恢复验证及清理本轮无连接的恢复库、仅0021/0022生产版本化迁移，并完成API/Worker受限发布；备份保留。不含完整setup、队列迁移、共享角色改密、生产业务写入、PG重建或生产覆盖恢复，不作为重新执行或其他迁移许可。当前版本和证据见当前计划。
- 2026-09-08 B2b单次授权待执行：用户明确“同意，通过全部门禁后执行”。仅在本批20项真实隔离PG验收通过后，允许本项目受限私有备份（保留旧备份）、专用`_test`恢复验证及清理本轮无连接测试库、仅0023_photo_analysis_control生产版本化迁移，随后API/Worker受限发布。不得改密码、权限或历史业务记录，不重建PG，不运行setup/队列迁移，不清理旧备份。固定SQL、阶段门禁及执行状态见.planning/exercise-app/photo-control-release-gates-2026-09-08.md；不泛化到其他迁移。
- 用户允许通过114服务的现有Codex任务操作。发送后使用有界wait_threads持续等待，直到取得远端明确的最终回复或需要介入的具体请求；一次超时、inProgress或暂时空正文不是结束等待、让用户代查或判定客户端故障的依据。等待期间可做独立工作，不反复播报未变化的状态。
- 状态变为completed后仍须核对该回合最终正文；若为空，继续等待同步并用read_thread最新页复核，不能仅凭状态认定验收成功。只有出现明确读取异常、无法继续自动取得正文时，才说明已核实的状态和异常并请用户复制；不把暂时空返回定性为版本或分页缺陷。用户询问最新结果时必须重新读取，不能沿用旧快照。
- 不为回传另建任务、重发执行、重跑验收或自行设计替代传输通道；收到最终回复后区分完成、失败、执行前门禁与待授权，再按已有权限继续。用户转贴结果须标注来源，持续等待不扩大执行授权。
