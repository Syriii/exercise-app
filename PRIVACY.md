# 隐私说明模板

Exercise App 是自托管软件。实际数据控制者是运行服务器的人，而不是本开源仓库的维护者。部署者在邀请朋友前应补充自己的联系方式、服务器位置和适用规则。

## 收集与用途

系统按用户主动输入保存账号、个人档案、身体测量、训练计划与实际记录、餐食与营养记录、提醒设置和必要的任务状态，用于提供训练与饮食记录、每日营养参考、历史回看、导出和账号删除。

密码不会明文或可逆存储；数据库只保存 Argon2id 哈希。浏览器会话使用 HttpOnly Cookie，服务端只保存不可逆 token 摘要。

## 餐食照片与 DeepSeek

只有用户主动拍照上传时，照片才会进入服务器临时存储。启用 DeepSeek 视觉能力的部署会把照片内容、营养估算提示和必要请求元数据发送给部署者配置的 DeepSeek API；训练记录、密码和完整个人档案不会随该请求发送。

模型结果是估算候选，不会自动计入饮食。用户确认或修正后才形成当前采用值；按默认设置，采用后删除服务器原图。失败或待复核照片到期后由 worker 清理。部署者还应阅读并提供当前 DeepSeek 服务条款与隐私政策链接。

## 保存期限

- 结构化训练、档案、测量和饮食记录：保留到用户修正、删除对应对象或永久删除账号。
- 临时照片：默认采用后删除；未采用照片按临时媒体过期规则清理。
- JSON 导出：默认保留 7 天，可由部署者调整。
- 运行日志：不应记录密码、Cookie、照片内容或模型原始内容；具体轮换期限由部署者公布。
- 数据库备份：按部署者的备份保留期保存。账号删除不会回写或逐份修改已经离线保存的历史备份；若以后从旧备份恢复，部署者必须重新执行删除请求或采用等效清理流程。

## 访问与共享

每个普通账号只能通过应用访问自己的健康记录。产品内管理员可以管理注册、账号状态、临时密码和聚合运行状态，但没有读取其他用户训练、饮食、照片或导出的接口。

服务器操作系统管理员、数据库 owner 和备份保管者在技术上仍可能接触数据。部署者不得以管理员页面限制代替主机权限控制，也不得在没有用户知情同意时分享或另作他用。

## 用户控制

用户可以修正记录、下载账号级 JSON 导出，并在设置中永久删除账号。删除请求会立即停用账号并退出所有设备，后台随后删除数据库记录和临时文件。预置管理员不能通过应用内自删，以免服务器失去唯一管理入口。

隐私或删除问题应联系实际服务器部署者。开源仓库维护者无法查看或删除第三方部署中的数据。
