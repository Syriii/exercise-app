# Exercise App 领域规则

| 项目 | 内容 |
|---|---|
| 阶段 | Phase 2 — 领域规则 |
| 状态 | 已由产品所有者整体确认；作为 Phase 3 及后续设计的当前领域事实源 |
| 日期 | 2026-08-24 |
| 上游事实源 | `docs/product.md` |
| 本文目的 | 定义业务概念、关系、状态、计算责任、不确定性和例外 |

## 1. 文档边界与规则等级

本文把已经确认的产品全貌变成能够支持后续体验设计和技术设计的领域规则，但不定义页面、数据库表、接口、框架、部署方式或首版开发顺序。

文中采用两种当前有效的规则等级：

- **继承规则：** 直接来自已经确认的 `docs/product.md`，本文不能擅自降低或删除。
- **已确认规则：** 产品所有者已经在 Phase 2 明确确认的领域规则，后续不得重新作为未知问题。

本稿形成过程中曾以“初始规则”标记 Agent 提出的可修改方案；产品所有者于 2026-08-23 整体确认后，这些规则已统一升级为“已确认规则”。发生冲突时，以产品所有者最新明确表述和 `docs/product.md` 为准。

## 2. 领域全貌

产品由六个相互协作但职责独立的领域组成：

| 领域 | 主要责任 | 不负责 |
|---|---|---|
| 个人档案与目标 | 保存计算参考所需的个人状态、目标及其生效时间 | 替用户决定训练或饮食安排 |
| 训练 | 系统建议、用户计划、动作指导、实际训练、消耗估算 | 把训练计划冒充实际完成情况 |
| 饮食 | 用户饮食计划、每日营养参考、餐食记录、摄入汇总 | 用训练提醒替代饮食提醒 |
| 图片分析 | 保存待分析照片、管理分析任务、候选结果、采用和修正 | 把模型输出表述为测量真值 |
| 当日联动 | 根据计划和实际活动更新当天饮食参考 | 合并训练与饮食两个任务体系 |
| 通用与安全 | 账号隔离、时间、修订、来源、提醒、数据控制和健康边界 | 提供诊断、治疗或绝对安全承诺 |

关系可以概括为：个人档案和目标产生系统参考；用户计划表达意图；训练与餐食记录表达事实；图片分析为餐食事实提供候选估算；当日联动只调整当前参考，不改写已经发生的事实。

## 3. 全领域共同规则

### 3.1 用户控制权

- **继承规则：** 系统训练建议不是命令，用户可以采用、修改或忽略；用户饮食计划由用户自主决定，系统每日营养参考只作为参考。用户也可以只使用部分能力。
- **继承规则：** 缺少记录不阻断程序使用，不产生强制补录、连续打卡或使用频率要求。
- **已确认规则：** 用户创建的计划、选择的策略和实际记录只能由用户主动操作或明确授权的规则改变；系统推导不得静默覆盖用户输入。

### 3.2 计划、事实和推导分离

- **继承规则：** 系统训练建议、用户训练计划和实际训练记录分别表达候选来源、用户意图和已发生情况；建议只有被用户采用或编辑后才复制为计划，训练主页不把建议与计划长期并列。
- **继承规则：** 用户饮食计划、系统每日营养参考和实际餐食记录是三类独立业务对象，分别表达意图、系统推导和已发生情况。
- **已确认规则：** 系统每日营养参考属于规划基准，餐食照片分析属于实际摄入估算。图片识别质量不得改变参考目标的计算方法或准确性要求；只影响已摄入、剩余量和偏差结果的完整性与置信程度。
- **已确认规则：** 所有汇总结果都要能追溯到原始记录、采用的计算规则和当时有效的个人信息；推导结果不得反向改写原始记录。

### 3.3 时间与历史

- **继承规则：** 体重或目标变化从变更当天开始影响当天及以后，不重新计算过去日期。
- **已确认规则：** 每个与日期有关的记录都按用户当时设置的时区归属自然日；跨时区后，历史记录仍保留原归属日。
- **已确认规则：** 当天发生个人信息或目标变更时，当天当前参考可以重算，但旧参考作为修订历史保留；已经记录的训练和摄入不变。
- **已确认规则：** 修改历史训练或餐食属于用户主动修正，应重新汇总被修改的日期；这与后来体重或目标变化导致的“历史不回算”不同。
- **已确认规则：** 用户修正历史训练或餐食时，默认使用该日当时有效的档案、目标、数据来源和计算方法版本生成新修订，不自动换用当前档案或最新算法。未来若允许迁移算法，必须是显式操作并保留新旧结果。
- **已确认规则：** 训练与餐食保留实际发生时间和当时时区。跨午夜训练默认归属开始时所在自然日，也允许用户显式更正归属日；同一次训练不得重复进入两个日期的联动。

### 3.4 缺失、零值和不确定性

- **继承规则：** 数据不完整时应用继续可用，同时说明相关统计或建议受到的影响。
- **已确认规则：** “未记录”“未知”“待分析”“估算为零”和“用户确认为零”是不同状态，不能互相替代。
- **已确认规则：** 只汇总已知值；当关键数据缺失时，汇总必须同时标记完整性，不能把部分合计展示成完整摄入。
- **已确认规则：** 当日汇总分别记录“已保存记录的处理完整性”和“全天摄入覆盖状态”。前者说明已保存餐食是否都有当前采用值；后者默认未知，因为系统不能从缺少记录推断用户没有进食。只有用户自愿明确确认时才能标为“用户确认已覆盖全天”，该确认不是必填，也不构成打卡要求。
- **已确认规则：** 能提供合理范围时，估算同时保留中心值与范围；不能提供范围时，至少保留置信等级、限制说明或未知因素。

### 3.5 来源、版本和修订

- **已确认规则：** 营养规划使用的每个公式、数值阈值、适用范围和安全边界都必须关联 `docs/domain/calculation-evidence.md` 中的证据记录，并能定位到官方文档的发布机构、标题、版本或日期及具体章节、表格或公式；只有二手摘要、博客或无来源惯例时不得进入自动规划核心。
- **已确认规则：** 产品自行形成的周预算守恒、日分配或约束求解规则必须列明官方输入依据和完整推导，标记为“产品推导”，不得表述成官方机构的直接结论。
- **已确认规则：** 计算结果保留输入快照、计算方法、方法版本、证据记录 ID、数据来源版本、生成时间和主要假设。
- **已确认规则：** 用户修正生成新修订，不抹掉模型原始估算或旧采用值；当前统计只读取被标记为当前的采用值，其历史修订仍可追溯。
- **已确认规则：** 引用的动作内容、食物营养数据和计算方法都要保留来源与许可状态，来源变化不能悄悄改写历史。

## 4. 核心业务词汇

本节只定义词汇。词汇中超出 `docs/product.md` 的粒度已经随 Phase 2 整体规则一并确认，其约束仍以对应规则章节为准。

### 4.1 个人与目标

| 概念 | 含义 |
|---|---|
| 用户账号 | 一名用户在系统中的独立身份及数据边界 |
| 当前个人档案 | 当前用于新建议和计算的身体与生活信息，如身高、出生日期、基础活动水平及公式确实需要的生理信息；页面不提供通用档案版本管理 |
| 身体测量记录 | 用户在某个时间记录的体重、腰围或其他身体测量事实；修改通过修订保留历史 |
| 当前健身目标 | 当前用于新建议和计算的目标与偏好；其中体重策略与训练或体成分意图分开保存，并记录本次开始使用的日期 |
| 当前饮食策略 | 用户当前在系统支持且证据适用的选项中作出的选择，如体重策略、训练期宏量分配偏好；它是系统计算输入，不是用户手填的营养数值目标 |
| 计算资格 | 在当前信息完整度下，某项建议能否计算；一项不能计算不影响其他功能 |
| 规划证据记录 | 一项规划公式、范围或安全边界所依据的官方文档及精确位置、适用范围、验证状态和版本 |

### 4.2 训练

| 概念 | 含义 |
|---|---|
| 系统训练建议 | 系统根据个人情况和目标提出的可编辑候选；采用或编辑时复制为用户计划，不在训练主页与计划并列 |
| 用户训练方案 | 用户自主建立的可复用动作安排；同一用户可以同时保存多个不同目的的方案，方案不必绑定固定星期或日期 |
| 训练周期计划 | 用户按多个周次和训练日组织的复杂训练安排；各周可以不同，每个训练单元可以独立编辑或从单次方案导入 |
| 周期训练单元 | 周期计划中某一周、某一训练日的安排；导入单次方案时保存副本及来源，不与源方案实时联动 |
| 当次计划训练单元 | 用户为某日提前安排或在训练开始时选中的方案、训练主题、动作和预期训练量；只有它参与当天剩余训练与临时活动参考 |
| 实际训练记录 | 用户真正完成、跳过、替换或临时增加的训练事实；计划内记录关联计划项，额外记录不要求关联计划 |
| 训练日汇总 | 某日长期保存的实际训练动作、完成量、消耗估算和完整性；必要时附带本次训练的计划上下文 |
| 动作定义 | 动作名称、训练部位、器械、计量方式等通用知识 |
| 动作指导内容 | 帮助理解动作的视频、要点、常见错误和必要限制，不构成绝对安全保证 |
| 运动消耗估算 | 基于实际活动与个人信息计算的近似能量消耗 |

### 4.3 饮食与营养

`docs/product.md` 中的每日营养数值在领域层统一称为“系统每日营养参考”。用户选择饮食策略作为计算输入，不另外创建数值目标；“当前采用值”只指餐食营养结果，不指每日参考。

| 概念 | 含义 |
|---|---|
| 用户饮食计划 | 用户对一周、一天或餐次准备如何吃的自主安排，可以只有文字意图，也可以包含餐次、食物或营养分配 |
| 系统每日营养参考 | 系统根据当日有效个人信息、目标和活动上下文计算的热量及营养素参考 |
| 饮食策略 | 用户从系统提供且证据适用的选项中选择的规划方向或范围内偏好；系统据此生成每日营养参考，用户不直接填写结果数值 |
| 餐食记录 | 某次实际进食；可以只含照片、只含手工条目，也可以混合 |
| 食物条目 | 餐食内的一种食物、饮品或组合菜品及其份量和营养值 |
| 营养向量 | 一组按已确定营养项保存的数值及其单位、已知状态和来源 |
| 常用食物 | 可重复使用的单个食物参考模板 |
| 常用餐食 | 可重复使用的一组食物与默认份量模板 |
| 当日营养汇总 | 当日当前采用值的合计、完整性、系统每日营养参考、剩余量或偏差 |
| 历史趋势 | 按日期汇总已经记录的数据，用于回顾变化；不把缺少记录推断成真实零值或健康结果 |

### 4.4 图片分析

| 概念 | 含义 |
|---|---|
| 餐食照片 | 为分析、重试和用户复核而可靠暂存，并归属某一餐食的临时原始图片；不是长期历史记录 |
| 图片说明 | 用户可选填写的份量、隐藏食材、烹饪方式或实际食用比例说明 |
| 分析任务 | 用户针对一组确定的照片、说明和分析目的明确发起的一次图片估算请求 |
| 分析尝试 | 分析任务的一次具体执行；同一输入失败后的重试产生新尝试而不覆盖旧尝试 |
| 模型原始估算 | 分析执行者返回的候选食物、份量、营养、假设和不确定性原文或结构化结果 |
| 当前采用值 | 当前实际计入餐食和当天统计的营养结果，可以来自模型暂定结果、手工录入、用户确认或用户修正 |
| 采用值审核状态 | 当前采用值是“暂定”还是“已确认”；暂定值计入统计，但必须保持可识别且不能冒充已核实结果 |

## 5. 个人档案、目标与每日参考

### 5.1 信息使用

- **已确认规则：** 只收集当前建议或计算确实需要的信息；敏感或不适用的信息允许不填。
- **已确认规则：** 某套公式缺少必要输入时，只暂停该项计算并说明缺少什么，不使用虚构默认值；这不阻断计划和记录。
- **已确认规则：** 新建议和当天新计算读取当前个人档案、当前目标与当前策略；历史结果及历史修正读取该结果已经保存的输入快照，而不是用今天的当前值替换过去依据。
- **已确认规则：** 体重、腰围和其他身体测量按实际测量时间保存；新增测量不覆盖旧记录。用于某日计算的体重来自该日可用的相应测量记录，并显示其测量日期，用户不需要每天称重；用户修正误录值时生成修订，不制造一次新的身体变化。
- **已确认规则：** 体重记录默认提供每周一次的柔性提醒，依据见 E-010、E-015 和 DERV-006；这是可调整的产品默认值，不是适用于所有人的医学处方。用户可以修改周期、暂缓或关闭，忽略提醒不产生惩罚状态。
- **已确认规则：** 超过用户选择的更新周期仍未记录时，依赖体重的当前计算继续采用最近一次有效记录，并同时显示采用值及测量日期；不得静默假设体重没有变化，也不得因资料“过期”阻断训练、饮食和记录功能。
- **已确认规则：** 身高、出生日期等相对稳定档案字段不使用统一周期强制复核；用户在信息实际变化时更新。身体测量提醒与训练提醒、饮食提醒相互独立。
- **已确认规则：** 页面主要展示当前个人档案、当前目标、当前策略和身体测量趋势，不提供通用的“档案第几版”或“目标第几版”浏览。过去使用了什么值由对应建议、每日参考和消耗估算的输入快照解释。
- **已确认规则：** 每套营养或消耗计算方法都要依据官方文档声明适用年龄、生理状态、必要输入和不适用条件；资格不满足或未知时不生成该方法的个性化数值，只保留记录能力、适用的一般人群参考和一般性说明。
- **已确认规则：** 当前能量规划明确区分维持、减重和增重三种体重策略；训练表现、力量、增肌、体态或其他目标作为独立且可扩展的训练或体成分意图保存。“增肌”不能自动等同于“体重增加”，未被系统识别的自由目标仍可保存且不阻断手工计划与记录。

### 5.2 系统参考和饮食策略

- **继承规则：** 系统根据身体情况和目标提出训练建议与每日营养参考，用户拥有最终选择权。
- **已确认规则：** 系统每日营养参考是可追溯的计算输出，用户不直接篡改该输出。
- **已确认规则：** 系统每日营养参考是饮食汇总唯一的数值比较基准；产品不提供独立的用户手填营养目标，也不允许用任意数值覆盖系统参考。
- **已确认规则：** 用户可以选择系统提供的体重策略、训练或体成分意图，以及有官方依据的宏量分配偏好。每个选项必须声明证据、适用资格和允许范围；选择变化会生成新的系统参考修订，而不是直接修改某个结果数字。
- **已确认规则：** “增肌期偏高蛋白”只在 E-006、E-007 等证据适用的健康规律运动成人范围内提供，并保持在已登记范围内；“减脂期较低脂肪”只表示依据 E-001、E-002、E-006 和 DERV-005 在适用脂肪范围内选择较低分配，不得降到范围以下，也不能牺牲蛋白质、必需脂肪或总能量约束来制造达标。
- **已确认规则：** 系统参考无法计算时，用户仍可记录餐食和摄入；页面可以显示适用的一般人群参考或相邻范围，但不能要求用户手填一个数字来伪装成系统计划。
- **已确认规则：** 训练建议只有在用户主动采用、编辑或复制后才进入用户训练计划；建议内容作为来源快照保留，训练主页随后只围绕用户计划和实际记录工作。
- **已确认规则：** 系统训练建议保留生成时使用的个人档案、身体测量、目标、策略和规则输入快照及生成时间；相关当前信息变化后，旧建议仍可查看但标记为可能过期，不被伪装成按当前信息新生成的建议。

### 5.3 参考值组成

- **已确认规则：** 系统每日能量与营养目标的准确性优先于餐食识别的准确性和当前实现便利。计算应选择与用户、目标和生理状态相符的可靠依据，并正确处理输入、单位、适用范围和活动重复计算；图片结果的不确定性不能成为降低规划规则质量的理由。
- **已确认规则：** 每日营养参考区分维持能量、目标策略调整、计划活动临时调整和实际活动修正，避免只保存一个无法解释的总数。
- **已确认规则：** 能量规划先形成周预算，再按当天训练净消耗相对周日均训练净消耗的差额分配到每天：`周能量预算 = 7 × 维持能量参考 + 周目标策略调整`；`某日规划目标 = 周能量预算 ÷ 7 + 当日训练净消耗参考 − 该周日均训练净消耗参考`。
- **已确认规则：** 在训练计划按原安排完成时，七日规划目标总和必须等于周能量预算；该日分配公式属于基于官方能量与活动数据形成的产品推导，不冒充官方直接结论。
- **已确认规则：** 无稳定周训练参考时仍可输出稳定每日目标，但不伪造训练日与休息日差异；形成计划或典型周以后才能启用日分配。
- **已确认规则：** 计算策略必须声明基础活动水平是否已经包含习惯训练；相对周均值的日分配只使用净活动差额，防止同一训练同时进入 PAL 和额外消耗。
- **已确认规则：** 活动变化首先调整能量；蛋白质先独立确定并保持稳定，脂肪保持适用范围，碳水承担满足总能量后的剩余。约束冲突时报告规划不成立，不静默压低某项继续生成伪达标计划。

## 6. 训练领域规则

### 6.1 系统建议与用户计划

- **继承规则：** 用户可以参考、修改或完全忽略系统训练建议，自主安排每周和每天练什么。
- **已确认规则：** 系统训练建议中的频率、强度、组数、次数或周训练量与营养规划数值采用相同证据门槛：必须引用 `docs/domain/calculation-evidence.md` 中达到“可实现”的适用证据，并保留目标、训练经验、可用器械、时间偏好、健康资格和方法版本；没有适用依据或必要输入时只给一般说明，不生成伪精确计划。
- **已确认规则：** 对一般健康中国成人，E-013 只作为总体活动基线：每周 150–300 分钟中等强度或 75–150 分钟高强度有氧运动、每周 2–3 次抗阻训练。它用于检查系统建议是否明显偏离公共健康边界，不变成用户必须完成的任务，也不代替目标特异的抗阻安排。
- **已确认规则：** 对符合 E-014 适用范围的健康成人，抗阻建议从覆盖主要肌群、每周至少 2 天且能够持续的简单安排开始，并按目标形成可修改候选：力量目标可参考约 80% 1RM、每个动作 2–3 组；肌肥大目标可参考每个肌群每周约 10 组；功率目标可参考 30%–70% 1RM 并强调向心阶段快速发力。具体动作、日程和负荷仍按经验、器械、偏好和安全条件调整。
- **已确认规则：** 只有用户已有可靠 1RM 或等效负荷参考时，系统才把百分比换算成具体重量；否则保留相对负荷描述或让用户记录实际可完成重量，不要求为生成计划先做最大重量测试。练到力竭、固定器械类型和复杂周期化不作为普通健康成人默认要求。
- **已确认规则：** 65 岁以上、疾病、伤病、孕期或哺乳期、竞技专项及其他超出 E-013/E-014 适用范围的用户，不套用上述自动参数；仍可手工制定计划、保存专业人员方案和记录实际训练。
- **已确认规则：** 用户计划分为单次训练方案和训练周期计划。单次方案保存一次训练的可复用动作组合；周期计划按任意周数和训练日组织多个训练单元，各周可以完全不同。两者共用动作与目标量编辑能力，但简单用户不需要进入周期编辑器。
- **已确认规则：** 用户可以同时保存多个彼此独立的单次方案。方案可以从空白创建、从系统建议采用或编辑、从已有方案复制，并可按训练部位、目标、器械条件、预计时长或用户自己的方式命名和选择；复制后归用户所有。
- **已确认规则：** 多个单次方案表达不同目的和选择，不是同一方案的历史版本。每个方案只维护当前内容并可以归档，不建立面向用户的编辑版本历史；已经产生的实际训练不得被后来编辑方案覆盖。
- **已确认规则：** 周期训练单元可以独立编辑，也可以从某个单次方案导入。导入时复制方案名称、动作、顺序和目标量，并保留可选来源关系及导入时间；此后源方案变化不会自动改写周期训练单元。
- **已确认规则：** 用户可以对尚未开始的周期训练单元主动执行“重新导入”，用源方案当前内容替换该单元；操作前必须说明会覆盖该单元的本地修改。已经开始、完成、放弃或取消的当次训练及其快照不能被重新导入改变。
- **已确认规则：** 单次方案不必绑定每周固定日期。用户可以提前为某日安排一个具体方案或训练主题，也可以到训练开始时临时选择任一方案，或从空白开始；方案库和周期计划中未被安排或选择的内容不属于当天未完成任务，也不进入当天临时活动参考。
- **已确认规则：** 用户采用或编辑系统建议时生成一个独立训练方案；放弃建议不会产生方案。建议来源只在创建、重做或查看来源时出现，不作为训练主页上的第三套安排。
- **已确认规则：** 一次实际训练以单次方案或周期训练单元为起点时，只把解释本次完成、跳过、替换和额外动作所需的来源名称、周次或训练日、动作名称、顺序及目标量保存为当次训练快照；不为此长期复制整个方案库或周期计划。历史页面默认围绕实际做了什么，计划快照只作为详情中的辅助上下文。
- **已确认规则：** 取消当天已安排或已选择的训练单元，表示本次原先打算训练但后来取消；它不是已完成训练，也不删除或归档可复用训练方案。

### 6.2 实际训练记录

- **继承规则：** 用户在训练时录入真正完成的动作和训练量，临时变化只进入实际记录。
- **已确认规则：** 实际记录允许进行中、已完成和已放弃；已放弃仍可保留已经完成的动作。
- **已确认规则：** 每个计划动作具有独立执行状态。用户勾选完成时生成或更新一条关联该计划项的实际记录；跳过或替换同样记录原因与对应关系，但不修改计划内容。
- **已确认规则：** 计划外临时增加的动作作为额外实际记录保存，不要求伪造一个计划项；同一实际动作不得同时作为计划项完成和额外动作重复统计。
- **已确认规则：** 训练未开始时使用计划活动形成临时参考；训练进行中时按“已完成实际量 + 尚未完成的剩余计划”形成不重复的参考；训练完成或放弃后，以最终实际记录替代该训练单元的计划活动。
- **已确认规则：** 不同动作按适合的方式记录，例如组次与重量、持续时间与距离或用户备注；不强迫所有动作使用同一组字段。
- **已确认规则：** 用户可以修正动作、组次、重量、时间和完成状态；修正后重新计算该日训练汇总与消耗估算。

### 6.3 动作指导

- **继承规则：** 动作指导需要帮助用户理解动作示范与要点。
- **已确认规则：** 指导内容区分来源、许可、版本和审阅状态；无法确认公开使用许可的内容不能随开源项目直接分发。
- **已确认规则：** 指导可说明常见错误、适用器械和替代动作，但不能宣称对所有人绝对标准或绝对安全。

### 6.4 运动消耗估算

- **继承规则：** 消耗结合个人体重等情况和实际训练内容作近似估算。
- **已确认规则：** 有实际记录时使用实际动作、时长或训练量；没有实际记录时，计划只能生成临时活动参考，不能记为已消耗。
- **已确认规则：** 对适用的 18–64 岁健康中国成人，活动能量来源优先使用 T/CSSS 002—2023；未覆盖活动才使用明确适龄的官方或同行评审补充来源，无法找到适用依据时不输出伪精确数字。
- **已确认规则：** 每次估算分别保存总活动消耗和扣除同一时段静息消耗后的净活动消耗；日目标调整只使用净活动部分，并保留活动分类或 MET 映射、时长、采用体重、公式、证据 ID、版本和限制说明。
- **已确认规则：** 当动作、时长、体重或映射不足以支持合理估算时，估算状态为“无法可靠估算”，不生成消耗数字；训练记录本身仍然有效。
- **已确认规则：** 力量训练、间歇训练和无法准确计时活动只有在动作、强度与时长能够映射到达到“可实现”状态的证据时才估算；无法映射时保留训练量并返回“无法可靠估算”。具体页面可以帮助补充输入，但不能在领域层预置无依据的 MET 或误差范围。

## 7. 饮食领域规则

### 7.1 用户饮食计划

- **继承规则：** 饮食计划由用户自主决定，系统每日营养参考只作参考。
- **已确认规则：** 饮食计划是可选的意图层，可以按周、日或餐次记录；允许只写原则，也允许细化为食物、份量或某餐的营养分配。
- **已确认规则：** 计划餐食和实际餐食分开；把计划复制为实际记录需要用户主动操作，之后互不覆盖。
- **已确认规则：** 没有饮食计划不影响记录、图片分析、系统每日营养参考或汇总功能。

### 7.2 餐食与食物记录

- **继承规则：** 照片、食物库搜索选择、手工录入、常用食物和常用餐食都可以形成餐食记录。
- **已确认规则：** 新建餐食只需要发生时间或所属日期；照片、餐次名称、文字说明和营养明细均可稍后补充。
- **已确认规则：** 食物库搜索可以返回食物、菜品、包装食品或用户自建条目；用户选择具体条目与实际份量后形成当餐快照，搜索结果本身不直接计入摄入。
- **已确认规则：** 手工输入的营养值在保存后直接成为已确认的当前采用值，并标记为用户输入。
- **已确认规则：** 常用模板被使用时复制一份当餐快照；后来修改模板不改写历史餐食，当餐份量可以独立调整。
- **已确认规则：** 每个营养值保留单位和份量基准；未提供的营养素保持未知，不能补零。
- **已确认规则：** 当前采用值是餐食当前有效的营养快照，可以由多个互不重叠的贡献项组成；贡献项可以来自采用的模型候选、手工食物条目或用户直接输入的整餐总量，每项都要声明覆盖范围，禁止同一食物或整餐总量重复计入。
- **已确认规则：** 用户直接输入整餐总量时，需要明确它是替代其他条目汇总，还是只补充尚未覆盖的部分；具体确认方式属于 Phase 3。

### 7.3 当日营养汇总

- **继承规则：** 只有当前采用值计入统计；修正后对应餐食和日期重新汇总。
- **已确认规则：** 汇总同时给出已记录摄入、系统每日营养参考、剩余量或超出量、待分析餐食数量和数据完整性；剩余量按“系统参考减去当前已记录摄入”计算。
- **已确认规则：** 暂定采用值与已确认采用值都计入当日汇总，但汇总保留两者的组成和数量；暂定值不能被表述为已经核实。
- **已确认规则：** 存在没有当前采用值的餐食时，已知营养仍可合计，但结果必须标为部分汇总。
- **已确认规则：** 所有当日合计和偏差都表示“基于已记录内容”；即使已保存餐食全部处理完成，全天摄入覆盖状态仍默认未知，除非用户自愿确认已覆盖全天。
- **已确认规则：** 首批每日规划和持续汇总的核心营养项为能量、蛋白质、碳水化合物和脂肪。膳食纤维、饱和脂肪、糖、钠及其他营养素在来源提供时继续保存并可查看，但在取得对应目标依据前不生成虚假的个性化达标结论；数据结构不得限制以后增加营养项。
- **已确认规则：** 用户删除餐食后，它不再参与当前汇总；删除与修订的可恢复期限属于后续技术与体验设计。

### 7.4 历史回顾

- **继承规则：** 用户可以回顾历史训练、饮食和营养变化趋势。
- **已确认规则：** 历史趋势使用各日期当前有效的用户记录修订和采用值，并保留该日期当时有效的档案、目标、数据来源与计算方法快照；后来档案或目标变化不触发回算。
- **已确认规则：** 趋势同时保留日期的数据覆盖状态；未记录日期与记录为零的日期分开，不能据此推断用户真实摄入、训练完成度或身体结果。

## 8. 图片分析与结果采用

### 8.1 照片和任务相互独立

- **已确认规则：** 手机上传后照片先被可靠暂存并关联到餐食；当前主要由应用调用已配置的多模态 API 异步分析，Codex 保留为服务不可用、结果复核或特殊餐食处理时的人工补偿方式。照片本身不是长期餐食记录。
- **已确认规则：** 照片保存成功不等于分析成功；餐食、照片、分析任务和营养结果分别拥有自己的状态。
- **已确认规则：** 一餐可以包含多张照片；一次任务明确使用了哪些照片版本和哪段可选说明。
- **已确认规则：** 用户确认或修正到满意结果后，原图进入异步删除流程；用户放弃分析时也删除原图。模型原始估算、当前采用值和采用值修订继续保留，除非用户另行删除对应餐食或数据。
- **已确认规则：** 暂定采用值不等于用户已经满意；在用户明确确认、修正或放弃前，照片在有效临时保留窗口内继续可供复核和重试。超过窗口的自动清理必须保留结构化结果并清楚表达原图已删除，具体时长属于技术配置。
- **已确认规则：** 用户主动删除照片后，照片不能再用于新分析；已采用的营养记录不能因此悄悄一起消失。
- **已确认规则：** 照片、餐食或账号进入删除流程后，引用它们且尚未完成的分析尝试必须取消或使结果失效；后来到达的外部结果不能恢复已删除对象，也不能进入汇总。
- **已确认规则：** 分析执行者每次只能读取任务明确授权的照片版本和可选说明；任务、照片、餐食、原始结果与当前采用值必须属于同一账号，跨账号读取、关联或回填无效。
- **已确认规则：** 用户完成多模态服务配置并明确授权照片发送范围后，自动分析默认开启；用户能随时关闭并改为手工发起。开关变化只影响后续照片，不自动批量分析历史照片，也不影响照片保存、手工录入或其他功能。

### 8.2 餐食分析、任务与尝试状态

- **已确认规则：** 餐食层不使用一个互斥状态概括全部分析历史，而是分别派生“是否存在可用候选”“是否存在等待中的任务”和“最近一次任务结果”。已有旧候选与最近一次任务失败可以同时成立；从未创建任务时最近一次任务结果为空。
- **已确认规则：** 自动触发或用户手工发起的每次分析请求都创建一个任务。任务引用一组确定的照片、说明和分析目的；照片、说明或分析策略发生变化后再次判断，应创建新任务。
- **已确认规则：** 同一任务可以包含多次执行尝试。因暂时错误对相同输入重试时，在原任务下新增尝试，不创建相同任务，也不覆盖旧尝试。

**已确认规则：** 分析尝试采用以下业务状态：

| 状态 | 含义 | 允许的下一步 |
|---|---|---|
| 等待处理 | 尝试已经创建，尚未开始执行 | 开始处理、取消 |
| 处理中 | 分析执行者正在读取被授权输入并生成结果 | 成功、失败 |
| 已成功 | 本次尝试已经产生模型原始估算 | 查看、采用、修正；输入变化时新建任务 |
| 已失败 | 本次尝试未产生可用结果并保留原因 | 在原任务下重试、手工录入、保持失败 |
| 已取消 | 用户取消尚未完成的尝试 | 在原任务下重试或以新输入创建任务 |

- **已确认规则：** 任务的当前状态由最近一次尝试派生；任务结果、餐食分析状态、旧错误和原始输出都不能覆盖旧尝试。
- **已确认规则：** 分析失败不删除餐食，也不阻止手工录入；原图在有效临时保留窗口内继续用于重试，用户放弃或窗口到期后进入删除流程。
- **已确认规则：** 更换模型 API、切换到本地模型或使用 Codex 人工补偿时沿用同一业务状态和结果边界；供应商名称不是餐食记录和当前采用值的业务身份。

### 8.3 原始估算、采用和修正

- **继承规则：** 模型原始估算与当前采用值都保留，当前采用值才计入统计。
- **已确认规则：** 模型成功后先永久保留模型原始估算。餐食尚无当前采用值时，该结果自动成为“暂定采用值”并计入统计；系统不得把暂定状态表述为用户已经核实。
- **已确认规则：** 用户可以确认、修改或拒绝暂定采用值。确认保留数值并把状态改为已确认；修改生成新的已确认修订；拒绝后该结果不再作为当前采用值，但模型原始估算仍保留。
- **已确认规则：** 餐食已经存在手工值或已确认采用值时，新模型结果只形成候选，不得自动覆盖；重新分析同样只产生新候选，除非用户主动切换当前采用值。
- **已确认规则：** 采用或修正生成新的采用值修订，记录它基于哪个模型结果或手工输入；旧采用值仍可追溯。
- **已确认规则：** 模型原始估算至少保留候选食物、份量假设、营养点估计、可用的范围或置信说明、隐藏食材风险、所用营养来源和模型版本。
- **已确认规则：** 图片候选份量注明它估计的是照片中可见的盛取量；当前采用值应尽量表示实际食用量。实际食用比例未知时，保留“按照片中盛取量估算”的假设并降低结论强度；用户可稍后补充剩余量或食用比例，生成新修订。
- **已确认规则：** 分析尚未成功、暂定值被拒绝且没有其他采用值，或用户没有录入任何结果时，该餐仍然存在但营养状态为待补充，日汇总标记不完整。

## 9. 训练与饮食的当日联动

### 9.1 联动对象

- **继承规则：** 联动调整的是当天饮食参考，不是训练计划、训练提醒或已经发生的摄入。
- **已确认规则：** 当天活动上下文分别保存计划活动参考和实际活动事实，二者不能合并成一个来源不明的数字。
- **已确认规则：** 训练开始前只用当天已经明确安排或选择的训练单元形成临时营养参考；方案库中未选择的方案不参与计算。实际训练开始或记录变化后，当前参考依次使用已完成实际量与本次剩余动作，完成、放弃或取消后使用最终实际信息。
- **已确认规则：** 若计划取消、实际少练或多练，当日参考按净活动差额相应修正，但已吃的食物和原计划历史保持不变。
- **已确认规则：** 活动变化只生成新的系统每日营养参考修订；饮食策略选择保持不变，除非用户主动更改。页面用最新系统参考重新计算剩余量或偏差，不改写已经发生的摄入。

### 9.2 训练时间与当日持续展示

- **已确认规则：** 用户可以在一天中的任何时间训练。计划时间属于用户意图，实际开始和结束时间属于训练事实；系统不得把晚上设为默认训练时段。
- **已确认规则：** “今天”持续展示最新系统每日营养参考减去当前已记录摄入的结果：结果为正时显示还可以摄入多少，结果为负时显示基于已记录内容已经超出多少。
- **已确认规则：** 记录实际训练后只需更新系统每日营养参考修订，并用同一规则刷新剩余或超出量；界面不要求用户回答当天是否还会继续进食，也不单独展示训练与饮食联动说明。
- **已确认规则：** “今天”同时按用户计划与实际完成关联，持续展示尚未完成的计划项。该结果表示计划执行进度，不是需要完成的运动消耗或热量配额。
- **已确认规则：** 如果当天餐食记录不完整，剩余或超出量必须说明只基于已记录内容，不能把未记录内容按零处理。

### 9.3 联动计算不变量

- **已确认规则：** 同一训练不能同时作为基础活动和额外训练被重复加入。
- **已确认规则：** 计划训练不能写入实际消耗；实际训练出现后，计划只保留为意图与比较基准。
- **已确认规则：** 按原周计划完成时，每日训练净消耗相对周日均值的差额之和必须为零；训练取消、少练或多练只通过实际与参考的差额改变对应日期目标。
- **已确认规则：** 联动只重算当日当前参考；后来体重、目标或规则版本变化不重算过去日期。
- **已确认规则：** 修改某日实际训练或餐食是对该日事实的主动修正；该日汇总使用当日快照和当时方法版本生成新修订，并保留来源。

## 10. 提醒领域规则

- **继承规则：** 训练提醒和饮食提醒彼此独立，用户分别决定是否开启及时间。
- **已确认规则：** 身体测量提醒是独立的第三类提醒，不归入训练完成提醒或饮食偏差提醒；每类提醒分别设置、暂缓和关闭。
- **已确认规则：** 训练提醒只围绕计划训练、开始训练或计划变化；饮食提醒只围绕营养目标、餐食记录状态和当前偏差。
- **已确认规则：** 提醒引用估算或不完整数据时要带上相同的不确定性，不能把“可能偏高”升级成确定的“已经超标”。
- **已确认规则：** 用户忽略提醒不会改变计划、目标或记录，也不会产生惩罚状态。
- **已确认规则：** 提醒触发的业务条件在本阶段定义；通知渠道、系统权限、页面入口和节流实现属于 Phase 3 与 Phase 4。

## 11. 计算与营养数据规则

### 11.1 每日营养参考

- **已确认规则：** 输入至少区分计算当时的当前个人档案、相应身体测量、目标、策略、基础活动、计划训练和实际训练，并把实际使用值保存到结果快照；具体公式只使用官方文档明确要求的输入。
- **已确认规则：** 输出区分能量和各营养素，保留单位、适用人群、方法版本、证据 ID、来源与限制。
- **已确认规则：** 每套正式采用的规划算法必须能够用公开依据、公式测试样例和边界样例复核；来源选择、公式转写、单位换算、舍入、活动去重和版本应用错误都属于规划计算缺陷，不能用“结果只是估算”免责。
- **继承规则：** 参考值属于一般健康与健身辅助估算，不是处方；个人实际需要可能高于或低于结果。
- **已确认规则：** 中国一般成人的基础能量和宏量营养基线优先采用中国营养学会 2023 DRIs；国家卫健委现行指南用于其明确覆盖的体重管理人群。National Academies 2023 EER、NIDDK 动态体重模型和国际专业组织正式立场用于补足与交叉核验，不与中国参考静默取平均。
- **已确认规则：** PAL 候选必须根据用户的惯常职业活动、日常移动和规律训练整体判断，并对照 E-004 第 7 章表 7-1 的活动模式；不能仅凭是否达到健康运动建议、单一日步数或一次训练直接确定 PAL。
- **已确认规则：** PAL 结构化输入分为三组：惯常工作或学习时的坐、站、移动和体力劳动模式；日常生活中通常发生的步行、家务和通勤；每周反复出现的训练或高强度休闲活动及其频率、时长和强度。三组输入分别保存，系统匹配 E-004 表 7-1 及第 148–149 页描述时必须去除同一活动的重复计入，不把某个单项直接换算成 PAL 分数。
- **已确认规则：** 当前没有官方认可的简便工具能够准确分类个人 PAL。活动资料清楚时系统可以提出可修改的候选档位；资料同时符合相邻档位或明显不完整时，展示相邻方程形成的参考范围，不伪造单一精确值，也不阻断训练与饮食记录。
- **已确认规则：** 输入不足以形成候选时不输出单一的个体 EER；可以展示中国 DRIs 对应人群参考和相邻 PAL 方程范围，并明确还缺少哪些输入。用户不补充仍可正常使用训练、饮食和体重记录。
- **已确认规则：** 初始 EER 必须显示采用的 PAL 候选及相邻档位差异。体重记录用于显示随时间的实际方向，并作为用户以后调整目标的依据；当前规则不设没有官方直接依据的统一观察天数、波动容差或自动调整步长，系统不得自动改写当前目标或历史。未来若引入自动校准，必须另行登记官方依据、适用范围和验证样例。
- **已确认规则：** 维持体重时周目标策略调整为零。减脂时只有在官方指南适用资格内才能采用其目标速度、能量缺口和安全边界，并默认从官方范围的保守端开始；长期体重变化使用动态模型，不用固定能量密度直接线性外推。
- **已确认规则：** 当前没有同等级官方依据能够保证能量盈余转化为肌肉；增重规划必须把总体重路径与增肌目标分开，不承诺增加部分均为肌肉，也不生成全体用户通用的固定盈余默认值。
- **已确认规则：** 18 岁及以上、非孕期或哺乳期且不处于需要医疗处理状态的一般成人，只有在用户给出目标体重和目标期限后，才可用 E-005 的完整动态模型生成总体重路径与相应能量计划；实现前必须完成独立复算和许可审查。没有目标期限或无法使用完整模型时，只显示维持参考与正能量平衡原则，不用固定常数补位。
- **已确认规则：** BMI 低于 18.5 的用户先显示 E-010 的“排除疾病原因并逐渐增加至推荐水平或稍高”边界；在用户确认已完成适当评估前，不自动生成个性化增重能量目标。BMI 不能替代体成分或医学判断。
- **已确认规则：** 无规律训练时蛋白质采用中国 DRIs 对应人群参考；健康规律运动成人的范围必须来自正式专业组织文件。普通阻力训练的约 1.6 g/kg/日仅作为位于 1.4–2.0 g/kg/日正式建议范围内、并由系统综述交叉支持的产品参考中心，不表述成确定阈值。
- **已确认规则：** 蛋白质使用何种计算体重必须有适用人群的直接依据。肥胖、明显低体重或特殊体成分时，不无条件用实际体重相乘；依据未完成时停止自动输出该个性化蛋白质目标。
- **已确认规则：** 对中国成人 BMI 低于 18.5、达到 28.0 及以上，或用户声明存在显著特殊体成分、疾病、孕期或哺乳期时，不自动套用健康运动成人的 `g/kg 实际体重`目标，也不借用住院或疾病人群的理想体重、调整体重公式。系统仍可显示 E-001 对应年龄和性别的一般人群固定参考量，但不让用户手填数字冒充系统个性化规划。
- **已确认规则：** 脂肪使用适用官方范围，单点值若取范围中点必须标记为产品分配规则；在蛋白质与脂肪约束满足后，碳水承担剩余能量和主要训练日能量变化。三者无法与总能量同时成立时，返回约束冲突并修正目标策略。
- **已确认规则：** 当前范围不把 PAL、体重趋势、特殊体重蛋白质或增重盈余中缺少官方依据的数值做成隐藏默认值；上述项目分别采用“候选或范围 + 用户确认”“不自动校准”“停止运动蛋白质自动计算”和“目标体重 + 期限的完整动态模型”处理。

### 11.2 运动消耗

- **已确认规则：** 使用有证据记录的活动分类或 MET、实际时长和当日有效体重形成近似估算，并说明标准化数据不等于个人测量。
- **已确认规则：** 不具备合格来源或必要输入时不输出消耗数字，不能用默认值伪装精确。
- **已确认规则：** 当前自动估算范围只包含能被 T/CSSS 002—2023 或其他达到“可实现”状态的直接来源可靠映射的活动。无法确定活动、强度或时长时保留训练事实并显示“无法可靠估算”；以后扩展来源不构成当前规则中的隐藏待定默认值。

### 11.3 食物营养

- **已确认规则：** 每条食物营养数据都要说明每 100 克、每份或用户输入份量的换算基准。
- **已确认规则：** 同名食物的地区、品牌、烹饪方式和数据类型可能不同；匹配结果需要可修正。
- **已确认规则：** 图片分析可以直接给出候选营养值，也可以先识别食物与份量再匹配营养数据源；无论采用哪条路径，都要保留各项结果的来源、版本和假设。
- **已确认规则：** 食物营养来源按具体性排序：用户录入或拍摄的当前产品营养标签；能够合法使用的 E-011 中国常见食物数据；其他已核验且许可兼容的数据；模型直接估算。更具体来源只替换它明确覆盖的贡献项，不能静默覆盖整餐。
- **已确认规则：** 预包装食品按标签所示的每 100 克、每 100 毫升或每份基准换算，并保留品牌、产品名称、标签版本或拍摄时间和实际食用量。执行时使用该日期有效的标签标准版本；E-012 的字段和单位可作为最新发布规则核对来源。
- **已确认规则：** 食堂混合菜无法可靠拆分时允许保存为“整道混合菜候选”，同时保留可见食材、烹饪方式、盛取量和隐藏油盐等假设；不能为了匹配数据库而伪造各食材克数。用户以后可以整餐修正或拆成互不重叠的贡献项。
- **已确认规则：** E-011 平台数据有明确版权声明。未取得批量使用和再分发许可前，只能作为来源核对和合法查询候选，不能把整库复制到公开仓库；若运行时也无法合法访问，则保留手工、包装标签和模型候选路径，不以内置盗版数据补位。

### 11.4 规划证据与算法验收

- **已确认规则：** 规划证据统一登记在 `docs/domain/calculation-evidence.md`；正式算法中的每个外部常数或范围必须引用一个已核验记录，产品推导同时引用全部输入记录。
- **已确认规则：** 只有证据状态为“可实现”的规则才能进入自动规划核心；“待取得原文”“待核对精确位置”或“只作补充”的资料不能单独支撑生产数值。
- **已确认规则：** 正式算法必须通过官方样例或独立复算、输入资格、单位与舍入、活动去重、周预算守恒、宏量能量守恒、结果可重现和边界场景八类验证。

## 12. 关键状态与冲突处理

本表是已确认规则的场景索引，不覆盖前文；如与前文冲突，以前文带等级的规则为准。

| 场景 | 当前规则 |
|---|---|
| 用户忽略系统训练建议 | 建议保留或可丢弃，不形成用户计划 |
| 用户采用或编辑系统训练建议 | 生成归用户所有的计划版本，训练主页不再并列显示原建议 |
| 用户临时换动作 | 原计划不变，实际记录保留与被替换计划项的对应关系并写真实动作 |
| 用户额外增加动作 | 形成不关联计划项的额外实际记录，不与计划项完成重复统计 |
| 计划训练取消 | 计划标为取消；取消本身不生成实际消耗。若有其他实际活动则按记录估算；若用户明确确认没有训练可记录为零，否则保持未知 |
| 训练少于计划 | 实际记录驱动新的当日参考，计划用于对比 |
| 照片上传但未发起分析 | 餐食有效存在，营养处于待补充；原图只在有效临时保留窗口内存在 |
| 分析失败 | 保留错误、旧结果和餐食；临时照片在有效窗口内允许重试，也可手工录入或放弃后删除原图 |
| 模型结果与手工值冲突 | 不自动覆盖；由用户选择当前采用值 |
| 用户修正采用值 | 新修订进入汇总，原始估算和旧修订保留 |
| 常用餐食模板后来改变 | 历史餐食快照不变 |
| 当日体重或目标改变 | 当天及以后参考重算，过去日期不回算 |
| 历史餐食或训练录错 | 用户主动修正该日事实，该日汇总随之更新 |
| 当天餐食没有记全 | 仍按当前已记录摄入展示剩余或超出量，同时明确未记录内容没有按零计算 |
| 多用户使用 | 档案、计划、记录、照片、任务和结果按账号隔离 |

## 13. 数据控制与多用户边界

- **继承规则：** 用户能够查看、导出、备份或删除自己的长期业务记录；部署和重启不能导致长期业务数据或仍在有效处理期内的临时照片意外消失。
- **继承规则：** 朋友阶段每个账号的数据默认相互隔离，不能访问他人的档案、训练、饮食或临时照片。
- **已确认规则：** 导出应包括用户原始记录、当前采用值、必要来源和时间信息；具体文件格式后移到技术设计。
- **已确认规则：** 长期导出和备份默认不包含原始照片；可以导出照片生命周期、分析任务和采用值的关联元数据。原图已经删除时仍须完整导出其余记录，不能把删除后的临时材料表述为数据缺失事故。
- **已确认规则：** 删除操作必须具有明确范围及影响集合，例如仅照片、单餐、某次训练或整个账号；删除前必须说明会从当前汇总排除什么、会取消哪些未完成任务、哪些已采用营养结果由用户选择保留或一并删除。确认方式和恢复机制后移到 Phase 3 与 Phase 4。
- **已确认规则：** 动作定义、获许可的动作内容和公共营养参考数据可以作为共享只读参考；用户计划、常用模板、餐食、临时照片、分析任务和采用值均为账号私有。主动公开或分享属于未来独立能力。
- **继承规则：** 照片、数据库、备份、凭证和用户记录属于私有运行数据，不进入公开 Git 仓库。

## 14. 健康、安全与内容边界

- **继承规则：** 产品只提供一般健身和营养辅助，不诊断疾病、不治疗、不替代医生或注册营养专业人员。
- **已确认规则：** 对缺少关键输入、特殊生理状态、伤病或明显超出规则适用范围的情况，系统应降低结论强度或不生成个性化数值，而不是猜测。
- **已确认规则：** 动作示范说明“如何理解和执行”，不能承诺某动作对所有用户安全；内容应允许提供限制条件和替代动作。
- **继承规则：** 系统每日营养参考、运动消耗和图片营养结果统一标为估算；“估算”不降低规划算法必须正确实现官方依据的要求。
- **已确认规则：** 系统每日营养参考和运动消耗同时保留官方证据记录、版本、适用范围、产品推导和不确定性；图片营养结果另行保留模型、营养来源、假设和不确定性。
- **已确认规则：** 不利用提醒、连续记录或超标文案制造羞耻、惩罚或医疗恐慌。
- **已确认规则：** 第三方动作视频、营养数据库和其他内容只有在许可允许时才能随开源版本分发；链接引用与内容再分发需要分别判断。

## 15. Phase 2 确认结果

### 15.1 分批确认的核心规则

产品所有者于 2026-08-23 确认以下领域规则，后续不再作为未知问题：

1. 用户完成多模态服务配置和照片发送授权后，自动分析默认开启；可以随时关闭并改为手工发起，开关变化不批量处理历史照片。
2. 餐食没有当前采用值时，模型结果自动成为计入统计的暂定采用值；统计必须保留其未经确认的状态。
3. 用户可以确认、修改或拒绝暂定采用值；模型原始估算始终保留。
4. 已有手工值或已确认采用值时，新模型结果和重新分析结果不得自动覆盖，只形成候选。
5. 系统每日能量和营养目标是独立的规划基准，必须优先保证依据、公式、输入、单位、边界和活动联动计算的正确性；图片营养识别的不确定性只影响实际摄入及其偏差判断，不能降低规划目标的准确性要求。
6. 营养规划采用“周能量预算 + 当日训练净消耗相对周均值的日分配”；按计划完成时七日总量守恒，无稳定周训练参考时不伪造日差异。
7. 训练前、训练中和训练后分别使用计划、已完成实际加剩余计划、最终实际形成不重复的当日参考；运动消耗优先采用适用的中国官方或专业标准。
8. 蛋白质先独立确定，脂肪保持官方适用范围，碳水承担剩余能量；约束冲突时停止生成伪达标计划并回到目标校正。
9. 维持、减脂、增重或增肌使用不同目标策略；减脂只在官方指南适用范围内使用其边界，增肌不得把总体重增加承诺成肌肉增加。
10. 所有规划公式、阈值、适用范围和安全边界必须由官方文档背书并登记精确出处；产品推导必须公开依据和过程，只有二手信息或惯例时不得进入自动规划核心。

### 15.2 随整体审阅确认的规则（摘要）

产品所有者于 2026-08-23 完成整体审阅，确认以下规则与其余正文规则。这里保留摘要用于快速定位，不替代正文：

1. 系统营养参考保持为不可直接修改的唯一数值比较基准；用户选择有官方依据的目标策略和范围内偏好，不另行手填营养目标。
2. 用户饮食计划允许从宽泛原则到具体餐次和食物，不强迫固定粒度。
3. 训练不预设发生在晚上；“今天”持续展示基于已记录摄入的剩余或超出量，以及用户计划中尚未完成的训练内容，不再询问是否还会继续进食。
4. 历史不回算不妨碍用户主动修正历史事实；修正后只更新被修改日期并保留修订痕迹。
5. 所有估算和外部内容都按来源与版本管理，未知营养项不按零处理。
6. 一餐的当前采用值允许由多个不重叠贡献项组成，混合模型与手工结果时必须明确覆盖范围并去重。
7. 图片份量默认表示照片中可见盛取量；实际食用比例未知时降低结论强度，允许以后按剩余量修正。
8. 当日合计永远基于已记录内容；全天摄入覆盖状态默认未知，用户自愿确认完整不是必填。
9. 跨午夜训练默认归属开始日，同一次训练不重复进入两天联动。
10. PAL 只根据惯常工作、生活移动和规律活动模式形成可修改候选或相邻范围；不采用步数阈值或自动趋势调参。
11. 一般成人增重使用目标体重、目标期限和完整动态模型；没有这些输入时不生成固定盈余，增肌意图不等于体重增加。
12. 低体重、肥胖、特殊体成分或特殊健康状态不自动套用健康运动成人的实际体重蛋白质公式；仍可查看适用的一般参考和记录实际摄入，但不以手填数字替代系统规划。
13. 中国常见食物、包装标签和模型估算按来源具体性管理；食物成分平台未获许可时不随开源项目复制整库。
14. 身体测量按时间保存并保留修订，不要求每日称重；照片或餐食删除后，未完成分析必须取消或失效，迟到结果不得恢复对象。

## 16. Phase 2 完成检查

本文已经为所有已发现的数值缺口给出“有依据计算、输出范围或停止自动计算”的明确路径，不以隐藏默认值占位。Phase 2 按以下条件完成检查：

- 核心概念没有同名不同义，计划、事实、估算和采用值不会互相覆盖；
- 每项主要计算能够说明输入、输出、来源、版本、不确定性和无法计算时的行为；
- 每个规划公式、阈值、适用范围和安全边界都已在证据登记表中定位到官方原文，状态达到“可实现”；
- 训练、饮食、图片分析和当日联动的正常、缺失、失败、重试与修正路径都能解释；
- 用户自主性、历史不回算、多用户隔离和健康安全边界没有被实现便利性削弱；
- 会改变主要业务流程的领域歧义已经解决；具体页面与交互差异留到 Phase 3，技术与版本差异留到 Phase 4。

上述条件已经满足，Phase 2 完成。后续若产品意图或官方依据改变，应通过显式修订更新本文件，不把已确认规则重新当作未知问题。
