# 原型验收记录 · 2026-09-18

范围：独立 Vue 原型、示例内存数据。不是生产版本验收。

## 已执行

- Vite 构建通过；未安装新依赖、未改现有应用入口。
- 浏览器逐页检查今天、饮食、训练、历史、我的：320 / 375 / 414 / 768 / 1440 CSS px 共 25 个组合，未发现内容横向越界。
- 目视查看手机概览、饮食卡片、窄屏训练、历史列表、平板和桌面概览；修改了手机概览留白。
- 炒鸡蛋份量 100 → 50 克，180 → 90 千卡，餐食合计 400 → 310。西兰花替换白菜、补充鸡蛋与豆浆，合计与列表同步。
- 删除餐食：取消保留、确认移除。补充保存后“完成”返回列表，不在同一餐详情循环。
- 手动添加默认显示全部 14 种示例食物，常用前置；选择后返回再进入仍保留。未保存不创建餐食。
- 拍照输入有 `capture="environment"`，相册输入无 capture。模拟上传失败保留预览；识别失败可重试到示例结果。
- 训练整次记录：改深蹲 3 → 1 组，返回再进入保留草稿；保存后计划完成/剩余更新。9/19 无计划，切回 9/18 原计划保留。
- 身体新增同日测量显示最新值；修改 65.4 → 65.3、删除后回到前次 65.5。弹层保存失败显示就地错误，64.8 输入可直接恢复正常并重试。
- 模拟注册、基础资料、身体、目标、跳过提醒可通；注册未默认开启识别。后续设置触发授权弹层，取消后开关仍关闭。
- Escape 关闭原生弹层并将焦点返回“记录体重”；浏览器错误日志为空。
- 刷新恢复演示状态，交付预览不残留测试输入。

## 明确未验证／仅占位

- 未使用实体 Android / iOS 摄像头、系统相册、软键盘；视口模拟与输入属性检查不能替代真机验收。
- AI 为固定结果模拟，没有识别准确率结论、网络上传、真实账号、数据持久化或生产 API。
- 体重趋势可交互；饮食／训练趋势、密码、导出、通知、反馈、账号删除目前为入口与说明，不冒充完整功能。
- 示例计划库仅一份可复用样例，完整多方案管理与生产领域规则留给设计确认后的正式接入。
- 没有提交、推送 GitHub、创建 SSH 任务或更改服务器。

## 建议评审顺序

今天 → 饮食 → 拍照／相册 → 使用示例照片 → 修改份量／换食物 → 删除餐食 → 手动添加 → 训练 → 我的身体。

先评估视觉层级和操作是否顺手，再确定正式实现范围。
